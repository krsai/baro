<!-- -->
# 프로젝트 개발 가이드

이 문서는 재봉 공장 ERP 프로그램 개발을 위한 기준과 합의된 사항을 정리합니다. 프로젝트 참여자는 이 문서를 기준으로 개발을 진행합니다.

## 1. 프로젝트 개요

- **프로젝트명**: 재봉 공장 ERP 시스템
- **목표**: 재봉 공장의 생산, 인사, 재고 등 전반적인 업무를 관리하는 ERP 프로그램을 개발합니다.
- **주요 소통 언어**: 한국어

## 2. 기술 스택 (Tech Stack)

### 2.1. 프론트엔드
- **현재 주요 언어**: JavaScript / JSX (일부 `.ts` 사용)
- **목표 언어**: TypeScript (`.ts`/`.tsx` 중심으로 전환)
- **프레임워크**: React (Vite 기반)
- **UI 라이브러리**: MUI (Material-UI)
- **라우팅**: React Router
- **스타일링**: Emotion (`sx` prop) / MUI

#### 2.1.1. TypeScript 전환 목표와 이유
- **목표**: 프론트엔드를 단계적으로 TypeScript로 전환하여 런타임 오류를 사전에 줄입니다.
- **이유 1 (안정성)**: API 응답/도메인 모델(Style, Order, Assignment) 타입을 고정해 회귀를 줄일 수 있습니다.
- **이유 2 (리팩토링 안전성)**: 대규모 화면(주문/배정) 수정 시 타입 오류로 영향 범위를 빠르게 확인할 수 있습니다.
- **이유 3 (협업 효율)**: 계약(Contract) 기반 개발이 가능해져 프론트-백엔드 연동 실수를 줄일 수 있습니다.
- **전환 원칙**:
  - 신규 파일은 가능하면 `.ts`/`.tsx`로 작성합니다.
  - 공용 훅/유틸/도메인 타입부터 우선 전환합니다.
  - 기존 `.jsx`는 기능 변경 시점에 점진 전환합니다.

### 2.2. 백엔드 기술 스택 구성 (Codex 전달용)

#### 2.2.1. 전체 아키텍처
- 프론트엔드는 Supabase에 직접 연결하지 않는다.
- 모든 데이터 접근과 비즈니스 로직은 자체 백엔드 API를 통해서만 처리한다.
- 흐름: `Frontend(React) -> Backend(Node.js + TypeScript + Express) -> PostgreSQL`

#### 2.2.2. 데이터베이스 구성
- 메인 DB는 PostgreSQL.
- Supabase는 PostgreSQL 호스팅 용도로만 사용 가능.
- Supabase Auth는 **소셜 로그인** 용도로만 사용한다.
- Realtime / Edge Function / 자동 API 기능은 사용하지 않는다.
- 결론: Supabase = DB 서버 역할만 수행.

#### 2.2.3. 백엔드 기술 스택
- Runtime: Node.js
- Language: TypeScript
- Framework: Express
- ORM: Prisma
- 인증/권한: 백엔드에서 직접 처리 (JWT 또는 Session)
- 공정, 급여, 전환시간(DT), 인센티브, 정산 등 **확정/정산 기준 계산 로직의 최종 권한은 백엔드**가 가진다.
- 프론트엔드 계산은 UX용 시뮬레이션으로 허용하되, 확정 시에는 백엔드가 재검증/고정 저장한다.

#### 2.2.4. 의도적으로 사용하지 않는 것
- 프론트엔드에서 Supabase SDK 직접 사용 ❌ (소셜 로그인 예외)
- RLS 기반 비즈니스 로직 처리 ❌
- SQL 또는 클라이언트 단 계산 로직 ❌

#### 2.2.5. 이유
- 계산 로직이 많은 ERP 형태 시스템이기 때문에, 백엔드 중심 구조가 필요함

#### 2.2.6. 최종 요약
- Node.js + TypeScript + Express + Prisma + PostgreSQL
- (Supabase는 선택적으로 PostgreSQL 호스팅만 사용)

### 2.3. 멀티테넌트(다중 회사) 기본 설계
- 목표: 향후 여러 회사에 판매 가능하도록 최소한의 테넌트 분리 기준을 미리 정의한다.
- 방식: 단일 DB/스키마 + 테넌트 컬럼(`orgId`) 방식으로 분리한다.
- 기준 엔티티: `Organization`(회사)을 테넌트의 기준으로 둔다.
- 구독 관리: 각 `Organization`은 1개의 `OrganizationSubscription` 레코드를 가진다. (상태: `NOT_SUBSCRIBED`/`TRIAL`/`ACTIVE`/`GRACE`/`SUSPENDED`)
- 적용 범위: 주요 도메인 테이블은 모두 `orgId`를 필수로 가진다.
- 접근 제어(기본): 모든 API는 현재 로그인 사용자의 `orgId`로만 조회/저장한다.
- 접근 제어(거래): 주문/관계 등 거래성 데이터는 관계로 연결된 상대 조직만 접근 가능하다.
- 초기 가입 흐름: 가입 직후에는 소속 없음 상태이며, 관리자가 배정해야 역할과 접근이 활성화된다.
- 유니크/인덱스: 조직 범위 유니크 제약으로 설계한다. (예: `businessNumber + orgId`)
- 데이터 분리 책임: RLS가 아니라 백엔드에서 테넌트 분리를 강제한다.

### 2.4. 멀티 플랫폼(고객사도 사용하는 구조) 설계
- 목표: 고객사와 봉제공장이 모두 앱을 사용하고, 고객사가 여러 공장에 의뢰할 수 있는 플랫폼 구조를 지원한다.
- 핵심 전제: "고객사"와 "봉제공장"은 모두 동일한 `Organization`(회사) 엔티티로 관리한다.
- 조직 구분: `Organization.type`으로 제조사/브랜드를 구분한다. (예: `MANUFACTURER`, `BRAND`)
- 회사 코드: `Organization.code`는 제조사/브랜드 공통 필드이며, **4자리 영문 대문자 약어**로 관리한다. 전체 조직에서 **유니크**해야 한다. (예: `BRVN`, `TSKR`)
- 사용자 소속/권한: `OrgMembership`로 사용자의 조직 소속과 역할/상태를 관리한다.
- 고객 구독 상태: `OrganizationSubscription`으로 조직 단위 구독 상태와 연락 이메일(멤버십/정산)을 관리한다.
- 공장 소유: `Factory`는 제조사 `Organization`에 소속된다. (`factory.orgId`)
- 제한: `Organization.type = BRAND`는 공장을 갖지 않는다. 공장 정보는 `MANUFACTURER`에서만 등록/조회한다.
- 관계/거래: 고객사-제조사 관계는 `OrgRelationship`으로 관리한다. (`Relationship`/`Account` 용어는 사용하지 않는다.)
- 고객(브랜드)은 `Organization.type = BRAND`를 기준으로 한다.
- 제조사-브랜드 연결은 `OrgRelationship`(예: `manufacturerOrgId`, `brandOrgId`) 테이블에 저장한다.
- 고객 관리(제조사 관점): 로그인한 제조사의 `orgId` 기준으로 연결된 브랜드만 조회/표시한다. (멀티테넌트 분리 1차 기준)
- 고객 관리 화면 표시 정보: 브랜드 기본 정보 + 관계 전용 필드(담당자/연락처/이메일 등).
- 고객 코드: 고객 관리 화면의 **고객 코드**는 브랜드 `Organization.code`와 동일하며, 제조사도 같은 규칙의 회사 코드를 가진다.
- 고객 등록: 제조사에서 고객을 등록할 때 `brandOrgId`가 없으면 **브랜드 `Organization`을 자동 생성**하고 `OrgRelationship`으로 연결한다.
- 주문 구조: 주문은 반드시 `buyerOrgId`(고객사)와 `sellerOrgId`(제조사)를 가진다.
- 다중 공장 의뢰: 주문을 공장별로 분할하는 방식을 기본으로 한다. (예: 주문을 공장별로 나누어 생성하거나 `OrderSplit` 개념으로 분배)
- 접근 제어: 모든 조회/저장은 로그인 사용자의 `orgId`가 `buyerOrgId` 또는 `sellerOrgId`에 포함되거나, 관계로 연결된 경우만 허용한다.
- 용어 통일: `Company`/`Corporation` 용어는 사용하지 않고 `Organization`으로 통일한다.
- 예시 매핑: "바로"는 `Organization`이며, 역할에 따라 `MANUFACTURER` 또는 `BRAND`로 정의한다. 사용자는 `OrgMembership`로 연결한다.

## 3. 프로젝트 구조

- `frontend/`: 프론트엔드 프로젝트 루트입니다.
- `frontend/src/pages/`: 각 페이지(화면) 컴포넌트를 위치시킵니다. (예: `Auth/Login.jsx`, `App/Style.jsx`)
- `frontend/src/components/`: 재사용 공통 컴포넌트를 위치시킵니다.
- `frontend/src/layouts/`: 페이지 공통 Layout 컴포넌트를 위치시킵니다.
- `frontend/src/context/`: 전역 상태(Context) 코드를 위치시킵니다.
- `frontend/src/theme/`: MUI 테마 커스터마이징 코드를 위치시킵니다.
- `frontend/src/router.jsx`: 프론트 라우팅 설정을 정의합니다.
- `backend/`: 백엔드 프로젝트 루트입니다.
- `backend/src/`: Express API 서버 코드를 위치시킵니다.
- `backend/prisma/schema.prisma`: DB 스키마 기준 파일입니다.
- `docs/test-initial-state.md`: 테스트 초기화 기준 상태(조직/계정/권한/관계/공장·라인)의 단일 기준 문서입니다.

## 4. 개발 컨벤션 및 규칙

*이 섹션은 프로젝트 진행에 따라 지속적으로 업데이트됩니다.*

### 4.1. UI 개발 및 스타일링
- **UI 라이브러리**: 모든 UI 컴포넌트는 MUI를 우선적으로 사용하여 구현합니다.
- **스타일링 및 우선순위**:
  1. **전역 스타일**: `frontend/src/styles/` 내의 CSS 파일들은 `frontend/src/index.jsx`에서 최상단에 임포트하여 애플리케이션 전체의 기본 스타일을 설정합니다.
  2. **개별 스타일**: 각 페이지나 컴포넌트에만 적용되는 스타일은 해당 파일 내에서 임포트하거나 정의합니다.
  3. **우선순위**: `전역 CSS` < `개별 페이지/컴포넌트 CSS` < `MUI sx prop` 순서로 적용되어, 개별 설정이 전역 설정을 덮어쓰도록 합니다.
  4. **권장 사항**: 컴포넌트 단위의 스타일링은 `sx` prop 사용을 권장합니다. 복잡한 스타일은 별도 CSS로 분리할 수 있습니다.
- **컨테이너 스타일**: 콘텐츠를 감싸는 컨테이너(예: `Paper`)는 그림자(`elevation`) 사용을 지양하고, 대신 외곽선(`variant="outlined"`)을 사용하여 평평하고 인더스트리얼한 디자인 통일성을 유지합니다. 이는 애플리케이션에 보다 깔끔하고 전문적인 느낌을 부여합니다.

  **적용 예시:**
  ```jsx
  // 지양 (Avoid)
  <Paper elevation={3} sx={{ p: 2 }}>
    ...콘텐츠...
  </Paper>

  // 권장 (Recommended)
  <Paper variant="outlined" sx={{ p: 2 }}>
    ...콘텐츠...
  </Paper>
  ```

### 4.2. 상태 관리
- 간단한 상태는 컴포넌트 내부의 `useState`를 사용하고, 여러 컴포넌트에 걸쳐 사용되는 전역 상태는 `Context API`를 활용합니다.

### 4.3. 파일 및 컴포넌트 명명
- 컴포넌트 파일명은 `PascalCase`를 따릅니다. (예: `MyComponent.jsx`)
- 일반적인 함수나 변수는 `camelCase`를 따릅니다.
- **약어 사용**: `Attribute`는 `Attr`로 축약하여 사용합니다. (예: `AttrBoard.jsx`, `AttrDetail.jsx`)

### 4.4. API 연동
- 프론트엔드는 비즈니스 데이터에 대해 로컬 저장소를 1차 저장소로 사용하지 않고, 백엔드 API를 1차 소스로 사용합니다.
- `localStorage`는 아래 예외 목적에서만 사용합니다.
  - 스타일 초기 1회 이관 소스 (`baro_styles_v1` -> `POST /styles/import`)
  - 주문/작업기록 드래프트 및 화면 편의용 임시 저장
  - 휴일 캘린더 임시 저장
  - 인증 개발 편의 플래그(개발 환경 한정)
- 스타일(Style) 도메인의 기준 API는 다음과 같습니다.
  - `GET /styles`: 스타일 목록 조회
  - `GET /styles/:styleId`: 스타일 상세 조회
  - `POST /styles`: 스타일 등록
  - `PUT /styles/:styleId`: 스타일 수정
  - `DELETE /styles/:styleId`: 스타일 삭제
  - `POST /styles/import`: 로컬 데이터 일괄 이관(초기 1회)
- 주문(Order) 화면은 주문 본문 저장은 기존 로컬 저장을 유지하되, 스타일 선택 목록은 `/styles` API에서 조회합니다.
- 신규 스타일 저장 성공 후 기본 이동은 상세가 아니라 스타일 목록(`/style`)으로 이동합니다.
- 스타일 저장소/이관의 상세 규칙은 `7.2.2 스타일 저장소 전환 규칙`을 단일 기준으로 따릅니다.

### 4.5. 코드 스타일
- 프로젝트에 설정된 Prettier와 ESLint 규칙을 따릅니다. (설정 필요)

### 4.6. 전역 스타일 관리
애플리케이션의 전역 CSS 스타일은 `frontend/src/styles/` 디렉토리 내에서 기능별로 분리된 파일들로 관리됩니다. 이 파일들은 `frontend/src/index.jsx`에서 임포트되어 전역적으로 적용됩니다.

- **`base.css`**: 애플리케이션 전반의 기본 스타일을 정의합니다. (예: `body`, `html` 태그 스타일, 기본 폰트 설정, **전체 텍스트 선택 방지** 등)
- **`theme.css`**: 색상, 폰트 크기, 그림자 등 디자인 시스템의 테마와 관련된 변수나 클래스를 정의합니다.
- **`layout.css`**: 주요 레이아웃 구조(예: 헤더, 사이드바, 컨텐츠 영역)와 관련된 스타일을 정의합니다.
- **`table.css`**: 테이블 컴포넌트의 공통 스타일을 정의합니다.
- **`form.css`**: 입력 필드, 버튼 등 폼(Form) 요소들의 공통 스타일을 정의합니다.

### 4.7. 샘플 데이터 및 더미 데이터
- 프론트엔드에서 사용하는 샘플 데이터는 반드시 **“백엔드 API 응답 형태”**로 만듭니다.
- 즉, 더미 데이터 구조는 실제 DB/API 스키마를 반영해야 하며, 추후 백엔드 개발 시 그대로 참고할 수 있어야 합니다.
- 임시 하드코딩 데이터라도 API 응답을 흉내 내는 구조로 작성하고, 실제 API 호출로 교체 가능하도록 구성합니다.
- `Size/Gender`는 속성 관리 API 대상이 아니라, 현재 `frontend/src/constants/productAttributes.js` 하드코딩 상수를 기준으로 사용합니다.

### 4.8. 프론트엔드 검증(빌드) 운영 규칙
- 목적: 개발 속도를 유지하면서 필수 안정성만 보장하기 위해, 프론트엔드 변경 시 검증 단계를 경량화합니다.
- 기본 원칙: **빠른 검증 우선, 전체 빌드 최소화**.
- 컴포넌트/로직 수정 직후에는 전체 프로덕션 빌드를 매번 실행하지 않고, 코드/임포트/기본 런타임 오류 중심의 빠른 검증을 우선 수행합니다.
- `npm --prefix frontend run build`는 기본적으로 작업 마무리 단계에서 **최종 1회만** 실행합니다.
- 예외적으로 다음 변경은 중간 빌드를 허용합니다.
  - 라우팅 구조 변경
  - 공용 유틸/공용 상태/전역 설정 변경
  - 번들/빌드 설정 변경
- 빠른 1차 확인이 필요할 때는 경량 빌드를 우선 사용합니다.
  - 예: `npm --prefix frontend run build -- --minify=false`
- 최종 머지/배포 전에는 반드시 정식 빌드(`npm --prefix frontend run build`)로 최종 검증합니다.

### 4.9. 공용 유틸 재사용 규칙
- 동일 도메인 책임(저장/조회/정규화/포맷)은 단일 유틸 또는 단일 API 모듈에서 처리합니다.
- 세 자릿수 콤마 포맷은 `frontend/src/utils/numberFormat.js`를 단일 기준으로 사용합니다.
- 시간(초) 표시는 `processTime`이 `numberFormat`을 재사용하는 구조를 유지합니다.
- 속성(`/attributes`) 조회/저장은 `frontend/src/utils/attributeApi.js`를 단일 경로로 사용합니다.
- 신규 유틸 추가 전 `frontend/src/utils`에서 기존 기능 검색을 먼저 수행합니다. (`rg` 권장)

## 5. 메뉴 구조 (Menu Structure)

*이 섹션은 `frontend/src/layouts/MainLayout.jsx`의 `menuItems`를 기준으로 한 좌측 네비게이션 메뉴의 실제 구조를 정의합니다.*

- **대시보드 (Dashboard)**
- **영업 관리 (Sales Management)**
  - 주문 (Order)
  - 스타일 (Style)
- **생산 관리 (Production Management)**
  - 작업 배정 (Task Assignment)
  - 작업 계획 협의 (Production Plan Review)
  - 작업 기록 (Work History)
- **조직 관리 (Organization Management)**
  - 사업체 관리 (Business Management)
  - 라인 관리 (Line Management)
  - 직원 관리 (Employee Management)
  - 고객 관리 (Customer Management)
- **기본 정보 (Basic Information)**
  - 속성 관리 (Attribute Management)
  - 권한 관리 (Permission Management)
  - 휴일 관리 (Holiday Management)
- **시스템 설정 (System Settings)**
  - 멤버쉽 관리 (Membership Management)
  - 접근 정책: `SYSTEM_ADMIN` 전용 (`SYSTEM` 엔트리). 조직 역할(`ADMIN`/`OPERATOR`/`ACCOUNTANT`/`WORKER`)에서는 메뉴 비노출.

## 6. 프론트엔드 구조 규칙 (Frontend Architecture Rules)

일관성 있는 개발을 위해 다음 구조 규칙을 모든 도메인에 적용합니다.

### 6.1. Board / Detail 화면 구조 규칙

- 각 도메인의 라우팅 진입점은 `frontend/src/pages/App/{Domain}.jsx` 파일이다. (예: `Style.jsx`, `Assign.jsx`)
- 진입점 이후 최초로 렌더링되는 화면은 항상 Board이다.
- Board는 해당 도메인의 업무 허브 역할을 하며, 리스트(List), 검색, 필터, 요약 정보 등을 포함할 수 있다.
- Board는 단일 객체를 생성하거나 저장하지 않으며, Detail 화면으로의 진입만 담당한다.

- Detail은 Board의 하위 개념이다.
- Detail은 단일 객체의 생성/수정/상세 설정을 담당하는 작업 세션 단위 화면이다.
- Detail은 항상 Board를 통해 진입한다.

- Detail의 실제 UI는 `detail/` 하위 컴포넌트로 분리한다.
- Detail 진입점은 상태 관리 및 저장 로직을 총괄하며, 하위 컨텐츠는 UI와 입력만 담당한다.

**폴더 및 파일 구조 예시**

- `frontend/src/pages/App/Style.jsx` : Style 도메인 라우팅 진입점
- `frontend/src/pages/App/style/StyleBoard.jsx` : Style 도메인의 Board 화면
- `frontend/src/pages/App/style/StyleDetail.jsx` : Style 도메인의 Detail 진입점
- `frontend/src/pages/App/style/styleDetail/StyleInfo.jsx` : Detail 하위 컨텐츠(탭 단위)

### 6.2. 기타 구조 규칙
1.  **라우트와 화면의 분리**: `frontend/src/pages`는 라우팅 진입점 전용입니다. 실제 UI와 로직을 담은 화면(예: `StyleBoard.jsx`, `StyleDetail.jsx`)은 `frontend/src/pages/App/{도메인명}/` 폴더에서 관리합니다.

2.  **Board / Detail 패턴**: 모든 도메인은 Board와 Detail 화면으로 구성됩니다.
    -   **`Board`**: 목록 표시, 검색, `Detail` 화면으로 네비게이션 역할만 수행합니다.
    -   **`Detail`**: 단일 데이터의 생성/수정 세션을 관리하며, 하위 탭들의 저장 로직을 총괄합니다.
    -   `Board`와 `Detail`은 ID로만 데이터를 주고받으며, 상태를 직접 공유하지 않습니다.

3.  **페이지 레이아웃 (Page Layout)**:
    -   **제목 및 구분선 제거**: 상위 탭(Tab) UI에서 페이지 제목을 표시하므로, 개별 페이지 내에서는 제목과 구분선을 제거합니다.
    -   **액션 버튼**: 페이지 상단 우측에 배치하여 일관성을 유지합니다.

4.  **단순성 유지**: 명확한 필요 전까지 과도한 추상화(조기 컴포넌트 분리 등)를 지양하고, 가독성과 실용성을 우선합니다. 리팩토링 시에는 기능 변경 없이 구조 개선에만 집중합니다.

## 7. 애플리케이션 핵심 로직 (Core Application Logic)

이 섹션은 애플리케이션의 핵심 비즈니스 로직과 데이터 흐름을 정의합니다.

### 7.0. 데이터베이스 테이블 요약 (DB Table Summary)

*현재 Prisma 스키마 기준으로 주요 테이블의 용도와 관계를 요약합니다.*

1. **Organization**
   - 용도: 제조사/브랜드 공통 회사 엔티티
   - 핵심 필드: `name`, `code`(4자리 영문 대문자, 유니크), `type`(MANUFACTURER/BRAND), 기본 회사 정보
   - 관계: `OrganizationSubscription`, `OrgMembership`, `Factory`, `Employee`, `OrgRelationship`, `Attr*`

2. **OrganizationSubscription**
   - 용도: 조직 단위 SaaS 구독 상태/연락처 관리 (결제/운영 상태)
   - 핵심 필드: `orgId`(유니크), `status`(`NOT_SUBSCRIBED`/`TRIAL`/`ACTIVE`/`GRACE`/`SUSPENDED`), `membershipEmail`, `billingEmail`
   - 관계: `Organization`(1:1)

3. **OrgMembership**
   - 용도: 조직 내 사용자 소속/승인/권한 관리 (가입 신청 → 승인)
   - 핵심 필드: `orgId`, `email`, `role`, `status`, `requestedAt`, `approvedAt`
   - 관계: `Organization`(N:1), `Employee`(1:1, 선택)

4. **Factory**
   - 용도: 제조사 조직의 공장
   - 핵심 필드: `orgId`, `name`, 임금 관련 필드
   - 관계: `Organization`(N:1), `Employee`(1:N)

5. **Line**
   - 용도: 공장 내 생산 라인
   - 핵심 필드: `orgId`, `factoryId`, `name`, `managerEmployeeId`, `isActive`
   - 관계: `Factory`(N:1), `LineAssignment`(1:N)

6. **LineAssignment**
   - 용도: 라인 ↔ 직원 배정 이력 (현재 배정은 `endAt = null`)
   - 핵심 필드: `lineId`, `employeeId`, `startAt`, `endAt`
   - 관계: `Line`(N:1), `Employee`(N:1)

7. **Employee**
   - 용도: 제조사 조직의 직원 정보 (승인된 멤버십과 연결)
   - 핵심 필드: `orgId`, `orgMembershipId`, `factoryId`, `roleId`, 인사/급여 관련 필드
   - 관계: `Organization`, `OrgMembership`, `Factory`, `AttrRole`, `LineAssignment`

8. **OrgRelationship**
   - 용도: 제조사 ↔ 브랜드 고객 관계
   - 핵심 필드: `manufacturerOrgId`, `brandOrgId`, 관계 전용 필드(담당자/연락처/메모 등)
   - 관계: `Organization`(제조사/브랜드)
   - 참고: 고객 코드 표시는 브랜드 `Organization.code`를 사용

9. **AttrColor / AttrCategory / AttrRole / AttrProcess**
   - 용도: 조직별 기준 속성 데이터(공정/역할/분류 등)
   - 핵심 필드: `orgId`, `code`, `name`
   - 관계: `Organization`(N:1)
   - 참고: `Size/Gender`는 현재 DB 속성 테이블이 아니라 프론트 상수(`productAttributes`)로 관리

10. **Style**
   - 용도: 스타일 마스터(기본정보/공정/BOM 스냅샷성 데이터)
   - 핵심 필드: `orgId`, `styleId`, `styleCode`, `name`, `customer`, `processes(Json)`, `bom(Json)`
   - 관계: `Organization`(N:1)
   - 제약: `orgId + styleId` 유니크

11. **SystemUser**
   - 용도: 시스템 전역 사용자 (플랫폼 운영자용)

### 7.1. 조직/권한/구독 구조 (Organization, Access, Subscription)

1.  **조직 (Organization)**: 시스템의 최상위 회사 단위입니다. 제조사/브랜드 구분은 `Organization.type`으로 합니다. (예: `MANUFACTURER`, `BRAND`)
    - 조직 코드는 `Organization.code`로 관리하며 4자리 영문 대문자 약어를 사용합니다. (예: `BRVN`, `TSKR`)
2.  **공장 (Factory)**: 제조사 조직에 소속된 생산 단위입니다. (예: `하노이 공장`) 모든 직원은 공장에 소속됩니다.
    - 공장 정보는 `MANUFACTURER` 조직에만 존재합니다. `BRAND` 조직은 공장이 없습니다.
    - 공장 급여 계산: 목표 월 급여 입력 시 `초당 급여 = 월급 / 26일 / 8시간 / 60 / 60` (소수점 2자리 표시).
3.  **라인 (Line)**: 공장 내에 존재하는 물리적/논리적 생산 라인입니다. (예: `A라인`, `B라인`)
4.  **직원 (Employee)**: 특정 공장에 소속된 사용자입니다. 직원은 특정 라인에 배치될 수 있습니다.
5.  **권한 모델 (Access Role)**:
    - `OrgMembership.role`은 시스템 접근 권한 역할입니다. (`ADMIN`, `OPERATOR`, `ACCOUNTANT`, `WORKER`)
    - **관리자(Admin)**는 업체 기능의 최상위 권한으로, 기본 관리자 계정은 수정/삭제 불가 정책을 적용합니다.
    - 그 외 권한/역할 분리는 조직 관리자(Admin)가 메뉴 단위 권한 정책에 따라 확장합니다.
    - `시스템 설정 > 멤버쉽 관리`는 조직 권한과 분리된 시스템 권한 영역이며, `SYSTEM_ADMIN`만 접근합니다.
6.  **업무 역할 (Job Role)**:
    - `AttrRole`은 생산/업무 직무 분류용이며, 권한(Role)과 분리합니다.
    - 조직 관리자(Admin)가 업체 내부 운영 목적에 맞게 역할 종류를 추가/수정할 수 있습니다.
7.  **고객사 (Customer)**: 제품 생산을 의뢰하는 회사입니다. `Organization.type = BRAND`에 해당합니다. (예: `더산`)

### 7.1.1. 사용자 소속 가입/승인 흐름 (OrgMembership)
- 사용자는 소셜 로그인 이후 **조직 미소속 상태**로 시작한다.
- 사용자는 시스템에 등록된 `Organization`을 선택하여 **가입 신청**을 한다. (상태: `PENDING`)
- 조직의 운영 관리자(해당 조직 `ADMIN`)가 승인하면 소속이 확정된다. (상태: `ACTIVE`)
- 제조사 조직은 승인 후 공장/라인 배정 입력이 가능해진다. 브랜드 조직은 공장 배정이 없다.
- 시스템 운영자(System Admin)는 플랫폼 전역 권한이며, 조직 운영자와 구분된다.

### 7.1.2. 조직 구독 흐름 (OrganizationSubscription)
- 각 `Organization`은 **반드시 1개의 구독 레코드**를 가진다.
- 기본 상태는 `NOT_SUBSCRIBED`이며, 조직/거래처 데이터(`Organization`, `OrgRelationship`)는 구독과 별개로 유지된다.
- `TRIAL`은 기본 30일이며, 만료 전 **수동 승인**으로 `ACTIVE` 전환한다.
- `ACTIVE` 전환 시 `membershipEmail`과 `billingEmail`이 모두 필요하다. (동일 이메일 허용)
- `GRACE`는 유예 상태이며, 미승인/미정산 지속 시 `SUSPENDED`로 전환한다.
- `SUSPENDED`는 조직 단위 전체 기능 차단 상태로 취급한다.
- 현재 BARO는 `ACTIVE` 상태로 운영하며, 멤버십/정산 이메일 모두 `baro.garment@gmail.com`을 기본값으로 사용한다.

#### 7.1.3. 조직 이동/퇴사 관리 (Future Work)
*향후 구현 예정. 현재는 정책만 기록.*
- `OrgMembership`는 조직별 이력 레코드로 유지한다. 퇴사는 삭제하지 않고 `TERMINATED`로 표시한다.
- 조직 이동 시 흐름:
  - 이전 조직 `OrgMembership` → `TERMINATED` (또는 `SUSPENDED`)
  - 새 조직 `OrgMembership` → 승인 후 `ACTIVE`
  - 제조사 조직일 경우 `Employee`는 새 `orgMembershipId`로 생성/갱신
- 다중 소속 정책은 추후 결정한다.
  - 기본안: 단일 ACTIVE 소속만 허용 (승인 시 기존 ACTIVE 종료)
  - 대안: 복수 ACTIVE 소속 허용 (로그인 후 조직 선택/전환 UI 필요)
- 장기적으로는 이메일 대신 Auth user id 기반 User 테이블을 도입해 OrgMembership이 `userId`를 참조하도록 개선한다.

### 7.2. 스타일 및 공정 관리 (Style & Process Management)

1.  **스타일 (Style)**: 특정 고객사에 속한 의류 모델입니다. 하나의 스타일에는 여러 생산 공정이 포함됩니다.
    - **이미지 관리**: 스타일 관련 이미지를 업로드 및 삭제할 수 있으며, 삭제 시 확인 절차(Confirm)를 거칩니다.
    - **데이터 통합**: 기본 정보, 공정 관리(Process), BOM 정보가 하나의 스타일 객체로 통합 관리됩니다.
2.  **공정 (Process)**: 스타일을 생산하기 위한 개별 작업 단계입니다.
    - **범위**: 본 앱에서 '공정'은 **봉제 공정**만 의미합니다. 포장, QC(검수) 등 기타 작업은 현재 범위에서 제외합니다.
    - **식별 원칙**: 공정은 **스타일 + 공정 조합**으로 식별합니다. 이름이 같더라도 스타일이 다르면 다른 공정입니다. (예: 스타일 A의 주머니 달기 ≠ 스타일 B의 주머니 달기)
3.  **공정 데이터의 흐름 (Process Data Flow)**:
    -   **공정 등록**: `속성 관리(AttrBoard.jsx)`에서 표준 공정을 등록/수정/삭제하고, 백엔드 `PUT /attributes`로 저장합니다.
        -   **목적**: "주머니 봉제", "봉제 주머니"와 같이 동일한 작업임에도 텍스트가 달라 발생하는 데이터 파편화를 방지하고, 명칭을 통일합니다.
    -   **공정 사용**: `스타일 관리` 공정 정보 화면(`App/style/styleDetail/StyleProcess.jsx`)은 `GET /attributes` 기반 표준 공정 목록을 조회해 선택 옵션으로 사용합니다.
    -   **목표**: 모든 스타일에 걸쳐 동일한 공정에는 일관된 명칭과 기준을 사용하게 하여 데이터의 정합성을 보장합니다.
    -   **활용 계획**: 통일된 공정 명칭을 기반으로 축적된 데이터는 추후 **AT, DT 산출 시 신뢰할 수 있는 통계적 가이드(도움말)**로 활용됩니다. 수집된 데이터를 바탕으로 특정 의류 카테고리(예: '바지')의 특정 공정(예: '주머니 달기')에 평균적으로 소요되는 시간을 분석하여, 신규 스타일의 PT(임시 제안 시간) 설정 시 참고용으로 제공합니다.

### 7.2.1. 공정 버전 및 스냅샷 (Process Versioning & Snapshot)
- **버전 관리**: 스타일 내 공정 구성(공정 목록, 순서, 설명)은 수정될 수 있으므로 `ProcessRevision` 개념으로 버전 관리합니다.
- **스냅샷 고정**: 작업 배정/작업 기록/급여 지급 내역은 **해당 시점의 공정 버전(Revision)** 을 스냅샷으로 고정합니다.
  - 이후 공정이 추가/삭제/수정되더라도, 이미 시작된 작업과 해당 작업의 지급/통계는 **고정된 스냅샷 기준**으로 유지됩니다.
- **목적**: 공정 변경으로 인한 계산 기준의 흔들림을 방지하고, 기록/급여/통계의 일관성을 보장합니다.

### 7.2.2. 스타일 저장소 전환 규칙 (Style Source of Truth)
- 스타일 데이터의 단일 진실 소스(Source of Truth)는 백엔드 DB(`Style` 테이블)입니다.
- 프론트엔드의 `localStorage` 스타일 데이터는 영구 저장소가 아니며, 초기 부트스트랩용 임시 데이터로만 취급합니다.
- 마이그레이션 규칙:
  - 앱 최초 진입 시 서버 스타일이 비어 있고 로컬 스타일이 존재하면 `POST /styles/import`를 1회 실행합니다.
  - 서버 데이터가 이미 존재하면 로컬 이관은 수행하지 않습니다.
  - 이관 플래그가 있으면 재시도하지 않습니다.
- 운영 규칙:
  - 스타일 등록/수정은 반드시 `/styles` API를 통해 처리합니다.
  - 스타일 목록/상세 조회도 반드시 `/styles` API를 통해 처리합니다.
  - 저장 직후 목록 화면에서 결과를 확인하는 흐름을 기본으로 합니다. (`/style` 이동)

### 7.3. 핵심 시간 관리 모델: PT / AT / DT / CT (역할 기반)

> **⚠ 주요 개념 확정 사항 (코드/문서 작업 시 반드시 참고)**
> 1. **표준시간/보정계수 방식 폐기**: 공장별 제안시간 산출을 위한 과거 표준시간·보정계수 개념을 사용하지 않습니다.
> 2. **AT 재정의**: 기존 "순수 공정 시간(DT 제외)"에서 → **"실제 작업 시간(부대작업 포함, 전환시간만 제외)"**으로 재정의되었습니다. AT는 제안 시간으로 직접 사용됩니다.
> 3. **DT 재정의**: 기존 "일반 비생산 시간"에서 → **"공정 전환 시간(Transition Time)"**으로 한정되었습니다. 전환 횟수로 명시적 추적이 가능합니다.
> 4. **PT/AT 공장 공통화**: PT/AT는 **스타일-공정 단위의 단일 공통값**으로 관리합니다. 공장/라인별 기준 모드는 사용하지 않습니다.
> 5. **업계 표준(SAM/GSD) 미채택**: 비용과 복잡성 문제로 업계 표준 방식을 사용하지 않으며, 자체 회귀 분석 방식을 사용합니다.

이 모델은 PT, AT, DT, CT 개념을 **역할 중심으로 명확히 구분**하여, 급여 사전 합의 구조를 설명합니다. 이 섹션은 코드 구현이 아닌, 개념의 역할과 경계를 정확히 이해하는 것을 목적으로 합니다.

#### 1. 전체 설계 철학

- **사전 합의 원칙**: 급여는 '사후 정산'이 아니라 **사전 합의**를 원칙으로 합니다. 작업이 시작된 이후에는 급여 재산정이나 차감이 발생하지 않습니다.
- **데이터의 역할**: 사후에 수집된 생산 데이터(작업 기록)는 급여를 조정하거나 차감하는 데 사용되지 않습니다. 대신, **향후 작업을 위한 기준(AT, DT)을 개선하고 학습하는 데이터**로만 활용됩니다.
- **지급 기준의 단순화**: 작업자에게는 복잡한 계산 과정(AT, DT 등)을 노출하지 않습니다. 작업 전 상호 합의된 **최종 지급 기준 시간(CT)**만 명확히 제시됩니다.

#### 2. 제안 기준과 확정 기준: PT / AT / CT

**PT(Provisional Time)**와 **AT(Actual Time)**는 지급 기준의 **제안값**입니다. 실제 지급 임금을 산출하는 최종 기준은 **CT(Contracted Time)**입니다.

> `지급 임금 = Σ(확정된 CT × 작업 수량) × 공장 초당 임금 + (전환 횟수 × DT) × 공장 초당 임금`
> `CT`에는 `DT`를 포함하지 않으며, `DT`는 전환 보너스로 별도 가산합니다.

- **PT (Provisional Time, 임시 제안 시간)**
  - **정의**: AT(실제 작업 시간)가 아직 없거나, 신뢰 가능한 AT가 축적되지 않은 신규/특수 작업에 대해 사용하는 **임시 제안 기준 시간**입니다.
  - **역할**: 초기 운영에서 기준을 제안하고, AT 산출을 위한 데이터를 축적하는 데 사용됩니다.
  - **소스**: 공장장의 경험과 판단에 기반하여 설정됩니다. (업계 표준 SAM/GSD 방식은 비용과 복잡성 문제로 채택하지 않음)
  - **스타일-공정 종속성**: PT는 **'스타일-공정' 조합 단위로 관리**됩니다. 공장별로 구분하지 않으며, 모든 공장에 공통으로 적용됩니다.

- **AT (Actual Time, 실제 작업 시간)**
  - **정의**: 특정 스타일의 **특정 공정 1단위**를 생산하는 데 실제로 걸리는 **평균 작업 시간**입니다.
  - **포함 범위**: AT는 순수한 재봉 작업뿐만 아니라, **실의 엉킴 처리, 위치 조정, 재작업 등 모든 실질적인 작업 과정**을 포함합니다. 다만, 공정 전환 시간(DT)은 제외됩니다.
  - **스타일-공정 종속성**: AT는 **'스타일-공정' 조합을 기준으로만 관리**됩니다. 동일한 공정명(예: '주머니 달기')이라도 스타일에 따라 난이도와 작업 밀도가 다르므로, AT는 절대 공유되지 않습니다.
  - **공장 공통**: PT와 마찬가지로 AT도 **공장별로 구분하지 않습니다**. 모든 공장의 작업 기록을 종합하여 단일 AT를 산출합니다.
  - **산출**: 여러 공장의 작업 기록(생산량, 총 소요 시간, 전환 횟수)을 회귀 분석하여, AT와 DT를 수학적으로 분리하여 산출합니다. (아래 7.3.1 참고)
  - **공정명 표준화**: 공정명 표준화의 목적은 AT를 통일하기 위함이 아니라, 신규 스타일의 PT를 설정할 때 참고 가능한 비교 기준을 제공하기 위함입니다.

- **CT (Contracted Time, 최종 합의 지급 기준 시간)**
  - **정의**: PT/AT 제안값을 바탕으로 생성되고, **라인장 검토(동의/조정 요청) 후 확정되는 실제 지급 기준 시간**입니다.
  - **기본값 규칙**:
    - AT가 있으면 `CT 기본값 = AT` (배정 시점의 최신 AT 기준)
    - AT가 없고 PT만 있으면 `CT 기본값 = PT`
    - CT 기본값에 사용된 값은 `proposalSeconds`로 저장하고, 최종 확정값은 `contractedSeconds(CT)`로 저장합니다.
  - **확정 규칙**:
    - 라인장은 **CT를 최종 확정하지 않으며**, `동의` 또는 `조정 요청`을 수행합니다.
    - 라인장은 조정 요청 시 **공정별 제안 CT(초)**를 직접 입력해 운영팀에 전달할 수 있습니다.
    - 라인장이 동의하면 기본값(PT 또는 AT 제안)이 CT로 확정됩니다.
    - 라인장이 조정 요청하면 `ctStatus = REJECTED`(운영팀 검토 대기)로 전환되며, **관리자/운영자(Admin/Operator)**가 CT를 수정(`ctSource = MANUAL`)하거나 배정을 재조정하여 최종 확정합니다.
    - 조정 요청 시점에는 Assignment를 즉시 해제하지 않고, 운영팀 결정 전까지 현재 라인 배정을 유지합니다.
    - 미합의 상태는 `ctStatus = PENDING`으로 유지하며 배정 최종확정/급여/정산 확정 불가

#### 3. 기준 시간 재료: DT (전환 시간)

**DT(Dead Time)**는 그 자체로 지급 기준이 아니며, 다공정 작업에 대한 **전환 보너스를 산출하기 위한 내부 계산 재료**입니다.

- **DT (Dead Time → Transition Time, 공정 전환 시간)**
  - **정의**: 작업자가 하나의 공정에서 다른 공정으로 전환할 때 발생하는 **준비 및 전환 시간**을 의미합니다.
  - **발생 조건**: 동일 작업자가 여러 공정을 수행할 때만 발생합니다. 단일 공정 작업에는 DT가 발생하지 않습니다.
  - **회사 요청 오버헤드**: DT는 회사가 작업자에게 여러 공정을 요청함으로써 발생하는 추가 시간이므로, 이에 대한 보상(전환 보너스)이 지급됩니다.
  - **산출**: 작업 기록의 전환 횟수를 명시적으로 추적하여, 회귀 분석을 통해 DT를 수학적으로 분리합니다. (아래 7.3.1 참고)
  - **공정성 보장**: DT 보너스 시스템은 단일 공정을 집중적으로 수행하는 작업자와 다공정을 수행하는 작업자 간의 생산량 격차를 공정하게 보상합니다.

#### 4. 제안 기준 선택 규칙 및 적용

제안 기준은 공장 설정값 없이, 작업 데이터 상태만으로 결정합니다.

1.  **공통 선택 규칙 (단일 기준)**:
    - `AT`가 존재하면 `AT`를 제안 기준으로 사용합니다.
    - `AT`가 없고 `PT`만 존재하면 `PT`를 제안 기준으로 사용합니다.
    - `PT/AT`가 모두 없으면 `proposalBasis = NONE`으로 처리하며 배정할 수 없습니다.

2.  **작업 배정 시점 확정 절차**:
    - 작업 배정 전 시뮬레이션은 현재 선택된 제안 기준(AT 또는 PT)으로 계산합니다.
    - 배정 확정 시점에는 라인장 검토(동의/조정 요청) 절차를 거쳐 **CT를 최종 확정**합니다.
    - 라인장이 제안 기준에 동의하면 CT는 제안값(AT 또는 PT)을 그대로 사용합니다.
    - 조정 요청 시 운영팀이 공정별 제안 CT를 검토하고, CT 수동 조정 또는 재배정으로 합의값을 확정합니다.
    - **일정 계산 기준은 PT/AT 제안값**, **최종 급여 기준은 CT + DT 보너스**입니다.

#### 5. 데이터 갱신 및 확정 시점 (Data Update & Freezing Timing)
사용자가 우려하는 "변동성"과 "급여 안정성"을 모두 만족시키기 위해, 시간 데이터는 **'주기적 갱신(Live)'**과 **'배정 시 확정(Snapshot)'**의 이원화된 구조로 관리합니다.

1.  **공식 반영 트리거 (Official AT Update): 월간 배치**
    -   **시점**: 매월 5일 배치 실행.
    -   **데이터 컷오프**: 전월 말일(`23:59:59`)까지 입력된 작업 기록만 반영합니다.
    -   **표본 단위**: 유효 작업 기록 1건을 표본 1개(`n`)로 계산합니다. (`n`은 당월 신규 건수가 아니라, 업데이트 시점의 롤링 표본 풀 총량)
    -   **표본 조건**: `n >= 80`일 때만 AT 업데이트 후보로 평가합니다. `n < 80`이면 AT는 동결(기존값 유지)합니다.
    -   **누적 원칙**: `n < 80`으로 업데이트를 스킵한 월의 표본도 폐기하지 않고 롤링 표본 풀에 유지하며, 다음 월 평가 시 합산합니다. (단, 롤링 상한 초과 시 가장 오래된 표본부터 제외)
    -   **신뢰도 게이트**: `95% CI 반폭 / 추정 AT <= 10%`일 때만 업데이트합니다.
    -   **저장 방식**: 기존 스타일 공정의 `atSeconds`를 공식값으로 갱신하고, 메타(`atUpdatedAt`, `atSampleCount`, `atSource`)를 함께 저장합니다.
    -   **주의**: 위 조건을 충족하지 못하면 해당 월 업데이트는 스킵하고 기존 `atSeconds`를 유지합니다.

2.  **확정 트리거 (Freezing Trigger): 작업 배정 시 (Assignment Snapshot)**
    -   **시점**: 관리자가 스케줄러에서 작업을 배정(Drop)하고 확정하는 순간.
    -   **로직**: 현재 시점의 제안값(PT/AT)을 기반으로 합의된 `CT`를 배정 건(Assignment)의 **고정 지급 기준**으로 스냅샷 저장합니다.
    -   **영향**: 배정 이후에는 `AT`나 `DT`가 변동되어도, **해당 배정 건의 CT와 급여는 변하지 않습니다.** (일정 계산은 기존 PT/AT 제안값 기준 유지)

#### 6. 핵심 용어 정리

- **PT (Provisional Time)**: AT가 없거나 불확실할 때 사용하는 **임시 제안 시간**. 공장장의 경험에 기반하여 설정. 스타일-공정 단위로 관리하며 공장 공통.
- **AT (Actual Time)**: **실제 작업 시간**. 여러 작업 기록에서 DT를 분리하여 산출한, 특정 `스타일-공정` 1단위를 생산하는 데 걸리는 평균 시간. 실 엉킴, 위치 조정, 재작업 등 모든 실질적 작업 포함. 스타일-공정 단위로 관리하며 공장 공통.
- **DT (Transition Time)**: **공정 전환 시간**. 작업자가 공정을 전환할 때 발생하는 준비 시간. 회귀 분석으로 산출하며, 전환 보너스 계산에 사용.
- **CT (Contracted Time)**: **최종 합의 지급 기준 시간**. 급여/정산은 CT를 기준으로 계산하며, 전환 횟수에 대한 DT 보너스가 별도 추가됨.
- **전환 횟수 (Transition Count)**: 작업 항목 수 - 1로 자동 계산됨. (예: 3개 공정 작업 시 전환 횟수 = 2)
- **운영 화면 분리 규칙**: `작업 배정` 화면은 배정/해제와 일정 시뮬레이션만 담당하며, CT 동의/조정은 `작업 계획 협의` 화면에서 처리합니다.
- **업계 표준 미채택**: SAM(Standard Allowed Minutes), GSD(General Sewing Data) 등의 업계 표준 방식은 비용과 복잡성 문제로 채택하지 않습니다. 대신, 실제 작업 데이터를 기반으로 한 회귀 분석 방식을 사용합니다.

#### 7.3.1. AT/DT 산출 모델 (회귀 기반)
아래 모델은 **생산 시간 데이터를 이용해 AT와 DT를 회귀 분석으로 분리 추정**하는 기준입니다. QF(Quantity Factor) 개념은 폐기되었으며, DT가 전환 시간으로 재정의되어 명시적으로 추적됩니다.

- **입력 데이터**
  - 날짜별 총 작업 시간 `T_d` (출퇴근 기록 기반 실근로시간 합계)
  - 공정별 생산 수량 `Q_{d,i}`
  - 스타일-공정 조합 목록 `i`
  - **전환 횟수 `N_d`** (해당 작업자가 당일 수행한 작업 항목 수 - 1, 시스템 자동 계산)

- **AT/DT 추정 (회귀 모델)**
  - 개념식: `T_d = Σ(AT_i × Q_{d,i}) + (N_d × DT) + error`
  - 의미: 여러 날짜의 작업 기록으로 각 **스타일-공정 단위 시간(AT)**과 **공정 전환 시간(DT)**을 수학적으로 분리하여 추정한다.
  - **AT_i**: 스타일-공정 `i`의 1단위 실제 작업 시간 (전환 오버헤드 제외)
  - **DT**: 1회 공정 전환에 걸리는 평균 시간 (모든 공정에 대해 단일 평균값 사용)
  - **N_d**: 당일 전환 횟수 = 작업 항목 수 - 1 (예: 3개 공정 작업 시 N_d = 2)

- **수학적 분리 원리**
  - 단일 공정만 수행한 작업자의 기록: `T = AT × Q` (DT 없음)
  - 다공정 수행 작업자의 기록: `T = Σ(AT_i × Q_i) + N × DT`
  - 이 두 유형의 데이터를 함께 회귀 분석하면, AT와 DT를 수학적으로 분리할 수 있다.

- **전처리 규칙 (비정상 제외 및 절사)**
  - AT/DT 산출에 사용하는 데이터는 **비정상 작업(장비 고장, 대기 등) 기록을 제외**한다.
  - 상위/하위 10% 극단값은 **절사 평균(Trimmed Mean)**으로 제거하여 이상치의 영향을 최소화한다.

- **표본 윈도우/드리프트 규칙 (장기 누적 스타일 대응)**
  - 장기간 반복 주문되는 스타일은 **전체 이력 무제한 누적 평균**을 사용하지 않는다.
  - `스타일-공정` AT 산출은 **롤링 표본 건수 상한(N개, 초기 500)** 기준으로 계산한다. (기간 제한 대신 최근 유효 표본 우선, 운영 중 조정 가능)
  - 업데이트 평가는 월 1회(매월 5일) 수행하며, 전월 말일 기준 데이터 컷오프를 적용한다.
  - 최신 데이터에 더 높은 가중치를 두고, 극단값은 절사(Trim)하여 특정 시기의 이상치가 AT를 과도하게 흔들지 않게 한다.
  - 유효 표본이 `n < 80`이면 AT 신뢰도 부족으로 간주하고 업데이트를 스킵(기존 AT 유지)한다.

- **급여 산출 공식**
  - `급여 = Σ(CT_i × Q_i) × 초당임금 + (전환횟수 × DT) × 초당임금`
  - CT 부분: 각 공정의 합의된 단가 기반 급여
  - DT 부분: 다공정 작업에 대한 전환 보너스 (회사가 요청한 오버헤드에 대한 보상)
  - 단일 공정 작업자: 전환 횟수 = 0이므로 DT 보너스 없음 (공정성 확보)

### 7.4. 작업 기록 관리 (Work Record Management)

작업자가 수행한 생산 내역을 입력하고 관리하는 핵심 모듈입니다.

1.  **데이터 구조 (Data Structure)**:
    -   **Master (WorkDetail)**: 작업일자, 공장 ID, 작성자(로그인 유저).
    -   **Detail (WorkerLog)**: 작업자 ID (선택된 공장 소속 직원으로 필터링).
    -   **Sub-Detail (WorkItem)**: 고객사 → 스타일 → 공정 → 수량.
    -   **전환 횟수 (transitionCount)**: WorkerLog 단위로 자동 계산. `작업 항목(WorkItem) 수 - 1`. 별도 입력 불필요.
    -   **근태 (AttendanceLog)**: 작업자별 `checkInAt`, `checkOutAt`, `breakSeconds(optional)`, `workingSeconds`를 저장하며, `T_d` 계산의 원천 데이터로 사용.

2.  **입력 로직 및 제약 (Input Logic & Constraints)**:
    -   **계층적 선택 (Cascading Select)**: `고객사` 선택 시 해당 고객사의 `스타일`만 노출, `스타일` 선택 시 해당 스타일의 `공정`만 노출되는 종속적 필터링 적용.
    -   **유효성 검사**: 수량은 **양의 정수**만 입력 가능 (소수점, 음수 차단).
    -   **자동 계산**: 공정 선택 시 `공임 단가`와 `합계`(`단가 * 수량`)가 실시간으로 계산되어 읽기 전용(Read-only)으로 표시됨.
    -   **UX 편의성**: 수량 필드에서 `Enter` 키 입력 시 자동으로 다음 작업 항목 추가.
    -   **AT 산출 연계**: 출퇴근 데이터가 없는 날짜의 기록은 AT 후보 산출에서 제외하거나 신뢰도 낮음으로 표시한다.
    -   **근태 보정 원칙**: 지각/조퇴/결근 등 근태 이슈는 개인 성과 평가 항목으로 분리하며, AT 산출에는 비정상치 필터(절사/제외)를 적용해 팀 전체 기준이 과도하게 왜곡되지 않게 한다.

#### 7.4.1. 정상/비정상 작업 구분 (Normal vs. Abnormal Work)
- 작업 기록에는 **문제 발생 여부**를 체크할 수 있어야 합니다. (예: 장비 고장, 대기, 재작업, 교육/훈련 등)
- 문제가 신고된 기록은 **분석/기준값 산출 대상에서 제외**하는 것을 기본 원칙으로 합니다.
- 신고 사유는 코드화하여 저장합니다. (예: `equipment`, `waiting`, `rework`, `training`)

### 7.5. 작업 현황 및 효율 분석 (Work Status & Efficiency)

입력된 작업 기록을 바탕으로 공장 및 작업자의 생산성을 분석합니다.

1.  **지표 정의**:
    -   **총 계약 공수 (Total Contracted Seconds)**: `∑ (확정 CT * 수량)`. 급여/정산 지표는 CT 스냅샷 기준으로 계산.
    -   **총 근로 시간 (Total Working Hours)**: 작업자들의 실제 출퇴근 체류 시간 합계.
    -   **생산 효율 (Efficiency)**: `(총 계약 공수 / 총 근로 시간) * 100`.
    -   **참고 지표(제안 기준)**: 필요 시 PT/AT 제안 기준 지표를 별도로 병행 표시할 수 있습니다.
2.  **시각화**:
    -   단순 초(Second) 단위가 아닌 `X시간 Y분` 형식으로 변환하여 표시.
    -   직원 1인당 평균 시간을 계산하여 제공.

### 7.6. 인증 및 사용자 세션 (Authentication & User Session)

-   **전역 상태 관리**: `AuthContext`를 통해 로그인한 사용자 정보(ID, 이름, 권한, 소속 공장 등)를 전역에서 관리합니다.
-   **데이터 연동**: 데이터 생성(예: 작업 기록) 시 현재 로그인한 사용자의 정보가 '작성자' 필드에 자동으로 매핑됩니다.

### 7.7. 스타일 신규 등록 로직 (New Style Registration Logic)

- **기본 정보 페이지 (Basic Information Page):**
  - 스타일 등록 시, `고객사` 정보를 필수로 선택해야 합니다.
  - **제품 카테고리(스타일 카테고리)**는 **`속성 관리`** 메뉴에서 미리 등록된 값을 불러와 선택합니다.
  - 기타 필요한 기준 정보들을 API를 통해 호출하여 사용합니다. (세부 항목은 추후 정의)

- **공정 정보 페이지 (Process Information Page):**
  - **평균 공정 시간 계산:** `작업 기록(Work)` 데이터를 기반으로, 공정별 평균 소요 시간을 계산하여 참고용으로 표시합니다. 단, 이 기능은 **의미 있는 예측을 위해 충분한 작업 기록 데이터가 축적되어야** 합니다.
  - **평균 시간의 분류:** 평균 시간은 위에서 선택한 `제품 카테고리`별로 분류되어야 합니다. (예: '셰프복 주머니 달기' vs '바지 주머니 달기') 이는 유사한 작업의 소요 시간을 예측하는 데 사용됩니다.
  - **공정 종류 표준화:** `기준 정보`에 미리 정의된 `공정 종류` 목록을 호출하여 사용자가 선택하도록 합니다. 이를 통해 '주머니 달기'와 같은 공통된 작업에 일관된 명칭을 사용합니다.
  - **PT 입력 (Optional):** PT는 스타일-공정 단위로 관리하며 공장 공통입니다. 공장장의 경험에 기반하여 설정합니다.
  - **AT 자동 계산 (Work 기반):** AT는 등록 시 수동 입력값이 아니라, 작업 기록이 누적된 뒤 시스템이 회귀 분석으로 자동 산출하는 **데이터 기반 제안값**입니다.
    - 기준식: `T_d = Σ(AT_i × Q_{d,i}) + (N_d × DT) + error`
    - `AT`: 스타일-공정 기준의 실제 작업시간 (공정 전환시간 제외, 부대작업 포함)
    - `DT`: 공정 전환 시간 (회귀 분석으로 분리)
  - **현 구현 기준(기초 작업):** 스타일 공정 데이터 모델은 `ptSeconds`(공장장 입력)와 `atSeconds`(자동 계산 결과)를 분리 저장합니다. 이 값들은 마스터 제안값이며, **실제 지급 확정은 Assignment 단위 CT**에서 결정합니다.

- **BOM (Bill of Materials) 페이지:**
  - BOM 정보는 추후 개발될 `재고 관리` 기능과 연동될 예정입니다.

### 7.8. 작업 배정(Assignment) 기능 명세

이 문서는 본 프로젝트에서 "작업 배정(Assignment)" 기능을 개발할 때 모든 에이전트가 반드시 따라야 하는 기준 문서입니다.

#### 7.8.1. 기능 정의 (Feature Definition)
작업 배정(Assignment) 기능은 단순한 일정 관리 UI가 아니라 봉제 공장의 생산 계획을 시간 단위로 시뮬레이션하는 핵심 기능입니다. 스타일 단위의 작업을 특정 생산 라인과 날짜에 배치함으로써 예상 소요 기간과 잔여 시간을 즉시 확인할 수 있어야 합니다.
**네이밍 규칙 (Naming Convention)**: 코드 및 파일명에서 `Assignment`는 **`Assign`**으로 축약하여 사용합니다. (예: `AssignBoard.jsx`, `AssignDetail.jsx`, `AssignContext.jsx`)

#### 7.8.1.1. 화면 역할 분리 (UI Responsibility Split)
- `작업 배정(/assignment)` 화면은 **배정/해제/일정 시뮬레이션**만 담당합니다.
- 배정 시 신규 Assignment의 기본 CT 상태는 항상 `ctStatus = PENDING`으로 생성합니다.
- `작업 계획 협의(/production-plan)` 화면에서 라인장이 배정된 작업의 일정/수량/비용을 검토하고 `동의` 또는 `조정 요청`을 처리합니다.
- `조정 요청`의 현재 운영 규칙: 해당 Assignment는 **배정을 유지한 채 운영팀 검토 큐로 전달**됩니다. 운영팀이 CT 재조정/재배정(타 라인 이동 또는 미배정 복귀 포함)을 결정합니다.

---

#### 7.8.2. Order / Style / Card 모델
- **Order**: 하나의 주문(Order)은 1개 이상의 스타일(Style)로 구성됩니다. 작업 배정의 최소 단위는 주문이 아니라 스타일입니다.
- **Style Card**: 각 스타일은 1장의 작업 카드로 표현됩니다. 모든 스타일 카드는 초기 상태에서 "미배치" 상태입니다.
  - **카드 생성 소스(필수)**: 배정 보드는 목업 데이터가 아니라 **등록된 주문 데이터**를 사용합니다.
  - **카드 구분 단위(현재)**: `주문번호 + 스타일 + 색상`. (사이즈/성별은 현재 배정 카드 분리 기준에서 제외)
  - **예시**: `A주문/B스타일/검정 100장`, `A주문/B스타일/흰색 120장`은 별도 카드 2장으로 생성됩니다.
  - **배정 단위**: 스타일 카드는 **스타일 단위 배정**을 의미합니다. 공정 단위로 배정하지 않으며, 배정된 라인이 해당 스타일의 봉제 공정을 끝까지 수행합니다.
  - **식별 키**: `orderNo`, `styleId(or styleCode)`, `colorId(or colorName)`, `originOrderId`.
  - **시간 데이터(제안값)**: `totalPt`, `totalAt`, `totalSeconds(현재 선택된 제안 기준 합계)`.
  - **시간 데이터(확정값/스냅샷)**: `proposalBasis(PT|AT)`, `proposalSeconds`, `contractedSeconds(CT)`, `ctStatus(PENDING|AGREED|REJECTED)`, `ctSource(PT|AT|MANUAL)`, `ctAgreedBy`, `ctAgreedAt`, `ctNote`.
  - **권한 의미**: `ctAgreedBy`는 최종 CT 확정자(라인장 동의 확정 또는 관리자/운영자 수동 확정)를 기록합니다.
  - **이미지 필드 우선순위**: `previewUrl` > `imageUrl` > `thumbnailUrl`.
  - **PT/AT는 스타일-공정 단위**이며 공장 공통값입니다. (공장별/라인별 구분 없음)

---

#### 7.8.3. 스타일 카드 상태 (Style Card States)
스타일 카드/배정의 상태는 **제안 기준 상태(PT/AT/NONE)**와 **CT 확정 상태(PENDING/AGREED/REJECTED)**를 함께 관리합니다.
- **제안 기준 선택 우선순위**: `AT 존재 시 AT`, `AT 없음 + PT 존재 시 PT`, `둘 다 없음이면 NONE`.
- **기간 계산 기준(요약)**: `총 필요초(선택된 PT/AT)`를 라인의 `일일 총공수 = 인원 × 1일 근무시간(초)`에 배분하여 일정을 계산합니다. (휴일/비가동일 제외 규칙 적용)

- **제안 기준 미지정 카드 (Unspecified Proposal Card, `proposalBasis=NONE`)**
  - **조건**: 공정 정보가 없거나, **PT 및 AT가 모두 0/없음**인 상태.
  - **배정 규칙**: **스케줄러 배치 불가 (드래그 불가)**. (경고 표시)
  
- **PT 제안 카드 (`proposalBasis=PT`)**
  - **조건**: PT 제안값은 존재하나, AT 제안값이 없는 상태.
  - **배정 규칙**: **배치 가능**.
  - **계산 기준**: 배정 전 시뮬레이션 시 **PT 제안값**으로 부하(Load)를 계산합니다.
  - **CT 기본값**: AT 제안값이 없으면 `CT 기본값 = PT`.

- **AT 제안 카드 (`proposalBasis=AT`)**
  - **조건**: AT 제안값이 존재하는 상태.
  - **배정 규칙**: **배치 가능**.
  - **표시 규칙**: 미배정 상태에서는 **예상 AT 합계**를 표시하여 관리자가 확인할 수 있도록 합니다.
  - **계산 기준**: 배정 전 시뮬레이션 시 **AT 제안값**을 기준으로 부하(Load)를 계산합니다.
  - **CT 기본값**: AT 제안값이 있으면 `CT 기본값 = AT`.

- **CT 확정 상태 (`ctStatus`)**
  - `PENDING`: 라인장 검토 전 상태. 임시 배정(시뮬레이션)만 가능하며 배정 최종확정/급여/정산 확정 불가.
  - `AGREED`: CT 최종 확정 상태. (라인장 동의 확정 또는 관리자/운영자 수동 확정) `contractedSeconds(CT)`를 지급/정산 기준으로 사용.
  - `REJECTED`: 라인장의 조정 요청이 등록된 **운영팀 검토 상태**. 운영팀의 CT 수동 확정 또는 재배정 결정이 필요합니다.
  - **운영팀 처리 규칙**: `REJECTED` 상태에서는 배정을 즉시 해제하지 않으며, 운영팀 결정 시점에만 타 라인 재배정 또는 미배정 복귀를 수행합니다.

#### 7.8.3.1. 카드 UI 및 표시 정보 (Card UI & Display Information)
스타일 카드는 배정 상태에 따라 UI 형태와 표시 정보가 완전히 변경됩니다. **스타일 이미지가 존재하는 경우, 카드 및 배정 바에 썸네일로 표시합니다.**

  1.  **배정 전: 미배정 카드 (Unassigned Card)**
      -   **UI 형태**: 정보 확인 및 드래그를 위한 **카드(Card)** 형태.
      -   **목록 관리**: 배정이 확정되면 미배정 목록(Source)에서 제거됩니다.
      -   **표시 정보**:
          -   **이미지**: 스타일 대표 이미지 (`previewUrl` > `imageUrl` > `thumbnailUrl` 우선순위 적용).
          -   **헤더**: `고객사` · `주문번호`
          -   **본문**: `스타일명`
          -   **하단**: `수량`
          -   **뱃지(제안 기준 표시)**: `PT 제안`(파스텔 블루), `AT 제안`(파스텔 그린), `기준 없음`(파스텔 레드). **배경색 없이 외곽선만** 사용.
          -   **기준 미지정**: **배정 불가(Disabled)** 상태 표시. (드래그 불가)
          -   **시간 정보(PT/AT 제안 합계)는 리스트에서 생략**하고, 계산을 위한 데이터로만 유지합니다. (상세/툴팁으로 노출 가능)
          -   **미배정 AT 표시 원칙**: AT가 존재하면 AT 값을 표시합니다.
          -   **미배정 일수 표시 원칙**: 미배정 카드에서는 단일 "예상 N일"을 기본 표시하지 않습니다. (라인/시작일/캘린더 점유가 미정이므로 오해 방지)
          -   필요 시 보조 정보로만 `라인별 예상 범위(min~max)`를 표시할 수 있으며, 확정성 있는 일수는 스케줄러 호버에서 계산해 보여줍니다.

  2.  **배정 후: 배정된 카드 (Assigned Card)**
      -   **UI 형태**:
          -   멀티데이 작업이라도 **하나의 연속된 블록(Continuous Block)**으로 표시합니다.
          -   **시각적 오프셋 (Visual Offset)**: 시작일의 공수가 일부만 사용된 경우, 해당 비율만큼 바(Bar)를 우측으로 이동시켜 시작 시점을 표현합니다.
          -   **길이 표현**: 마지막 날이 100%가 아니면 부분 길이로 표시하며, 전체 길이는 총 소요 공수(초)를 반영합니다.
      -   **표시 정보 (Bar 내부)**:
          -   **이미지**: 좌측에 소형 썸네일 표시.
          -   `고객사` → `스타일명` → `수량` 순서로 표시.
          -   **주문번호는 배정 바에서 표시하지 않음**. (필요 시 Tooltip에서만 노출)
          -   **말줄임은 뒤쪽**에서 발생해야 하며, **좌측 정렬**로 고정합니다.
          -   `소요 기간`은 우측 상단 배지로 표시합니다. (예: 1.5일)
          -   **휴일/비가동일은 소요 기간에서 제외**합니다. (바 길이는 캘린더를 포함하되, 배지 표기는 작업일 기준)
          -   **색상 규칙**: PT 제안은 파스텔 블루, AT 제안은 파스텔 그린, 기준 없음은 파스텔 레드.
          -   **CT 상태 배지**: `CT 대기(PENDING)`, `CT 확정(AGREED)`, `운영팀 검토(REJECTED)`를 별도 표시합니다.
      -   **상세 정보 (Tooltip/Click)**:
          -   **확정 기준**: 배정된 시점에 합의된 기준(CT)과 총 공수.
          -   **일정**: 배정 기간 (시작일 ~ 종료일). *구체적인 종료 시각은 표시하지 않습니다.*

---

#### 7.8.4. 부하와 용량의 개념 (Concept of Load & Capacity)
작업 배정은 **라인의 용량(그릇)**에 **카드의 부하(물량)**를 채우는 과정입니다.

  1.  **용량 (Capacity) = 라인 총 공수**
      -   **정의**: 생산 라인이 하루에 공급할 수 있는 총 작업 시간(초 단위)입니다.
      -   **공식**: `Daily Capacity` = `라인 인원 수` × (`하루 근무 시간` - `휴식 시간`)
      -   *예: 10명 × 8시간(28,800초) = 288,000 Man-Seconds*
      -   **라인 속성**: 용량 계산을 위해 라인은 인원 수, 근무 시간(08:00~17:00), 휴식 시간(12:00~13:00) 정보를 가집니다.
      -   **중요**: 실제 스케줄링은 **라인별 `dailyCapacitySeconds`**를 사용해야 합니다. 고정값(예: 28,800초) 사용 금지.

2.  **부하 (Load) = 작업 요구량**
    -   **정의**: 스타일 카드를 생산하기 위해 필요한 총 작업 시간(초 단위)입니다.
    -   **공식**: `Card Load` = `Σ(기준 시간 × 주문 수량)`
    -   *배정 시뮬레이션/일정은 제안 기준(PT/AT)을 사용하고, 정산은 CT + DT 보너스를 사용합니다.*

---

#### 7.8.5. 배정 상호작용 모델 (Assignment Interaction Model)
스케줄러에서 카드를 드롭하는 행위는 사용자의 의도에 따라 두 가지 핵심 시나리오로 구분되며, 각기 다른 규칙을 적용합니다.

- **A) 작업에 연결하여 삽입 (Inserting by Linking to a Job)**
  - **정의**: 사용자가 새 작업 카드(B)를 기존에 배정된 다른 작업 카드(A) **위에** 직접 드롭하는 경우입니다.
  - **결과**: **'밀어내기 삽입 (Insert & Push)'** 규칙이 발동됩니다. 카드 B는 카드 A 바로 뒤에 배치되며, 원래 카드 A 뒤에 붙어있던 카드 C(및 그 후속 작업들)는 카드 B가 끝나는 시간 이후로 밀려납니다. 즉, `A -> C` 구조가 `A -> B -> C` 구조로 변경되며 전체 일정이 뒤로 연장됩니다.

- **B) 빈 공간에 배정 (Assigning to an Empty Space)**
  - **정의**: 사용자가 특정 라인의 비어있는 날짜/시간 영역에 작업 카드를 드롭하는 경우입니다.
  - **결과**: **'빈 공간 채우기'** 규칙이 발동됩니다. 드롭한 위치에서부터 가장 가까운 다음 가용 시간에 작업이 배정됩니다.
  - **공간 부족 시**: 만약 해당 공간에 작업이 온전히 들어가지 않는 경우, **해당 공간 바로 뒤에 있는 작업(Next Task)의 앞으로 삽입**되며, 기존 작업들은 뒤로 밀려납니다(Insert & Push).

- **C) 배정 해제 (Unassigning)**
  - **정의**: 배정된 카드를 타임라인 영역 밖으로 드래그하여 드롭하는 경우입니다.
  - **결과**: 배정이 취소되며, 해당 카드는 '미배정 카드 목록'으로 복귀합니다.

- **D) 연결 및 체인 이동 (Linking & Chain Move)**
  - **정의**: 배정된 작업이 앞선 작업과 시간적으로 떨어져 있는 경우, UI에 **연결 버튼(화살표)**이 표시됩니다.
  - **동작**: 버튼 클릭 시, 해당 작업뿐만 아니라 **그 뒤에 연결된 모든 후속 작업(Chain)**들이 앞선 작업의 종료 시점에 맞춰 일괄적으로 앞으로 당겨집니다.
  
- **E) 동일 원본 주문 병합 (Merge by Drag, 2차 구현 예정)**
  - **정의**: 카드/배정 작업을 다른 카드/배정 작업 위에 드롭했을 때 **`originOrderId`가 동일**하면 병합을 수행합니다.
  - **현재 상태**: 기능 개념만 정의하며, 1차 구현 범위에서는 보류합니다.
  - **규칙**:
      - **드롭 대상이 배정 카드**인 경우: 대상에 병합되고 **대상 라인의 용량 기준으로 기간을 재계산**합니다. (필요 시 후속 작업 밀어내기)
      - **드롭 대상이 미배정 카드**인 경우: 대상 미배정 카드로 병합하며, **소스는 제거**됩니다.
      - **배정 → 미배정 드롭**은 배정을 해제한 뒤 병합으로 처리합니다.
  - **병합 내용**: `수량`, `totalSeconds`, `totalPt`, `totalAt`를 합산합니다.

---

#### 7.8.6. 시뮬레이션 규칙 (Simulation Rules)
배정 시뮬레이션은 위의 상호작용 모델에 따라 다음과 같이 수행됩니다.

- **공수 계산 및 연속성 규칙**:
  - **단위**: 모든 배정은 **일별 공수(초)**를 기준으로 계산합니다.
  - **시작일 처리**: 시작일에 이미 사용된 공수가 있다면, **잔여 공수(Remaining Capacity)**부터 차감하여 배정을 시작합니다.
  - **연속성 보장**: 배정은 **연속된 일자**로만 진행됩니다. 작업 기간 중간에 다른 작업이 있어 공간이 단절된 경우(빈 공간 채우기 시), 배정은 실패합니다.

- **'밀어내기 삽입 (Insert & Push)' 규칙 상세**:
  1.  **타겟 식별**: 드롭된 위치의 기존 작업 카드(A)를 확인합니다.
  2.  **삽입**: 새 작업 카드(B)의 시작 시간을 타겟 카드(A)의 종료 시간으로 설정합니다.
  3.  **후속 연결 변경**: 기존에 카드 A 뒤에 후속 작업(C)이 있었다면, 작업 C의 시작 시간을 카드 B의 종료 시간으로 변경합니다.
  4.  **연쇄 이동 (Cascade)**: 작업 C의 시작 시간이 변경됨에 따라, **라인의 가용 용량(Capacity)에 맞춰 C의 종료 시간을 다시 계산**합니다. (단순히 시간만 더하는 것이 아니라, 휴식 시간과 퇴근 시간을 건너뛰며 부하를 채워 넣습니다.) 이 과정은 라인의 마지막 작업까지 연쇄적으로 수행됩니다.

- **'빈 공간 채우기' 규칙 상세**:
  1.  **시작점 확인**: 드롭된 날짜/시간을 기준으로 배정을 시작할 지점을 확인합니다.
  2.  **공간 확인 및 분기**:
      -   **공간 충분**: 잔여 공수를 차감하며 배정합니다.
      -   **공간 부족**: 드롭 위치 이후의 첫 번째 작업을 찾아, 그 작업 바로 앞에 새 작업을 배치하는 **'밀어내기 삽입'** 로직을 수행합니다.

#### 7.8.6.1. 주문 분할/병합 (보류)
- **상태**: 사용자 요구에 따라 1차 구현 범위에서 제외합니다. (개념 설명만 유지)
- **설명**:
  - **분할(Split)**: 한 카드를 수량 기준으로 2개 이상으로 나누는 동작.
  - **병합(Merge)**: 동일 원본 기준의 카드들을 1개로 합치는 동작.
- **적용 시점**: CT 승인 플로우와 주문-스타일-색상 카드 소스가 안정화된 뒤 2차로 적용합니다.

#### 7.8.6.2. 주문 분할 (참고 개념)
- **트리거**: 카드 **우클릭**(미배정/배정 동일).
- **입력**: 분할할 수량을 입력합니다. (`1 ~ 현재 수량-1`)
- **동작**:
  - **미배정 카드**: 기존 카드 수량/시간을 비례 축소하고, 분할된 카드 1장을 **미배정 목록에 추가**합니다.
  - **배정 카드**: 기존 배정 카드의 **길이만 줄이고 빈 공간을 유지**합니다. (뒤 카드 자동 당김/연결 없음)
- **계산 규칙**: `totalSeconds`, `totalPt`, `totalAt`를 **수량 비율로 분할**합니다. (0이 되지 않도록 최소값 보정)
- **CT 규칙**:
  - `ctStatus = AGREED`이고 `contractedSeconds`가 있으면, `contractedSeconds`도 수량 비율로 분할합니다. (최소 1초 보정)
  - `ctStatus = PENDING/REJECTED`이면 CT 값은 확정하지 않습니다.

#### 7.8.6.3. 주문 병합 (참고 개념)
- **트리거**: 동일 `originOrderId`를 가진 카드/배정을 **드래그 드롭**.
- **상태 규칙**: **드롭 대상의 상태를 유지**합니다. (배정 대상이면 배정 유지, 미배정 대상이면 미배정)
- **배정 카드 병합**: 대상 라인의 용량 기준으로 기간 재계산 및 **후속 작업 밀어내기**가 발생할 수 있습니다.
- **CT 규칙**:
  - 병합 대상의 CT 단가(수량당 CT)가 동일하면 `contractedSeconds`를 합산하고 `ctStatus = AGREED`를 유지할 수 있습니다.
  - CT 단가가 다르거나 한쪽이라도 미확정이면 `ctStatus = PENDING`으로 전환하고 CT 재합의를 수행합니다.

- **타임라인 자동 확장 (Timeline Auto-Expansion)**:
  - 배정 시뮬레이션 결과, 작업 종료일이 현재 로드된 타임라인(날짜 범위)을 벗어나는 경우, **자동으로 날짜 범위를 확장(예: +10일)**하여 배정을 완료해야 합니다. 백엔드는 이러한 범위 확장을 수용할 수 있어야 합니다.

---

#### 7.8.7. 배정 압축 및 휴일 (Compaction and Holidays)

#### 7.8.7.1. 배정 압축 규칙 (Schedule Compaction)
- **정의**: 라인 위의 배정된 작업 중 하나가 제거(삭제 또는 미배정으로 이동)될 경우, 해당 작업이 있던 공간을 메우기 위해 뒤따르던 모든 후속 작업들이 앞으로 당겨져 빈 시간 없이 재배치됩니다. 이는 '삽입 및 연쇄 조정'의 반대 개념입니다.

#### 7.8.7.2. 휴일/비가동일 (Holidays / Non-working Days)
- **기본 규칙**: **일요일은 항상 휴일**로 처리합니다.
- **추가 규칙**: **국경일 등 비가동일을 설정하는 화면**을 제공하고, 시뮬레이션은 이를 반영해야 합니다. 작업 시간 계산 시, 비가동일은 가용 시간에서 제외됩니다.
- **UI 표시**: 일요일/공휴일 컬럼은 **파스텔 레드 톤 배경**과 강조 색상으로 표시합니다.

#### 7.8.7.3. 기존 배정 수정 (Rescheduling / Move)
이미 배정된 작업(Task 2)을 다른 위치로 이동하는 동작은 **'제거 후 삽입'**의 트랜잭션으로 정의합니다.

1.  **Step 1: 제거 및 압축 (Remove & Compact)**: 원래 위치에서 Task 2를 제거합니다. `7.8.7.1` 규칙에 따라, Task 2 뒤에 있던 Task 3(및 후속 작업)이 앞으로 당겨져 Task 1 뒤에 붙습니다. (`1-2-3` → `1-3`)
2.  **Step 2: 새 위치 삽입 (Insert & Push)**: 이동하려는 새 위치(예: Task 5 뒤)에 Task 2를 삽입합니다. `7.8.6`의 밀어내기 규칙에 따라 Task 5 뒤에 있던 작업들은 뒤로 밀려납니다.
3.  **원자성 보장**: 이 두 단계는 하나의 동작으로 처리되어야 하며, 중간에 실패 시 롤백되어야 합니다.

---

#### 7.8.8. 스케줄러 책임 (Scheduler Responsibility)
Scheduler(ScheduleTimeline)는 다음을 책임집니다:
- 라인 × 날짜 그리드 표현 (셀 크기는 **고정**하여 표현 안정성 확보)
- **날짜 표시**: 로컬 날짜 기준으로 생성하며, 라벨에 요일을 포함합니다. (예: `2/6 (금)`)
- 라인 정보 표시: **`공장명` + `라인명(인원)`** 순서로 표시합니다. (근무시간 텍스트는 생략)
- 날짜별 가용 시간, 사용 시간, 잔여 시간 계산 및 표시
- 드래그 중 배치 가능 / 불가 시각적 피드백
- 예상 종료일 미리보기
- **호버 미리보기 규칙**: 카드가 특정 라인/날짜 셀 위에 호버되면, 그 시점의 조건(AT, 라인 용량, 기존 점유, 휴일)을 반영한 `예상 작업일수`와 `예상 종료일`을 표시합니다.

---

#### 7.8.9. 드래그 & 드롭 의미 (Drag & Drop Semantics)
Drag & Drop은 단순한 UI 이동이 아닙니다. Drop 발생 시:
1. 배정 요청이 생성됩니다.
2. 생산 계획 시뮬레이션이 수행됩니다.
3. 임시 배정 상태가 생성됩니다.
4. CT 기본값(PT/AT 기반)을 스냅샷 필드에 저장합니다. (`contractedSeconds`, `ctStatus` 등)
5. CT 동의/조정은 `작업 계획 협의(/production-plan)` 화면에서 처리합니다. (배정 화면에서는 변경하지 않음)
6. 라인장이 `조정 요청`을 선택하면 해당 배정은 `REJECTED(운영팀 검토)` 상태로 전환되고, 배정은 유지됩니다. (공정별 제안 CT 포함 전달 가능)
7. `ctStatus = AGREED`인 경우에만 정산 확정이 가능합니다.
배정 확정은 별도의 액션으로 수행하며, `PENDING/REJECTED` 상태는 확정 API에서 차단됩니다.

---

#### 7.8.10. 프론트엔드 책임 (Frontend Responsibility)
프론트엔드는 다음을 책임집니다:
- 배정 시뮬레이션
- 소요 기간 계산
- 잔여 시간 계산
- 배치 가능 여부 판단
- 즉각적인 사용자 피드백 제공
- 프론트 계산 결과는 UX 가이드이며, 확정 진실 소스는 아닙니다.
백엔드는 배정 확정 시 다음을 책임집니다:
- 권한/정합성 검증
- 확정 스냅샷(CT/공정 버전) 저장
- 최종 확정 기준으로 재검증

---

#### 7.8.11. 절대 규칙 (Non-Negotiable Rules)
- 날짜 단위 계산만 하는 스케줄링은 금지합니다.
- 마지막 날 잔여 시간을 무시해서는 안 됩니다.
- AssignBoard에 계산 로직을 직접 작성하지 않습니다.
- 계산 로직은 순수 함수 또는 명확히 분리된 모듈로 관리합니다.
- **시간 계산 단위 통일**: 모든 핵심 로직(계산, 저장, 시뮬레이션)은 무조건 **초(Second)** 단위로 처리합니다. 분/시간/일 단위는 오직 UI에서 사용자가 보기 편하도록 변환하여 표시하는 용도로만 사용합니다.

#### 7.8.12. 구현 시 핵심 고려사항 (Key Implementation Considerations)
새로운 배정 규칙을 실제 시스템으로 구현할 때는 다음의 기술적 요구사항을 반드시 고려하여 설계해야 합니다.

1.  **연쇄 이동과 비가동일 계산 (Cascade and Non-Working Day Calculation)**
    - **정의**: '삽입 및 연쇄 조정' 또는 '배정 압축' 시 발생하는 날짜 이동은 단순한 일자 더하기/빼기가 아닙니다. 모든 날짜 계산은 `7.8.7.2`에 정의된 **비가동일(주말, 공휴일)을 제외한 순수 근무일 기준**으로 이루어져야 합니다.
    - **예시**: 금요일에 배정된 작업 뒤로 2일의 푸시(push)가 발생하면, 해당 작업의 새 시작일은 다음 주 화요일이 되어야 합니다(토, 일 제외).

2.  **대규모 연산 성능 (Performance with Large-Scale Operations)**
    - **정의**: 하나의 생산 라인에 수백 개 이상의 작업이 장기간에 걸쳐 배정된 경우, 앞쪽의 작업 하나를 수정(삽입 또는 삭제)하면 뒤따르는 모든 작업의 시간을 재계산하는 연쇄(Cascade) 연산이 발생합니다.
    - **요구사항**: 이와 같은 대규모 연산은 UI의 응답성을 저해할 수 있으므로, **비동기(Asynchronous) 처리 방식**을 도입해야 합니다. 연산이 진행되는 동안 사용자에게는 로딩 인디케이터와 같은 시각적 피드백을 제공해야 합니다.

3.  **데이터 정합성을 위한 원자적 연산 (Atomic Operations for Data Integrity)**
    - **정의**: '밀어내기 삽입'이나 '재배정(이동)'과 같이 여러 데이터(배정된 작업들)를 한 번에 수정하는 작업은 **하나의 트랜잭션(Transaction)**으로 간주되어야 합니다.
    - **요구사항**: 연산 과정 중 일부에서 오류가 발생할 경우, 변경된 모든 사항을 즉시 이전 상태로 되돌리는 **롤백(Rollback) 메커니즘**이 필수적입니다. 이는 데이터가 겹치거나 손상되는 것을 방지하여 정합성을 보장합니다.

4.  **사용자 경험 개선: 공간 부족 시 제안 (UX Enhancement: Proposal on Insufficient Space)**
    - **정의**: '빈 공간 채우기' 시나리오에서 공간이 부족하면, 현재 규칙은 **해당 구간 뒤 첫 작업 앞에 삽입하고 후속 작업을 밀어내는 Insert & Push**를 수행합니다.
    - **개선 제안**: 자동 수행 전에 "공간이 부족합니다. 뒤따르는 작업을 밀고 배정할까요?" 확인 모달을 제공하는 UX를 고려할 수 있습니다.

5.  **프론트엔드 렌더링을 위한 스냅샷 데이터 (Snapshot Data for Rendering)**
    - 배정 확정 시, 다음의 계산된 필드들이 백엔드에 저장되어야 프론트엔드에서 정확한 바(Bar) 형태를 복원할 수 있습니다.
    - **필수 필드**: `startIndex`, `endIndex` (날짜 인덱스), `startDayOffsetPercent` (시작일 오프셋), `startDayPercent` (시작일 사용 비율), `endDayPercent` (종료일 사용 비율), `totalSeconds`.
    - **병합/표시용 추가 필드**: `quantity`, `orderNo`, `originOrderId`, `cardId`, `lineId`, `proposalBasis(PT/AT)`, `proposalSeconds`, `contractedSeconds(CT)`, `ctStatus`, `ctSource`, `ctAgreedBy`, `ctAgreedAt`, `ctNote`, `color/stripe`(렌더링용).

6.  **체인 업데이트 트랜잭션 (Chain Update Transaction)**
    - '연결 버튼' 기능이나 '중간 삽입' 발생 시, 단일 작업만 수정되는 것이 아니라 **연결된 후속 작업 전체(Chain)**의 일정이 변경됩니다. 백엔드는 이 요청을 하나의 트랜잭션으로 처리하여 데이터 정합성을 보장해야 합니다.

7.  **분할/병합 데이터 정합성 (Split & Merge Integrity)**
    - `originOrderId`는 **분할/병합의 유일 키**로 사용됩니다.
    - 병합 시 `수량`, `totalSeconds`, `totalPt`, `totalAt`는 **합산**되어야 합니다.
    - 배정 카드 병합은 **라인 용량 기준**으로 기간을 재계산해야 하며, 필요 시 후속 작업이 밀려납니다.

#### 7.8.13. 단계별 개발 계획 (Step-by-Step Development Plan)

이 섹션은 작업 배정 기능을 구현하기 위한 구체적인 실행 계획입니다. 개발 진행 상황을 추적하기 위해 완료된 항목에 체크(x)를 표시합니다.

##### Phase 1: 핵심 데이터 구조 및 유틸리티 구현 (Core Logic)
- [ ] **타입 정의 (Type Definitions)**
  - `StyleCard`, `ProductionLine`, `Assignment` 인터페이스 정의.
  - `Assignment`에는 `proposalBasis`, `proposalSeconds`, `contractedSeconds`, `ctStatus`, `ctSource`, `ctAgreedBy`, `ctAgreedAt`, `ctNote`를 포함한다.
  - `TimeSlot` (날짜별 가용/사용 시간) 구조 설계.
- [x] **시간 계산 유틸리티 (Time Utils)**
  - `calculateCardLoad(style)`: 스타일 카드의 총 필요 공수(Man-Seconds) 계산.
  - `getLineDailyCapacity(line)`: 라인의 일일 총 가용 공수(Man-Seconds) 계산.
  - `calculateRemainingCapacity(line, date, assignments)`: 특정 날짜의 잔여 공수 계산.
- [x] **배정 시뮬레이션 로직 (Simulation Logic)**
  - `simulateAssignment(line, dropTarget, newCard, existingAssignments)`:
    - **상호작용 모델 분기**: `dropTarget`이 기존 배정 카드인지, 아니면 빈 타임라인 공간인지에 따라 '삽입 및 연쇄 조정' 또는 '빈 공간 채우기' 규칙을 적용합니다.
    - **공수 차감 및 날짜 계산**: 시작점부터 날짜별 `잔여 공수`를 순차적으로 차감하여 종료일을 계산합니다.
    - **결과 반환**: 새 배정 정보(시작/종료일) 및 변경된 후속 배정 정보들의 배열.

##### Phase 2: UI 스켈레톤 및 D&D 기초 (UI Skeleton & D&D)
- [x] **AssignBoard 레이아웃 구성**
  - 좌측: 미배정 스타일 카드 목록 (Source).
  - 우측: 생산 라인별 스케줄 타임라인 (Target).
- [x] **StyleCard 컴포넌트 구현**
  - 정보 표시: 스타일명, 공정, 총 소요 시간.
  - Draggable 구현 (dnd-kit 등 라이브러리 활용).
- [x] **ScheduleTimeline 컴포넌트 구현**
  - 라인(Row) × 날짜(Column) 그리드 렌더링.
  - Droppable 영역 설정.

##### Phase 3: 배정 인터랙션 및 시각화 (Interaction & Visualization)
- [x] **Drop 핸들러 구현**
  - 드롭 이벤트 발생 시 `simulateAssignment`를 호출하여 배정 결과를 계산.
  - 계산된 배정 정보를 상태(State)에 반영.
- [x] **시각적 피드백 (Visual Feedback)**
  - 타임라인에 배정된 작업 바(Bar) 렌더링.
  - 작업 바의 길이는 소요 기간을 반영.
  - 툴팁: 시작일, 종료일, 잔여 시간 표시.

##### Phase 4: 상태 관리 및 데이터 연동 (State & Persistence)
- [ ] **AssignContext 구성**
  - 전체 배정 현황 관리 (배정된 작업 목록).
  - `addAssignment`, `removeAssignment`, `updateAssignment` 액션 구현.
- [ ] **데이터 저장 (Persistence)**
  - 임시 배정 상태를 백엔드(또는 로컬 스토리지)에 저장하는 로직 구현.

##### Phase 5: 예외 처리 및 최적화 (Refinement)
- [ ] **휴일 및 비가동일 처리**
  - 주말 및 공휴일에는 작업 시간이 차감되지 않도록 로직 개선.
- [x] **배정 수정 기능**
  - 이미 배정된 작업의 재배치(Re-scheduling) 및 배정 취소(Unassign) 기능.

#### 7.8.14. 배정 시 공정 스냅샷 고정 (Assignment Snapshot Rule)
- 작업 배정 확정 시점에 해당 스타일의 **공정 버전(Revision)** 과 **CT(Contracted Time)** 를 스냅샷으로 고정합니다.
- 배정 이후 공정/AT/DT가 변경되더라도, 해당 배정 건의 일정/기준/급여 내역은 **CT 스냅샷 기준**을 유지합니다.

### 7.9. 급여 지급 내역 연계 원칙 (Payroll Snapshot Policy)
- 급여 지급 내역은 **작업 기록/배정 시점에 고정된 공정 스냅샷과 CT 스냅샷**을 참조합니다.
- 이후 공정 변경이나 AT/DT 재산출이 발생하더라도, **이미 생성된 급여 내역(CT 스냅샷 기반)은 변경하지 않습니다.**

### 7.10. 주문 관리 (Order Management)

주문(Order)은 고객사로부터 접수된 생산 의뢰를 시스템에 등록하는 단계입니다.

1.  **데이터 구조 (Data Structure)**:
    -   **필수 필드**: 주문번호, 고객사, 스타일명, 수량, 납기일, 상태.
    -   **선택 필드**: 스타일코드 (없는 경우도 있음).
    -   **상태 값**: `주문접수` -> `작업중` -> `생산완료` -> `출고완료`.

2.  **입력 로직 및 상호작용 (Input Logic & Interaction)**:
    -   **계층적 선택 (Cascading Select)**: `고객사`를 먼저 선택하면, `스타일` 선택 목록에는 해당 고객사에 등록된 스타일만 필터링되어 표시됩니다.
    -   **역방향 자동 선택 (Reverse Auto-Select)**: `스타일명`을 먼저 선택하거나 변경할 경우, 해당 스타일이 소속된 `고객사`가 자동으로 선택(입력)되며, `스타일코드`가 존재할 경우 함께 자동 입력됩니다.
    -   **초기화 규칙**: `고객사`를 변경하면, 기존에 선택된 `스타일` 정보(명칭/코드)는 초기화됩니다.

### 7.11. 라인 관리 (Line Management)

공장 내 생산 라인을 구성하고 작업자를 배치하는 기능입니다.

1.  **데이터 구조 (Data Structure)**:
    -   **라인 (Line)**: 라인명, 소속 공장, 기본 인원 수(Capacity 산출용), 라인장 ID.
    -   **라인 배정 (LineAssignment)**: 라인 ID, 직원 ID, 배정 시작일, 배정 종료일(옵션).

2.  **작업자 배치 및 라인장 (Worker Assignment & Line Manager)**:
    -   **작업자 배치**: 공장에 소속된 직원 목록에서 특정 라인으로 직원을 배치합니다. 한 직원은 특정 시점에 하나의 라인에만 소속될 수 있습니다.
    -   **라인장 (Line Manager)**: 배치된 작업자 중 1명을 반드시 **라인장**으로 지정해야 합니다. 라인장은 작업자이면서 동시에 해당 라인의 관리 책임을 가집니다.
    -   **역할**: 라인장은 현장에서 작업 지시를 확인하고, 작업 시작/종료 보고 및 이상 발생 신고 등의 권한을 가질 수 있습니다.

3.  **운영 규칙 (Operational Rules)**:
    -   **라인 생성 → 작업자 배치** 순서로 진행합니다. (라인을 먼저 만들고 배정)
    -   **배치 대상**: 역할이 `WORKER`인 직원만 라인 배치 목록에 노출합니다.
    -   **드래그 앤 드롭 배치**: 직원 목록에서 라인으로 드래그 & 드롭하여 배치하고 저장합니다.
    -   **라인장 지정 시점**: 라인에 작업자가 배치된 이후 라인장을 지정합니다.
    -   **라인 이동/퇴사 처리**: 직원이 라인을 옮기거나 퇴사하면 해당 라인 배정은 종료됩니다. (LineAssignment 종료)
    -   **라인명 저장 위치**: 라인명은 `Line` 테이블에 저장하는 것을 기준으로 합니다. (`Employee.lineName`은 임시 필드 취급)

4.  **배정(계획)과 작업 기록의 구분**:
    -   라인은 **작업 배정(계획)**의 기준점입니다. (실적 기록과 분리)
    -   라인 구성(인원) 변경에 따라 **배정 시 총 공수(일일 용량)**가 변합니다.
    -   라인에는 별도 공임 개념이 없으며, **최종 합의 CT × 공장 초당 임금**을 기준으로 정산합니다.
    -   배정 시 PT/AT는 제안값이며, 라인장은 CT에 대해 `동의` 또는 `조정 요청`을 수행합니다.
    -   라인장은 조정 요청 시 공정별 제안 CT를 입력할 수 있고, 운영팀이 이를 검토해 CT 수동 조정/재배정을 통해 최종 확정합니다.

## 8. UI 패턴 (UI Patterns)

*이 섹션은 반복적으로 사용되는 UI 컴포넌트의 표준 구현 방식을 정의합니다.*

- **저장/처리 결과 알림**: 저장/승인/반려/등록 등 처리 결과는 페이지 내부 텍스트가 아니라 **전역 토스트(Snackbar)** 로 잠깐 표시한다. 위치는 상단 중앙(탑바 영역)이며, 성공/오류 구분을 포함한다.
- **입력 성능 기준**: 직원 관리처럼 행이 많은 테이블은 입력 상태를 **부모에서 통합 관리하지 않는다.** 타이핑 시 전체 테이블이 리렌더되므로 지연이 발생한다. 기본 규칙은 다음과 같다.
  - 입력은 **행 단위 로컬 상태**로 관리하거나, 필요한 경우 **가상 스크롤(react-window)** 를 적용한다.
  - 저장 시에만 필요한 payload를 합쳐서 전송한다. (입력 단계에서는 전체 상태를 합치지 않음)

## 9. 협업 운영 원칙 (Agent Collaboration)

- 실행 우선: 구현에 필요한 사실은 먼저 코드/환경에서 확인하고, 바로 작업을 진행합니다.
- 질문 최소화: 저장소 탐색으로 확인 가능한 내용은 질문하지 않습니다.
- 질문이 필요한 경우: 기능 방향을 바꾸는 고영향 의사결정만 짧게 묻고, 가능하면 한 번에 묶어서 확인합니다.
- 기본값 진행: 명시되지 않은 선택지는 프로젝트의 기존 패턴과 최근 합의를 기본값으로 적용하고, 결과를 변경사항에 기록합니다.

