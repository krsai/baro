# 주문 완전 잠금·수출 청구서 개발 로드맵

최종 갱신: 2026-09-23
현재 단계: B1/B1.5/B2와 C의 발행·역순 취소·회차·입금/주문별 배분 원장, 주문별 이전 청구/실입금/차감 정산, 신규 채권 중복 방지와 발행 후 개정 이력까지 구현했다.
현재 선행 작업: C 후속까지 구현했다. 실제 운영 DB/브라우저 검증은 별도이며 다음 개발은 D 최종 잠금 정책 확정·구현이다. 아래 최신 정산 정책을 따른다.
용도: 다른 개발 환경·AI 도구·새 대화에서 이 파일을 읽고 이어서 개발한다.

### 2026-09-22 개발 환경 통합 및 B2 초안 저장

- 이 PC의 기준은 2d4e4de2였고 원격 fa6153ca까지 새 커밋 3개(출퇴근 사번/근로기간, 관리 제외·이력 보존, 공장 도메인 정리·회귀 게이트)가 있었다. 로컬 청구 초안과 별개 작업이므로 모두 유지했고 schema.prisma/index.ts도 충돌 없이 통합했다. 원격 fa6153ca의 GitHub CI 성공도 확인했다.
- 원래 미완성 수정 14개 파일과 임시 빌드 로그는 **로컬 전용** backup/pc-invoice-b2-20260922 / cc677003에 보존했다. main에는 마무리한 코드만 반영하며 임시 로그는 백업에만 남긴다. 이전 backup/pc-order-sync-20260917도 유지한다.
- [x] InvoiceDraft / InvoiceDraftOrder / InvoiceDraftLine, 판매자 INVOICE 권한으로 제한한 생성·목록·조회·수정·삭제 API, 초안 저장 버튼과 목록 재개를 구현했다. 입력 중인 수량·주문별 비율·문서/거래처 정보·행별 Remark/사유를 저장한다. 계산 금액과 브라우저 단가는 확정 데이터로 저장하지 않는다.
- [x] Serializable Tx에서 선택 고객·판매자·원주문 updatedAt·관계형 항목 키를 검증하고 주문/항목 FK를 연결한다. revision으로 동시 수정·삭제를 거부하며 생성 clientKey 재시도는 같은 입력이면 동일 초안을 반환한다. 링크 쓰기 실패 시 초안도 롤백한다.
- [x] 재개 시 현재 생산 정보/단가표로 다시 계산하고 출력 검토를 다시 요구한다. 원주문 변경/삭제 시 기존 입력을 보존하고 검토용으로 표시하며 새 주문 상태에 자동 합치지 않는다. 원본 FK는 삭제 시 SetNull, 원본 식별자/입력은 보존한다. 기존 주문/배정 삭제 보호와 CT/ST/실적은 그대로다.
- [x] 저장 중 입력 보호, 미저장 닫기 경고, 다른 목록의 저장/삭제 이벤트 갱신, 초안 캐시 무효화, 한/영/베트남어 안내를 연결했다. migration_fix.sql에 반복 실행 가능한 생성 SQL을 연결하고 Prisma client/시작 시 필수 컬럼·FK 검사도 추가했다.
- 검증: 전체 회귀 34개 스위트 실행 후 기존 청구 API 테스트 도구에 새 등록 함수를 연결하고 해당 29건을 재검증했다. 최종 로컬 결과는 33/34개 스위트·362건 통과, 나머지 관계별 버킷 PostgreSQL 통합은 전용 URL 미설정으로 실행 불가다. 초안 저장 7건은 권한/조직 범위·생성 재시도·수정/삭제 경쟁·롤백·복원 및 PGlite의 반복 SQL/FK/원본 삭제 보존을 포함한다. Prisma client 생성·양쪽 빌드·번역/인코딩도 통과했다.
- 정식 발행·발행 스냅샷·원장 잔액·선금 차감·최종 정산 잠금은 이 단계에 포함되지 않는다. 실제 Prisma 초안 API 동시 요청·운영 DB 반영/브라우저 사용 검증은 별도로 남긴다. GitHub CI는 푸시 후 결과를 확인한다.

### 2026-09-22 C 발행 원장 서버 기반

- [x] `Invoice`·`InvoiceOrder`·`InvoiceLine` 불변 스냅샷 원장과 반복 실행 가능한 생성 SQL을 추가했다. 한 발행본의 여러 주문과 주문의 여러 발행본을 허용하고, 원주문/항목 삭제 뒤에도 공개 ID·표시값·수량·단가·구간·금액을 보존한다.
- [x] 초안 발행 서비스는 Serializable Tx에서 판매자·구매자 범위, 초안 revision, 주문 updatedAt, 전체 행 키와 현재 관계 단가표를 다시 검증한다. 브라우저 계산값은 저장하지 않고 주문별 스타일 수량으로 구간 단가와 행 반올림, 주문별 비율 금액을 서버에서 다시 계산한다. 다른 주문의 같은 스타일 수량은 합산하지 않는다.
- [x] 조직별 발행번호·순번·발행 재시도 키를 고유하게 검사하고 발행 요청 재시도는 기존 발행본을 반환한다. 발행 목록 API는 판매자 조직 범위만 조회한다.
- [x] 저장된 초안을 다시 검토한 뒤 정식 발행하는 버튼, 발행 목록, 불변 스냅샷 재출력과 취소 사유 입력을 연결했다. 발행 전 미저장 변경이 있으면 발행할 수 없고 서버 재검증을 다시 거친다.
- [x] 취소는 행을 삭제하지 않고 상태·사유·담당자·시각을 보존한다. 같은 주문을 포함한 후속 발행본이 있으면 뒤 회차부터 취소하도록 Serializable Tx에서 거부한다.
- [x] 주문별 발행 회차 번호를 저장한다. 여러 주문을 한 문서에 담아도 각 주문의 기존 원장 기준으로 회차를 별도 증가시키고 목록에 주문번호와 회차를 표시한다.
- [x] 실제 입금은 발행 청구액과 분리한 `InvoicePayment` 원장에 기록한다. 통화는 발행본 통화를 고정하고 재시도 키로 중복 등록을 막으며, 잘못된 입금은 삭제 대신 무효 담당자·시각·사유를 남긴다. 취소된 발행본에는 새 입금을 등록하지 않되 기존 입금 이력은 보존한다.
- [x] 발행 목록에 청구액·유효 실입금 합계·미수 또는 초과액을 구분해 표시하고 새로고침 없이 갱신한다. 여러 주문 문서의 입금액을 주문 사이에 임의 배분하지 않는다.
- [x] 주문·통화별 이전 신규채권 합계와 단일 주문 문서의 유효 실입금액을 불러와 기본 차감액을 계산한다. 담당자는 차감액을 바꿀 수 있으며 기본값과 다르면 사유를 필수로 저장한다. 과거 발행본과 입금 행은 수정하지 않는다.
- [x] 안내금액, 기존 미수, 신규 채권 증가분을 분리했다. 예시의 이전 청구 5,000·입금 3,000·새 총액 10,000·이번 차감 3,000은 안내금액 7,000, 기존 미수 2,000, 신규 채권 5,000으로 저장·목록·PDF에 표시한다. 따라서 기존 미수 2,000을 다시 신규 채권에 더하지 않는다.
- [x] 정산 입력을 초안에 보존하고 발행 시 현재 원장에서 서버가 다시 계산한다. 통화가 다른 발행은 섞지 않으며 여러 주문 문서의 입금은 주문별로 자동 배분하지 않고 검토 표시한다.
- [x] 여러 주문 문서 입금의 명시적 주문별 배분 원장을 추가했다. 배분 변경은 기존 행을 무효 이력으로 보존하고 사유를 남기며, 결제·주문·문서 복합 FK가 교차 문서 연결을 DB에서도 거부한다. 미배분액은 추측하지 않는다.
- [x] 발행본에서 개정 초안을 만들고 새 불변 발행본으로 발행한다. 새 발행본은 원본·루트·개정 차수·사유를 보존하고 원본은 `SUPERSEDED`가 되어 채권을 중복 집계하지 않는다. 원본 입금·배분 이력은 유지하며 목록/상세에서 개정 연결과 배분 이력을 조회한다.
- 검증: 발행 계산/누락 단가/스키마·라우트/반복 SQL·원본 삭제 보존 4건, 기존 초안 저장 7건, Prisma 스키마 검증·client 생성·백엔드 빌드 통과. 실제 PostgreSQL/브라우저 검증은 미실행이다.

### 2026-09-17 B1.5b 잠금 의존 전환

- [x] 카드 재구성의 수동 잠금 필터를 제거했다. `isOrderReadyForAssignment`로 저장된 주문 PK, 비어 있지 않은 관계형 항목, 모든 항목의 스타일 FK·비음수 정수 수량, 양수 총수량을 검사한다. 불완전한 주문은 새 카드 대상에서 제외하며 기존 배정 카드는 보존한다. PT/ST·공정 및 기존 배정 보호 검사는 별개로 유지한다.
- [x] 카드 응답의 `isOrderAssignmentReady`와 드래그 시작/완료, 같은 Serializable Tx의 보드 저장 검사를 연결했다. 준비 값 누락, 주문 FK 누락, 접근 범위 밖 또는 불완전한 주문은 배정 변경을 거부한다. 기존 미배정 분할·배정 수량·CT/ST·실적 보존 정책은 그대로다.
- [x] 진행 상태는 수동 잠금을 보지 않는다. 과거 EDITING 응답은 ORDER_RECEIVED로 정규화하고 배정 범위로 자동 진행 상태를 계산한다. 완료/출하/정산 상태의 기존 일반 동기화 보호는 유지한다. 과거 잠금 시각/담당자는 DB에서 지우지 않으며 수정 차단이나 새 정산 잠금으로 해석하지 않는다. 일괄 데이터 변경은 실행하지 않았다.
- [x] 주문 목록/상세의 잠금 버튼과 호출 코드를 제거했다. 구 잠금 API는 인증 후 410 `ORDER_MANUAL_LOCK_RETIRED`를 반환하며 배정 수량/ST 재계산을 호출하지 않는다. 열린 배정 탭은 ORDERS 이벤트를 받아 갱신하되 미저장 내용이 있으면 기존 보호 흐름으로 갱신을 보류한다.
- [x] 주문 삭제도 같은 Serializable Tx에서 주문 접근/수정 경쟁과 모든 연결 배정을 검사한다. 0수량·완료·실적 유무와 무관하게 배정이 있으면 검토를 요구한다. 배정 없는 주문만 삭제하고 양쪽 카드 정리까지 함께 커밋한다. 재구성 실패 시 주문 삭제도 롤백한다.
- [x] 관련 94건(새 준비/전환 8건 포함), 양쪽 빌드, 번역 키·인코딩 검사 통과. 실제 카드 생성 함수, 보드 준비 검사 구간, 삭제 라우트와 Tx 더블을 실행했다. `test:order-assignment-readiness`를 추가했다.
- [ ] 실제 Prisma/PostgreSQL 경쟁·브라우저 검증은 미실행이다. 기존/신규 주문 저장 → 미배정 카드 → 드래그 → 보드 저장, 열린 미저장 탭 충돌, 과거 잠금 주문의 배정 보존을 운영 검증에서 확인한다.
- 확대 검사 제한: 관계 무결성의 직원 등급 seeding/WorkRecord 정규화 2건, 매출 단가 변경분 저장 검사 1건은 변경 전 HEAD 소스를 주입한 검사에서도 동일하게 실패했다. 이번 변경의 관련 94건과 구 잠금 API 종료에 맞춘 매출 단가 독립성 검사는 통과했지만 전체 회귀 통과로 표현하지 않는다.
- 다음: B2 초안 서버 저장·재개·동시 수정 보호. 청구 항목 FK, 발행 스냅샷과 회차별 차감/미수 이월은 최신 정책에 따라 설계한다. 최종 정산 잠금 시점은 여전히 미확정이다.

### 2026-09-17 B1.5b 카드 수량 보존 선행 수정

- [x] 카드 재구성의 기존 전체 수량 덮어쓰기/분할 카드 누락을 수정했다. `reconcileAssignmentCards`는 같은 Tx에서 실제 배정 FK와 수량을 읽고 배정된 카드를 유지한다. 주문 전체 수량에서 배정량을 뺀 잔량만 미배정 카드로 만든다. 미배정 분할 합계가 잔량과 같으면 분할 ID를 유지하고, 다르면 미배정 잔량 하나로 정리한다. 배정된 카드와 기존 배정 수량·CT/ST·실적을 이 함수가 재작성하지 않는다.
- [x] 참조된 과거/0수량 카드는 보존하며, 이번 재구성의 원본 주문 범위 밖에 있는 카드도 유지한다. 중복 배정 연결·FK 누락·카드/배정 수량 불일치·주문보다 많은 배정량은 409로 거부한다. 이전 카드 불일치를 자동 수선하거나 실적을 재배분하지 않는다.
- [x] 신규/반복 재구성, 부분·복수·전체 배정, 수량 증감, 분할 유지, 스타일 제거, 다른 주문 격리, 트랜잭션/경쟁/스냅샷 관련 55건 및 백엔드 빌드 검증. 순수 수량 함수와 실제 재구성 함수의 Tx 더블을 포함하며 운영 DB/브라우저 검증은 미실행이다.
- [x] 당시 남았던 잠금 의존 제거는 위 후속 전환에서 구현했다. 실제 운영 검증은 별도 미완료다.

### 2026-09-17 개발 환경 동기화

- 이 PC는 `5ed36cfe`에서 B1.5b를 수정하던 중 원격이 `3a3dd049`까지 16개 커밋 진행했다. 주문/카드 트랜잭션과 배정 보호 구현이 겹치며, 작업 트리를 변경하지 않는 `git merge-tree` 검사에서 `backend/src/index.ts` 충돌을 확인했다.
- 이 PC의 미완성 코드와 임시 편집 스크립트는 **로컬 전용** `backup/pc-order-sync-20260917` 브랜치의 `bfe422cd`에 보존했다. 원격에 푸시하거나 main에 병합하지 않았다. 이 커밋은 빌드/화면 전환이 미완료인 참고용 WIP이며 다른 환경에서 자동으로 조회되지 않는다.
- main은 원격 최신으로 fast-forward했다. 원격의 저장 통합·배정 가드와 최신 선금 정산 정책을 기준으로 계속한다. 백업 전체를 병합/cherry-pick하거나 과거 편집 스크립트를 실행하지 않는다. 필요한 미배정 잔량 조정·기존 잠금 의존 제거만 최신 코드에 맞춰 다시 구현하고 검증한다.
- 동기화 후 백엔드 빌드와 주문 영향/보호·카드 트랜잭션·저장 경쟁·항목 ID 검사 30건 통과. 이번에는 기능을 추가하지 않았으며 아래 미완료 체크 상태를 유지한다. 실제 운영 DB/브라우저 검증은 미실행이다.

## 진행 요약 — 완료 항목은 체크하고 다음 미완료부터 진행

- [x] 최신 정산 요구 기록: 발행본 보존·역순 삭제, 선금 발행 후 주문/스타일 변경 허용, 새 수량/단가로 재산정, 이전 정산액 기본 차감·실입금 비교 및 수동 차감액 변경. 아래 최신 정책이 이전 발행 즉시 주문 잠금 제안을 대체한다. **정책 기록 완료이며 기능 구현 완료가 아니다.**
- [x] B2/C 후속: 이전 회차 참조·발행 스냅샷·역순 취소, 청구액/입금액/확정 차감액 분리, 삭제된 스타일과 무관한 주문별 금액 상계, 미수 이월의 신규 채권 중복 방지 구현.
- [x] C 후속: 여러 주문 문서 입금의 명시적 주문별 배분, 발행 후 정정/개정본 연결과 정산 변경 이력 조회.

- [x] B1 메뉴/탭 정정: 사이드바는 청구서 하나만 표시하고 목록(`/invoices`)을 연다. 목록에서 청구서 작성 버튼을 누르면 작성 페이지(`/invoices/new`)가 새 작업 탭으로 열린다. 스타일 목록→추가와 같은 구조이며 목록/작성 사이드바 하위 메뉴는 제거했다. 기존 `/invoices/list`는 `/invoices`로 이동한다. 저장된 청구서 수정은 B2/C 구현 전까지 미지원이다.
- [x] B1 최신 UX: 수량×단가 기반 작성으로 통합. 끝의 정산 영역에서 주문별 금액 비율(빈칸 100%, 0 초과 100 이하·소수 둘째 자리)을 지정하며 전체 적용도 지원한다. A 100% + B 30% 혼합, 주문별 통화 반올림 및 출력 정산표를 구현했다. 이전 세 가지 방식 선택은 현재 화면에서 제거했다.
- [x] B1 주문 선택 표: 선택 열 너비/줄바꿈 수정, 고객 중복 열 제거, 납기·생산수량/주문수량 진행률·배정 펼치기 제공. 각 배정의 스타일·공장·배정/생산 수량·기존 진행률을 표시한다. 조회는 판매자·고객·현재 페이지 범위를 제한하고 진행률을 일괄 계산한다.
- [x] B1 후속: 고객 선선택, 해당 고객 주문만 조회, 주문·생산 기반 청구 작성 연결. 공장·고객 범위를 서버에서 함께 검사.
- [x] B1 화면 정정: 메뉴 기본은 발행 목록 영역과 청구서 작성 버튼. 버튼으로 별도 작성 화면을 열고 목록으로 돌아올 수 있다. 기본 화면에서 고객/주문을 조회하지 않는다.
- [x] B1 다중 주문: 같은 고객 주문을 체크박스로 여러 개 선택해 한 초안으로 작성. 주문·스타일별 주문/생산/청구 수량·단가·합계와 출력용 행 Remark 제공. 출력본은 주문/생산 참고 수량 및 내부 변경 사유를 제외한다.
- [x] 실제 발행 목록 데이터를 연결하고 과거의 이력 조회 불가 자리표시자를 제거했다. 작성 대상은 원장 기준 `발행 이력 없음/있음`으로 필터링하며 통화별 누적 신규채권을 표시한다. 비율 청구를 수량 잔여로 임의 환산하지 않는다.
- [x] B1.5a: 주문 항목 ID 보존.
- [x] B1.5b 준비: 현재 환경 재검증, CRLF 검사 수정, 읽기 전용 배정 영향 판정 및 단위 검사.
- [x] B1.5b 저장 통합 선행: 카드 재구성의 보드 revision 검사·Serializable 저장·충돌 시 전체 재계산.
- [x] B1.5b 저장 통합 선행: 원본 조회·공정 저장·카드 쓰기를 호출자가 전달한 단일 트랜잭션으로 분리하고 기존 재구성 경로에 연결.
- [x] B1.5b 신규 저장: 주문 생성/재사용과 구매자·판매자 카드 재구성을 같은 Serializable 트랜잭션으로 연결.
- [x] B1.5b 수정 보호 선행: 수정 트랜잭션 안에서 주문 접근 범위·updatedAt·주문/배정 잠금을 다시 확인한다. 잠금 조회도 같은 Tx를 쓰며 Serializable 충돌은 STALE_EDIT(409)로 반환한다. 기존 항목 ID 보존을 유지한다. 카드 갱신/영향 판정 통합은 아래 미완료 상태다.
- [x] B1.5b 수정 저장: 같은 Tx에서 조직별 배정 영향 판정 후 항목 ID 보존 저장과 이전/현재 구매자·판매자 카드 재구성을 실행한다. 카드 실패는 주문까지 롤백한다. 기존 배정 수량·CT/ST·실적은 재작성하지 않는다. 배정 연결 카드 삭제도 방지한다.
- [x] B1.5b 카드 조회/드래그/보드 저장/진행 상태/잠금 UI 일괄 전환 및 자동 검사. 실제 DB/브라우저 검증은 위 제한을 따른다.
- [x] B2 서버 초안 저장·목록·재개·삭제 및 동시 수정 보호. 검증 범위는 상단 2026-09-22 기록을 따른다.
- [x] C 명시적 입금 배분·정정/개정 이력.
- [ ] D 최종 잠금 정책 확정·구현 → E 운영 검증.

완료 이력은 아래에 보존하되 미완료 작업으로 반복하지 않는다. 매 작업에서 코드·검증·체크 표시를 함께 갱신하고 관련 변경을 자동 커밋·푸시한다.

2026-09-16 저장 통합 검증: 영향/가드·신규/수정 Tx·항목 FK·스냅샷/수정 경쟁·청구서 관련 81건 통과, 양쪽 빌드·번역 검사 통과. 실제 라우트/서비스와 Tx 더블로 거부/롤백 경로를 검사하고 기존 PGlite 항목 FK 검사를 유지했다. 실제 Prisma/PostgreSQL 통합·운영 브라우저 검증은 미실행이다.

2026-09-16 이번 검증: 청구서 29건(혼합 비율·기본 100%·오류 비율·주문별 반올림/출력, 주문/배정 진행 투영·조회 범위, 선택/펼치기/팝업 연결 포함), 주문 항목/잠금 경쟁 8건, 탭 탐색 3건 총 40건 통과. 양쪽 빌드·번역 검사 통과. 실제 브라우저 및 운영 DB 검증은 미실행이다. `test:order-item-identity`에 저장 경쟁 검사를 연결했다.

남은 큰 단계는 3개다: ① 정식 발행·정정·누적 정산 ② 최종 잠금 정책 확정과 구현 ③ 운영 검증. 서버 초안 저장·재개와 기존 잠금 의존 전환은 구현했으며 운영 DB/브라우저 검증은 별도로 추적한다.

## 1. 다음 작업자가 먼저 읽을 내용

### 최신 확정 — 선금 이후 수량 변경 및 회차별 정산

이 절은 아래의 과거 발행 즉시 주문/배정 잠금 제안보다 우선한다. 이번에는 사용자 결정을 문서화했으며 앱 로직은 아직 바꾸지 않았다.

1. **발행본 보존·역순 삭제:** 1차 원본을 보존한다. 후속 발행본이 있으면 앞 회차를 삭제할 수 없고 3차 → 2차 → 1차 순서로만 삭제한다. 구현은 삭제/취소의 업무 효과와 별개로 원본·취소 이력을 보존하는 방향이다. 서버 트랜잭션에서 후속 회차 존재를 검사하고 동시 발행/삭제 경쟁도 막는다. 여러 주문을 담는 발행본은 영향받는 모든 주문의 후속 참조를 검사하는 설계가 필요하다.
2. **선금 후 주문 변경 허용:** 수량 변경과 특정 스타일 삭제가 가능해야 한다. 이전 선금은 삭제된 스타일에 억지로 배분하거나 함께 소멸시키지 않고, 해당 주문의 새 총금액에서 금액으로 차감한다. 여러 주문 사이의 선금을 자동으로 섞어 상계하지 않는다. 발행본 스냅샷과 원주문/배정 수정 제한을 분리한다. 실제 생산기록·확정 CT/ST가 있는 배정 보호는 별도로 유지하며, 현재 연결 스타일 삭제 가드는 이 요구와 양립하도록 배정 이력 보존/관계 분리 후 전환해야 한다.
3. **최종 금액 재산정:** 변경된 최종 수량과 새 단가표의 해당 단가로 계산한다. 담당자가 단가를 검토하고 확정하며 과거 발행본의 수량·단가는 재계산하지 않는다. 2차 이후 작성에는 이전/최종 수량, 단가, 조정 사유, 새 총금액과 이전 회차 내역을 보여준다.
4. **차감액 검토·수정:** 이전 회차에 확정된 정산액을 기본으로 불러오되, 이전 청구액·실제 입금액·이번 차감액을 서로 다른 값으로 표시한다. 예를 들어 1차 청구 $5,000·입금 $3,000이면 불일치를 검토하게 하고 차감액을 수동 변경할 수 있다. 변경 이유·담당자·시점·기본값과 변경값을 남기며 입력을 바꿨다고 실제 입금 기록이나 과거 발행본이 수정되지는 않는다. 실제 입금 확인 없이 입금액을 0 또는 청구액으로 추측하지 않는다.
5. **중복 청구 방지 설계:** 최종 총액 $10,000에서 $3,000만 차감하면 이번 안내 총액은 $7,000이다. 이때 1차 미수 $2,000과 새 정산분 $5,000을 구분해야 한다. $7,000 전부를 신규 채권으로 더한 뒤 기존 미수 $2,000도 남겨 총 미수 $9,000으로 만드는 것은 금지한다. 미수 이월 표시/대체 처리의 저장·출력 방식은 구현 설계로 구체화하되 과거 청구액을 덮어써서 맞추지 않는다.
6. **초과 선금:** 최종 금액이 차감 대상액보다 작으면 감액 정산·환급 또는 차기 청구 차감 대상으로 표시한다. 임의 음수 일반 청구나 자동 환급 처리는 하지 않는다. 사용자 검토 후 처리 방식을 확정한다.

잠금 적용 시점은 다시 구분해야 한다. **선금 발행 자체로 주문 변경을 막지 않는다**는 것은 확정이며, 최종 정산 시 주문 전체 잠금의 정확한 트리거는 아직 확정하지 않는다. 기존 생산/급여 보호를 선금 정산 때문에 자동 해제하지 않는다.

1. `AGENTS.md`의 현재 정책과 이 로드맵을 읽는다. 전체 프로젝트의 다른 할 일은 `todo.md`를 확인한다.
2. `git status --short`, 현재 브랜치, 최신 커밋을 확인한다. 사용자 변경을 덮어쓰거나 커밋에 섞지 않는다.
3. 아래 단계 중 첫 미완료 단계부터 진행한다. 완료 표시가 있어도 운영 적용까지 끝났다는 뜻은 아니다.
4. 코드 위치는 아래 심볼로 검색한다. 환경마다 줄 번호가 달라지므로 줄 번호를 계약으로 삼지 않는다.
5. 구현·검증·커밋·푸시 후 이 문서의 체크박스와 인계 기록을 갱신한다. 실제 실행하지 않은 검증은 미실행으로 남긴다.

## 2. 사용자 요구와 현재 동작

### 사용자 요구 방향

- 2026-09-15 사용자 정정: 발행된 청구서가 있으면 주문·배정 잠금을 유지하고, 금액·주소 등 청구서 정정 저장 때문에 주문을 해제하지 않는다. 잘못 만든 청구서를 삭제하면 잠금 해제하는 방향이다. 이전의 별도 `청구 완료`만 잠금 트리거로 삼는 설계는 이 요구에 맞춰 변경한다. 여러 분할 발행본 중 일부 삭제 시 조건은 아래 미확정 사항을 따른다.
- 최신 사용자 결정: 모든 청구를 수량×단가로 작성한 뒤 주문별 금액 비율을 적용한다. 빈칸은 100%이고 동일 청구서에서 A 100%·B 30%를 허용한다. 별도 방식 선택/합의 고정금액 입력은 제거한다. 비율은 금액에만 적용하며 수량을 비율로 나누지 않는다. 금액 기준은 이번 입력한 수량·단가 합계이며 다른 계약 총액을 임의 생성하지 않는다.
- 다음 발행 시 잔여 내역은 보존된 발행 기준 수량·단가·금액과 누적 유효 발행액에서 계산해야 한다. 현재 단가표로 과거 발행액을 다시 계산하지 않는다. 수량 청구 잔량과 같은 수량 기준의 미청구 금액을 별도로 보존해 일부 비율 발행 후 같은 수량을 중복 소비하지 않도록 B2/C에서 연결한다. 현재 UI 차액은 이번 계산의 참고값이며 누적 잔여액으로 표시하지 않는다.
- 검토용 초안은 주문 전체 생산 완료를 요구하지 않는다. 부분 출하·선청구 검토가 가능하도록 생산 미완료/불명은 경고와 사용자 검토로 처리하며 단가 누락·잘못된 수량/비율은 계속 출력을 차단한다. 정식 발행 판단·원장 반영은 미구현이다.
- 청구 금액과 입금액은 별개다. 금액 방식과 수량 방식의 혼용 시 선청구 금액의 최종 정산을 따로 기록하고, 최종 청구에서 선청구를 중복 가산하지 않는다.

- 완료 주문의 생산 수량을 검토하고 필요하면 청구 수량을 조정한다. 기존 단가표를 자동 조회해 고객에게 보낼 PDF 청구서를 만든다.
- 청구서는 **별도 청구서 메뉴**에서 작성·관리한다. 한 주문을 여러 번 나눠 청구할 수 있어야 하며 주문 1건당 청구서 1장으로 제한하지 않는다.
- 청구서 메뉴는 **기존 발행 청구서 목록이 기본**이다. `청구서 작성` 버튼으로 작성 화면을 열어 **고객 선택 → 미청구 또는 잔여 청구 주문 선택 → 기존 주문·생산 정보 기반 청구 내역 작성** 순서로 진행한다. 작성 흐름을 메뉴 첫 화면으로 보여주지 않는다. 초안 저장/PDF 출력은 정식 발행이 아니므로 잠그지 않는다.
- 2026-09-16 추가 확정: 같은 고객의 여러 주문을 한 청구서에 포함한다. 작성 표는 주문별 주문 수량·생산 수량·청구 수량·단가표 단가·합계(청구량×단가)를 보여준다. 스타일별 단가가 다를 수 있어 주문/스타일 행으로 구분하며 주문 단위 임의 평균 단가를 만들지 않는다. 생산은 스타일 수준의 참고값이며 세부 색상/사이즈 실적으로 배분하지 않는다. 출력은 청구 수량·단가·금액·Remark를 포함하고 주문/생산 수량은 제외한다.
- 청구 수량은 주문 수량보다 적거나 많을 수 있다. 분할 청구와 과소·초과 마감을 구별하고 원주문 수량을 청구 수량에 맞춰 덮어쓰지 않는다.
- 잠금 해제는 **판매자 제조사(공장) 조직의 ADMIN**으로 제한한다. 고객사는 참고용이며 해제 권한이 없다. 기존 ADMIN 자격은 유지하고, 현장 직무(`roleId`)나 관리자 표시 이름으로 판단하지 않는다. 청구서 삭제/취소로 해제를 유발하는 경우에도 이 자격을 적용한다.
- 여러 개발 환경에서 이어서 작업할 수 있도록 이 로드맵을 저장소에 유지한다.

### 코드로 확인된 현재 동작 — 아직 새 정책이 아님

- 주문 잠금은 `WorkOrder.modificationLockedAt/By`에 저장된다. 주문 수정·삭제를 막으므로 단순 카드 생성 트리거만은 아니다.
- 잠글 때 `assertOrderItemsReadyForLock`, `syncAssignmentPlansForOrderLock`, 카드 재구성 등이 실행된다. 해제는 잠금 필드를 비우며 배정을 삭제하지 않는다.
- 현재 주문 잠금 해제 API에는 조직 ADMIN 전용 조건이 없다. 조직 접근 검사와 완전 잠금 해제 자격을 별도로 구현해야 한다.
- 기존 잠금은 주문·배정·작업기록 전체를 동결하는 완전 잠금이 아니다.
- 청구서 초안의 기준 커밋은 `d9a9f15a`. 정식 발행·청구서 서버 보관·완전 잠금은 아직 없다.

## 3. 권장 설계 — 구현 전에 남은 업무 선택을 확인할 것

2026-09-15 결정이 이전의 별도 청구 완료 시 잠금 설계를 대체한다. **발행된 청구서의 정정은 잠금을 유지**하며, 정정 전 주문/배정 해제를 요구하지 않는다. 잘못 발행한 청구서 삭제에 따른 해제를 설계한다. 발행본의 삭제는 이력 보존을 위해 업무상 삭제(취소)로 처리하는 안을 권장하며, 물리 삭제 방식은 확정하지 않았다. 배정 준비는 발행 잠금과 독립적이어야 한다.

| 업무 | 권장 동작 | 상태 |
| --- | --- | --- |
| 배정 준비 | 유효한 주문 생성·수정 저장 성공 시 해당 주문의 배정 카드를 갱신한다. 청구 완료 잠금은 걸지 않는다. | 2026-09-14 사용자 방향 확정, 전환 미구현 |
| 생산 중 | 기존 완료·급여 보호를 적용하면서 배정·실적 입력을 허용한다. | 권장 방향 |
| 별도 청구서 메뉴 | 주문을 선택해 이번 청구 수량을 입력하고 저장·발행·재출력한다. 주문별 누적 청구/잔량을 함께 표시한다. | 사용자 요구 확정 |
| 정식 청구 발행 | 발행 스냅샷·누적 청구와 주문/배정 잠금을 일관되게 처리한다. 분할 첫 발행/마지막 유효본 취소 규칙은 확인 중이다. | 발행 잠금 방향 확정, 세부 확인 중 |
| 주문 청구 완료 | 수량·금액이 맞는다는 이유만으로 자동 마감하지 않는다. 과소·초과 마감 업무 기준은 미정이며 잠금과 분리한다. | 미정 |
| 잠금 해제 | 판매자 제조사 조직 ADMIN만 허용하며 고객 조직에는 허용하지 않는다. 삭제/취소 시 다른 유효 발행본과 기존 급여 잠금을 검사한다. | 권한 범위 확정, 복수 발행본 조건 확인 중 |
| 발행 후 정정 | 금액·주소 등 정정 저장 동안 주문·배정 잠금을 유지한다. 원본/개정 이력 보존을 권장한다. | 잠금 유지 확정 |

추가 선택 사항:

- 공유 주문 해제 권한은 판매자 제조사 조직 ADMIN으로 확정했다. 고객사는 참고용이며 해제하지 못한다. 시스템 관리자 자동 우회는 추가하지 않는다.
- 분할 발행 세부 질문: 첫 정식 발행부터 잠그고, 정정/추가 발행은 잠금 유지, 마지막 유효 발행본을 삭제(취소)했을 때만 해제할지 사용자 확인 중이다. 일부 청구서를 삭제해도 다른 유효 발행본이 남는 경우를 임의로 해제 처리하지 않는다. 이 미확정 경로는 아직 구현하지 않는다.
- 메뉴 접근은 화면/API 사용 자격이고, ADMIN은 해제 행위 자격이다. 메뉴 권한을 받았다는 이유로 비관리자에게 해제를 허용하지 않는다. ADMIN이라고 타 조직 접근을 허용하지 않는다.
- 주문/생산 원본의 잠금은 관리자도 임의로 우회하지 않는다. 청구서 금액·주소 등의 정정은 주문/생산 원본을 바꾸는 작업과 분리하고, 주문·배정 잠금을 유지한 채 저장한다.
- 완전 잠금은 해당 주문의 데이터를 보호한다. 스타일·공정 마스터를 조직 전체에서 잠그지 않으며, 마스터 변경이 해당 주문의 저장 CT/ST나 발행본을 재작성하지 못하도록 한다.
- 기존 잠긴 주문을 새 완전 잠금으로 일괄 해석하지 않는다. 생산 중 주문이 멈추지 않도록 구 잠금과 새 잠금을 구분하는 전환이 필요하다.

### 누적 수량과 청구 마감

- 청구서 문서 상태(초안/발행/취소)와 주문 청구 상태(미청구/부분 청구/청구 완료)를 분리한다. 여기서 청구 완료는 입금 완료나 생산 완료를 뜻하지 않는다.
- 집계 권장안: 유효한 발행 행만 원주문 항목 FK별로 합산한다. 초안은 제외하고 취소/대체된 원본과 개정본을 중복 합산하지 않는다. 한 청구서가 다른 주문에도 연결될 경우 완료 판정은 주문별로 독립적이다.
- 기본 비교값은 주문 수량, 생산 실적, 기존 누적 청구량, 이번 청구량, 발행 후 누적량, 잔량/초과량이다. 주문 총합만 비교하지 말고 스타일·색상·성별·사이즈 등 원주문 행별 차이를 보여 서로 다른 품목의 과소/초과가 상쇄되지 않게 한다.
- 예: 1,000장 주문에서 500장씩 나누어 발행해도 누적 마감과 잠금을 혼동하지 않는다. 첫 발행 잠금의 분할 적용 세부는 확인 중이며, 추가·정정 발행 때문에 잠금을 해제하는 흐름은 만들지 않는다.
- 예: 누적 980장은 잔여 20장의 후속 청구인지, 부족 생산에 따른 최종 마감인지 수량만으로 알 수 없다. 1,020장도 초과 생산 청구인지 중복 청구인지 검토해야 한다. **`누적 >= 주문 수량`만으로 무조건 완료/잠금하지 않는다.**
- 권장안: 차이가 있으면 정상 분할 청구 상태를 유지하고, 사유·최종 인정 수량을 남긴 명시적 과소/초과 마감으로 완료할 수 있게 한다. 허용 오차, 승인 역할, 생산 수량과의 관계는 구현 전 결정하며 임의 허용률을 만들지 않는다.
- 발행본과 원주문 항목 FK를 보존하고, 정정은 개정 이력으로 연결한다. 잠긴 주문에서도 청구서 정정은 가능해야 하며 주문/생산 원본을 수정하는 행위와 구분한다.
- 정정 저장으로 수량·금액이 달라져도 잠금은 유지한다. 삭제/취소로 잠금을 해제할 조건과 담당자 이력을 별도로 설계하며, 급여 잠금·생산 완료 보호까지 같이 해제하지 않는다.

## 4. 단계별 작업 및 완료 기준

### A. 청구서 초안 — 코드 완료

아래는 A단계 당시 완료 이력이다. 현재 진입점은 B1의 별도 청구서 메뉴이고 구 invoice-source API도 ORDER 대신 INVOICE 권한을 검사한다.

- [x] 판매자 주문 상세 진입, ORDER 접근 검사, 저장된 주문 세부 수량·스타일별 생산량 조회.
- [x] CMT/FP·통화 선택, 활성 버킷/스타일 예외 단가 조회, 청구 수량 조정·행별 변경 사유.
- [x] 소수 금액 정수 연산, 행별 반올림, 영문 검토용 PDF 인쇄.
- [x] `DRAFT / NOT ISSUED` 표시. 서버 저장 없이 창을 닫으면 폐기.
- [ ] 운영 완료 주문과 수동 완료 조정 사례로 API·화면을 실제 검증.
- [ ] 한국어/베트남어 이름과 다페이지 PDF를 실제 출력물로 확인.

### B. 별도 청구서 메뉴·분할 청구 설계 — B1/B2 완료, C 진행 중

- [x] B1: 영업 관리 → 청구서(`/invoices`)는 목록을 기본 화면으로 하고 별도 작성 화면에서 고객/주문을 선택한다. INVOICE 독립 권한과 주문/고객 변경 시 작성 후보 갱신을 유지한다. 실제 발행 목록 데이터는 아직 없으며 조회 불가 상태로 표시한다.
- [x] B1: 주문 상세의 초안 버튼을 제거하고 별도 메뉴에서 기존 수량 초안을 재사용한다. 기존 주문 잠금 버튼은 배정 준비 분리 전까지 기존 기능 그대로 유지한다. 새 완전 잠금 버튼은 만들지 않았다.
- [x] B1 당시 이력: 수량/총액 비율/합의 금액의 입력·검토용 PDF. 현재 화면은 상단 최신 정책의 수량 기반·주문별 비율로 대체됐다. 이전 금액 계산 유틸 검증은 호환 이력으로 유지한다.
- [x] B1: 통화별 소수 자릿수·정수 소수 연산을 사용한다. 최신 주문별 비율은 0 초과 100 이하·소수 둘째 자리까지이며 빈칸 100%. DRAFT 표시를 유지하고 금액/통화/비율 변경 후 재검토가 필요하다.
- [x] B2: 서버 초안 저장/목록/재개와 초안 수정/삭제·동시 수정 검증. 회차 관리는 정식 발행 원장인 C에서 계속한다.
- [x] B2/C: 주문별 청구 현황과 메뉴 이동 연결. 누적 신규채권은 유효한 발행 원장 기준으로 통화별 표시한다.
- [x] 주문 선택 후 이번 청구 행/수량과 기존 유효 발행·실입금·배분 이력을 조회한다. 비율 청구를 수량 잔여로 환산하지 않고 미완료 생산은 기존 검토 경고 정책을 유지한다.
- [x] 같은 고객의 여러 주문을 한 청구서에 합산하는 요구 확정 및 검토 초안 구현. 다른 고객/판매자 또는 중복 주문은 거부한다.
- [x] 원장에서도 주문↔청구서 다대다 연결을 지원한다. 주문 1건당 문서 1개라는 유니크 제약은 만들지 않는다.
- [x] 수량·금액 일치만으로 자동 마감하지 않는다. 잠금 기준은 최신 발행/정정 정책을 따르며 과거의 별도 청구 완료 트리거 설계를 대체한다.
- [x] 공유 주문 해제 조직 범위: 판매자 제조사(공장) ADMIN, 고객사는 참고용.
- [ ] 과소·초과 마감의 승인·사유·기준 수량은 미정으로 유지한다.

완료 기준: 청구서 메뉴에서 같은 주문을 여러 차례 청구하는 흐름과 주문별 완료 조건을 검토할 수 있다. 순서는 B → C → D → E이며 정식 발행 공개는 C·D의 통합 검증 이후다.

### B1.5 주문 저장 → 배정 카드 갱신 전환 — 코드/자동 검사 완료, 운영 검증 대기

2026-09-14 조사 결과다. 주문 항목 PK 재생성은 B1.5a에서 수정했고 나머지 결합은 B1.5b 대상이다. B1의 청구서 메뉴가 새로 발생시킨 장애라는 뜻은 아니다.

- [x] B1.5a: 주문 수정 시 현재 주문 범위에서 공개 항목 ID로 기존 행을 찾고 PK를 유지해 수정한다. 새 행만 생성하고 제출에서 빠진 행만 삭제한다. 레거시 빈 itemId는 기존 응답과 같은 PK 문자열로 비교한다. 중복 식별자는 409로 거부하며 스타일/색상 유사도로 추측하지 않는다.
- [x] B1.5a 검증: 백엔드 빌드, 주문 항목 5건 및 청구서/금액/주문 검색 19건 통과. PGlite의 별도 최소 스키마에서 반복 수정 후 FK 유지와 참조된 행 삭제 실패 시 롤백을 검증했다. 실제 Prisma API/운영 DB 통합 검증은 아니다.
- [x] B1.5b: 카드 조회·드래그·보드 저장·진행 상태·기존 잠금 UI를 함께 전환했다. 청구된 행의 삭제·속성 변경 보호는 원장 구현 시 추가한다. 아래 표와 이전 진행 기록은 전환 전 조사 이력이며 최신 상태는 상단 2026-09-17 절을 따른다.

| 현재 결합 | 확인된 코드 | 전환 시 필요한 조치 |
| --- | --- | --- |
| 잠긴 주문만 카드 생성 | 카드 재구성 조회의 `modificationLockedAt: { not: null }` | 저장된 유효 주문의 배정 준비 조건으로 대체. 미완성 스타일 연결 검증 유지 |
| 수정 저장은 카드/배정 갱신 안 함 | `PUT /orders/:orderId`는 WorkOrder/WorkOrderItem만 저장 | 저장 직후 관련 주문 카드 갱신과 배정 영향 검증 연결 |
| 신규 저장은 재구성을 호출하나 미잠금 주문은 제외됨 | `POST /orders` + 같은 잠금 필터 | 생성/수정 저장의 준비 조건 일치 |
| 미잠금 카드 드래그 금지 | AssignBoard의 `isManualOrderLocked` / `ORDER_UNLOCKED` | 생산 준비 상태와 청구 완료 잠금으로 판정 분리 |
| 보드 저장도 미잠금 주문을 409로 차단 | `changedWorkOrderIds`, `order manual lock required before scheduling assignment` | 프론트와 같은 새 판정 사용, 서버 검증 제거만으로 처리하지 않음 |
| 미잠금 주문 진행 상태가 EDITING으로 돌아감 | `resolveAutoOrderProgressStatus`, `resolveCanonicalWorkOrderStatusForLockState` | 주문/생산 상태와 청구 마감 상태 분리 |
| 잠금 시 배정 수량·ST까지 변경 | `syncAssignmentPlansForOrderLock` | 카드 표시 갱신과 기존 배정 재계산의 의미를 분리해 검증 |
| 주문 세부 행 PK 보존 — B1.5a 완료 | `planOrderItemWrites` + 범위 제한 삭제/개별 수정·생성 | B2/C에서 청구 행 FK와 발행 후 수정·삭제 보호 연결 |

- [x] 갱신 트리거를 유효한 주문 생성/수정 저장으로 옮겼다. 구 잠금 API를 종료했으며 잠금/해제로 배정 카드를 삭제·재생성하지 않는다.
- [x] 조회·드래그·보드 저장·진행 상태와 잠금 버튼을 함께 전환했다. 과거 잠금 필드를 새 청구 완료 잠금으로 사용하지 않는다.
- [x] 미배정 잔량 갱신과 기존 배정 수량 조정을 분리했다. 저장 경로는 `syncAssignmentPlansForOrderLock`을 호출하지 않으며 기존 배정·CT/ST를 재분배하거나 재계산하지 않는다.
- [x] 수량 감소·스타일 제거 시 실적/완료/급여 구분과 무관하게 모든 기존 배정을 보호한다. 배정량 침범·연결 스타일 삭제는 검토를 요구하고 0수량 배정도 보존한다.
- [x] 동일 총수량의 성별 변경을 검사한다. 단일 성별 유지 범위만 허용하고 혼합 성별 수량 변경은 근거 부족으로 차단한다. CT와 기존 실적/급여값은 재작성하지 않는다.
- [ ] 주문 항목의 안정적인 PK/FK를 유지한다. 부분 청구된 행의 삭제·스타일/색상/성별 변경은 발행 이력 추적을 끊지 않게 설계한다. 이름·문자열 우연 매칭으로 청구 관계를 복원하지 않는다.
- [x] 주문 저장과 양쪽 카드 갱신을 같은 Serializable Tx로 연결했다. 카드 실패 시 주문까지 롤백하며 revision·경쟁 검사와 열린 탭 이벤트를 연결했다. 실제 DB/브라우저 검증은 별도다.
- [ ] 신규 저장, 수량 증가/감소, 납기/표시 변경, 스타일 추가/제거, 다중 배정, 동일 총수량 성별 변경, 완료/급여 보호, 중간 실패 롤백·재시도, 반복 저장 카드 중복 없음, 청구 행 FK 보존을 회귀·통합 검증한다.

완료 기준: 주문 저장 성공 후 관련 카드가 일관되게 갱신되고 기존 실적·확정값·배정 연결을 보존한다. B1.5 → B2 → C → D 순으로 진행한다. D의 배정 준비 관련 항목은 B1.5에서 완료한 내용을 재구현하지 않고 검증 결과를 연결한다.

#### B1.5b 진행 단위 — 2026-09-15 현재 환경 재조사

- [x] 현재 코드와 이전 인계 비교 및 기준 검사 재실행. B1/B1.5a 구현과 B1.5b 미구현 상태가 일치한다.
- [x] Windows CRLF 체크아웃에서 배정 스냅샷 검사가 빈 코드 구간을 읽던 문제 수정. 검사 입력만 LF로 정규화하며 앱 동작은 변경하지 않는다.
- [x] 첫 구현: `backend/src/utils/orderAssignmentImpact.ts`의 `analyzeOrderAssignmentImpact`로 변경 전후 스타일별 수량·성별 구성과 기존 배정의 영향 판정을 분리했다. 표시/납기만 변경하면 배정 재검토 대상이 아니며, 잔량/초과량·스타일 추가/제거·같은 총수량 성별 변경을 구별한다. 실적·완료·급여 잠금 배정 ID와 0수량 배정을 보존해 반환한다. 기존 배정 수량과 CT/ST를 변경하거나 재분배하지 않는다.
  - 한 주문·한 조직으로 FK 범위를 제한한 입력을 요구한다. 수량/ID/보호 플래그 누락·비정상값·중복 배정 ID를 거부한다. 조직별 배정 합계를 서로 더하지 않는다.
  - 단위 검사 7건 완료. 이는 변경 영향의 사실 판정이며 저장 허용/차단 가드가 아니다. 실제 API 호출·조직 범위 조회·트랜잭션·급여 잠금 경쟁 검증은 다음 통합 단계에 남아 있다.
- [x] 주문·항목·카드 갱신의 트랜잭션 경계를 통합했다. 아래 완료 항목과 상단의 실제 DB 검증 제한을 따른다.
  - [x] 카드 재구성 시작 시 보드 revision을 읽고, `commitAssignmentCardRebuild`의 Serializable 트랜잭션 안에서 다시 비교한 뒤 카드를 저장한다. 충돌 시 기존 최대 3회 재시도 루프에서 원본 조회·계산부터 다시 수행하며 오래된 카드의 저장만 재시도하지 않는다.
  - [x] `rebuildAssignmentCardsForOrgTx(orgId, db)`로 조직/거래관계/주문/스타일/저장 카드 조회, 공정 저장, 카드 쓰기를 동일 클라이언트로 연결했다. 기존 재구성은 이 함수 전체를 Serializable 트랜잭션에서 호출하므로 카드 저장 실패 시 선행 공정 저장도 같은 트랜잭션에서 취소된다.
  - [x] 주문 생성/재사용의 구매자·판매자 카드 갱신을 같은 트랜잭션에서 수행한다. 중복 조직은 제외하고 ID 순서대로 실행한다. 레거시 소유 조직 교정도 이전 조직까지 같은 트랜잭션에 포함한다. P2034/P2002 경쟁은 전체 트랜잭션을 재시도하며 카드 실패를 기존 주문 조회 성공으로 덮지 않는다.
  - [x] 수정 저장 연결 및 영향 판정 가드. 주문 저장의 Tx 경로는 기존 배정 스냅샷 갱신과 조직 전체 진행 상태 후처리를 호출하지 않는다. 다른 기존 재구성 래퍼의 후처리는 유지한다. 주문 잠금 필터가 아직 남아 있으므로 미잠금 주문의 카드가 즉시 노출되는 최종 동작은 미완료다.
  - 단일 성별 스타일은 같은 성별을 유지하며 각 조직의 기존 배정 합계 이상인 범위에서 수량을 증감할 수 있다. 조직별 배정량을 합산하지 않는다. 연결된 스타일 삭제·배정량 침범·성별 전환·혼합 성별 수량 변경은 409로 먼저 배정 검토를 요구한다. 혼합 성별 수량 변경은 배정별 성별 근거가 없는 단계의 보수적 제한이며 자동 재분배하지 않는다. 미배정 스타일 추가/제거는 허용한다.
  - 구매자/판매자 변경은 배정이 있으면 거부한다. 배정의 주문·스타일·수량 FK 누락/불일치도 거부하며 문자열로 복원하지 않는다. 실적/완료/급여 여부와 무관하게 모든 배정 수량·스냅샷을 보존한다. 급여 주석 함수도 전달받은 Tx를 사용하나 현재 급여 잠금 로더는 기존 정책에 따라 빈 집합을 반환한다(수당 스냅샷을 최종 급여 잠금으로 해석하지 않음).
  - 주문 생성/수정 카드 재구성은 기존 배정에서 참조하는 카드를 삭제 대상에서 제외한다. 기존 잠금 필터 때문에 새 목록에 빠져도 FK가 끊어지지 않는다. 이 옵션은 주문 저장 경로에 적용하며 다른 재구성 동작은 유지한다.
- [x] 전환 구현: 준비 판정과 저장 트리거, 드래그/보드 저장, 진행 상태, 기존 잠금 UI/데이터 해석, 열린 탭 이벤트를 함께 전환했다. 자동 검사는 완료했고 실제 브라우저/DB 검증은 남아 있다.

추가 확인: `syncAssignmentPlansForOrderLock`는 `db`를 받지만 급여 잠금 조회인 `annotateAssignmentPlanRowsWithPayrollLocks(orgId, plans)`에는 전달하지 않는다. 저장 경로에 통합할 때 해당 조회의 DB 사용과 동시 급여 잠금 경쟁도 점검해야 한다. 현재 코드를 그대로 옮기는 방식은 채택하지 않는다.

### C. 청구서 저장·정식 발행

- [ ] 한 주문의 여러 청구서 및 한 청구서의 여러 주문 연결과 행별 누적 집계를 구현한다. 두 방향 모두 사용자 요구로 확정됐으며 추가 확인 대상이 아니다.
- [ ] 수량·단가 스냅샷과 주문별 기준액·비율·이번 청구액을 연결해 저장한다. 잔여 수량과 동일 수량 기준의 미청구 금액을 분리하고 비율 청구를 수량으로 환산하지 않는다. 누적 청구 차감은 유효 발행본만 반영하며 초안/PDF 검토는 잔여를 소비하지 않는다. 취소·정정·동시 발행 시 잔여 복원/중복 차감 방지를 검증한다.
- [ ] 혼합 회차의 선청구 정산: 수량×단가 총액, 이미 청구한 선금의 정산 배분액, 이번 순청구액을 구분한다. 원선금 청구와 정산 연결을 남겨 누적액 중복 합산·이중 차감을 막는다. 기청구액과 실제 입금액을 혼동하지 않는다.
- [ ] 30%+40%+잔액의 통화 반올림 잔차, 수량 분할+선금 차감, 일부 취소/개정 후 잔액을 검증한다. B1의 독립 금액 계산은 누적 정산 구현이 아니다.
- [ ] 정식 발행 단가 구간 기준을 원주문 수량 또는 청구 수량 중 확정한다. 현재 초안은 기존 계산을 유지해 각 주문 내 스타일별 청구 수량 합계를 사용한다. 여러 주문의 같은 스타일을 합산해 단가 구간을 바꾸지 않는다.
- [x] 문서·주문 연결·청구 행의 발행 스냅샷 스키마와 비파괴 생성 SQL을 구현했다. 행은 원주문 항목 FK 및 발행 시 표시값·수량·단가·단가 출처를 보관한다.
- [x] 취소 상태·사유·행위자·시각을 원본에 보존하고 후속 발행이 있으면 역순 취소만 허용한다.
- [x] 개정본 연결과 주문별 명시적 회차 번호·정산 연결을 구현했다.
- [x] 초안 서버 저장·재개·동시 수정 방지 및 생성 재시도 중복 방지(B2).
- [x] 조직별 고유 정식 청구번호·순번과 발행 중복 요청 방지 서버 기반. 초안 reference는 정식 청구번호로 자동 채택하지 않으며 입력 번호의 조직별 중복을 거부한다.
- [x] 발행 직전에 서버에서 조직 범위·원주문 변경·이번 수량·현재 단가를 재검증하고 브라우저 계산 결과를 신뢰하지 않는다. 부분 발행을 허용하므로 주문 전체 생산 완료 조건은 발행 차단에 재사용하지 않는다. 기청구 누적/잔여 검증은 다음 원장 집계 단계다.
- [ ] 발행본·누적 청구·주문/배정 잠금 변경을 일관되게 확정한다. 정정 저장은 잠금을 유지하고, 실패 시 기존 발행본과 잠금이 함께 보존되어야 한다. 삭제/취소와 잠금 해제는 복수 발행본 조건을 확정한 뒤 원자적으로 구현한다.
- [ ] 발행 당시 거래처·주소·통화·수량·단가·결제조건·문구를 고정한다. 마스터 변경 뒤에도 동일 발행본을 재출력한다. 원본 PDF 보관 여부와 템플릿 버전 보존도 결정한다.
- [x] 취소/개정본·원본 연결·사유·행위자 이력을 보존하고 개정 시 원본 채권을 중복 집계하지 않는다. 비율 청구는 재청구 가능 수량으로 환산하지 않는다.
- [x] 서버 저장이 된 정식 발행본만 발행 목록과 재출력에 표시하며 DRAFT 문구를 사용하지 않는다. CMT/FP 기준은 발행 스냅샷에 고정한다.

완료 기준: 고객에게 보낼 정식 PDF를 보관·재출력할 수 있고, 관련 주문·배정의 보호와 감사 이력을 검증한다.

### D. 발행 잠금·정정 시 유지·삭제/취소 시 해제와 배정 준비 분리

- [ ] 위 미확정 업무 선택을 사용자와 확정하고 이 문서에 기록한다. 독립적인 코드 조사·쓰기 경로 목록화는 먼저 진행할 수 있다.
- [ ] 카드 생성·배정 수량/ST 동기화가 잠금 및 재잠금에 의존하는 모든 경로를 조사한다. 조회 필터와 주문 진행 상태 계산도 포함한다.
- [ ] B1.5의 주문 저장 트리거 전환 완료 여부를 확인하고 발행 잠금과 통합 검증한다. 반복 저장으로 카드 중복/기존 배정 삭제/CT·급여 변조가 발생하지 않아야 한다.
- [ ] 잠금 근거 발행본·행위자·시각·잠금/해제 이력과 기존 데이터 전환 방식을 설계한다. 예전 잠금 시각만으로 과거 주문을 일괄 완전 잠금 처리하지 않는다.
- [ ] 발행과 잠금, 정정과 잠금 유지, 삭제/취소와 조건부 해제를 원자적으로 실행한다. 복수 발행본·추가 발행과 삭제의 경쟁·정정 저장 실패·중복 삭제 요청을 검증한다. 복수 발행본 조건은 사용자 확인 후 구현한다.
- [ ] 판매자 제조사 조직 ADMIN 해제 검사를 서버에 구현한다. 고객 조직에는 허용하지 않는다. 프론트는 같은 자격으로 버튼과 설명을 표시한다. 사용자 payload의 역할·이메일을 인증 근거로 쓰지 않는다.
- [ ] 완전 잠금 쓰기 가드를 공통화하고 아래 경로를 실제 트랜잭션 안에서 검증한다.
  - 주문 수정/삭제, 일반 잠금 해제, 복사·가져오기·일괄 처리 중 기존 주문 변경.
  - 배정 생성/삭제/재귀속/수량/일정 변경, 보드 전체 저장, 완료 해제·완료 조정.
  - 작업기록/외주기록 생성·수정·삭제·import, 공정 버전 적용·CT/ST 재계산.
- [ ] 전체 보드 저장에 잠긴 배정이 섞여 있어도 무관한 배정의 수정은 저장 가능하게 한다. 잠긴 배정의 실제 변경은 거부하고 좌표 재계산 같은 파생값은 기존 보존 규칙과 조율한다.
- [ ] 잠금과 수정의 동시 요청을 검증한다. 검사 이후 쓰기 사이에 잠금이 걸리는 우회를 막는다.
- [ ] 비ADMIN+메뉴 권한 보유의 해제 실패, ADMIN의 정상 해제, 타 조직 ADMIN 차단, 위조 요청 차단, 재잠금·이력 보존을 통합 테스트한다.
- [ ] 잠금 해제가 기존 급여 잠금·완료 보호를 해제하거나 배정/작업기록을 삭제하지 않음을 검증한다.

완료 기준: 발행된 청구서 정정은 주문·배정 잠금을 유지한다. 삭제/취소에 따른 해제는 확정된 발행본 조건과 판매자 제조사 ADMIN 자격을 적용하고 이력을 보존한다. 생산 준비와 급여 보호는 청구 잠금과 분리한다.

### E. 운영 적용 및 인계

- [ ] 운영 DB 대상 확인, 기존 잠금 주문·완료 상태·FK 연결의 읽기 전용 사전 감사.
- [ ] 분리된 테스트 DB/schema에서 마이그레이션 반복 실행과 실패 롤백 검증. 운영에 가짜 주문·작업기록을 만들어 검증하지 않는다.
- [ ] 실제 배포 파이프라인과 필요한 스키마 적용을 확인한다. 푸시 성공을 운영 배포 성공으로 기록하지 않는다.
- [ ] 운영 역할별 메뉴·직접 API 호출, 50%+50% 분할 청구, 98% 부족 마감, 102% 초과 검토, 품목별 차이 상쇄 방지, 주문/배정 잠금, 실제 PDF 출력을 검증한다. 예시 비율은 허용 오차 정책이 아니다.
- [ ] 최종 커밋·검사 결과·운영 적용 여부·남은 문제를 아래 인계 기록에 남긴다.

## 5. 코드 진입점과 검사

| 영역 | 파일 / 검색 심볼 |
| --- | --- |
| 주문 잠금·단가·생산 진행 API | `backend/src/index.ts`: `modification-lock`, `buildOrderModificationLockState`, `syncAssignmentPlansForOrderLock`, `rebuildAssignmentCardsForOrgIds`, `buildAssignmentPlanProgressRows`, `invoice-source` |
| 인증·역할 | `backend/src/middleware/access.ts`, `Employee.orgRole`, `requireOrgRole` |
| 메뉴 접근 정책 | `backend/src/index.ts`: `hasRoleAccessPolicyFeature`; `frontend/src/utils/roleAccessPolicyCore.mjs` |
| 데이터 모델 | `backend/prisma/schema.prisma`: WorkOrder, WorkOrderItem, AssignmentPlan, CustomerSalesPriceList |
| 주문 UI / 초안 | `frontend/src/pages/App/order/OrderList.jsx`, `InvoiceDraftDialog.jsx` |
| 청구서 메뉴 / 금액 방식 | `frontend/src/pages/App/invoice/InvoiceWorkspace.jsx`, `frontend/src/utils/invoiceBilling.mjs`, `frontend/src/constants/invoiceMessages.js` |
| 초안 원본 및 계산·인쇄 | `backend/src/services/invoiceSource.ts`, `frontend/src/utils/invoiceDraft.mjs` |
| 배정·실적 UI | `frontend/src/pages/App/assign/`, `frontend/src/pages/App/work/` |
| 현재 회귀 | `scripts/invoice-draft.test.mjs`, `scripts/access-policy-regression.test.mjs`, `scripts/assignment-snapshot-integrity-regression.test.mjs` |

현재 실행 가능한 검사: `npm run test:invoice-draft`, `npm run test:access-policy`, `npm run test:assignment-snapshot-integrity`, `npm --prefix backend run build`, `npm --prefix frontend run build`, `npm run check:encoding`, `npm run check:i18n`.
주문 행 보존 검사: `npm run test:order-item-identity`. Windows PowerShell에서 `npm.ps1` 실행 정책으로 차단되면 같은 명령을 `npm.cmd`로 실행한다.
잠금 변경 시 위 기존 검사 외에 실제 API/DB 권한·동시성 테스트를 추가해야 한다. 기존 검사 통과만으로 완전 잠금을 검증했다고 간주하지 않는다.

## 6. 인계 기록

- 2026-09-16 / 다중 주문 초안: 고객별 주문 체크박스와 선택 건수·작성 버튼 추가. 페이지/검색 이동에도 선택을 보존하고 고객 변경 시 비운다. 선택 주문의 원본을 모두 읽은 후 초안을 열며 하나라도 실패하면 부분 초안으로 진행하지 않는다. 원본의 고객/판매자 ID를 검증하고 원주문 itemId/styleId를 보존하면서 화면 키와 가격 집계 키만 주문별로 분리한다.
- 작성 요약은 주문/스타일별 주문·생산·청구 수량·단가·합계이며 청구 수량은 세부 행에서 수정한다. 세부 행 Remark와 문서 전체 Remarks를 출력한다. PDF 표에 주문번호와 Remark를 추가하고 내부 참고 수량·수량 변경 사유는 출력하지 않는다. 통화별 정수 소수/행 반올림과 단가 누락 차단, 수량 초안의 기존 생산 완료 검사를 유지한다. 서버 저장/정식 발행·누적 잔여 판별은 미구현이다.
- 검증: 청구 관련 26건, 양쪽 빌드, 메시지 키·인코딩 검사 통과. 동일 스타일의 서로 다른 주문 단가 구간, 원본 FK 보존, 고객/판매자 격리, 중복 주문 거부, 출력 Remark 이스케이프·참고값 제외, 여러 페이지 선택/고객 변경 초기화를 검사했다. 실제 브라우저/PDF 인쇄·운영 배포는 미검증이다.

- 2026-09-16 / 메뉴 진입 UX 정정: 발행 목록 기본 화면과 작성 화면을 분리했다. 기본 화면은 고객/주문 API를 호출하지 않고 `청구서 작성` 버튼을 제공한다. 작성 화면에서 고객 → 주문 → 기존 초안으로 연결하고 목록 복귀 버튼을 제공한다. 작성 다이얼로그가 열린 동안 목록 복귀 버튼을 비활성화해 닫기/입력 폐기 확인을 먼저 거친다.
- 실제 발행 목록·미청구/잔여 필터는 미완료: 발행 저장 모델/API가 없어 목록 조회 불가 및 작성 후보의 이력 확인 필요를 표시한다. 모든 주문을 미청구로 취급하거나 잔여량을 원주문 수량으로 채우지 않는다. B2/C에서 유효 발행·취소·개정 및 수량/금액 혼합 정산을 근거로 서버 필터와 목록을 연결해야 한다.
- 메뉴 분리 검증: 청구서 관련 22건 및 양쪽 빌드 통과. 기본 화면에 고객 선택이 없고 API를 호출하지 않는지, 작성 버튼 진입/목록 복귀와 기존 고객 전환 동작을 컴포넌트 더블로 검증했다. 실제 브라우저·발행 원장·운영 배포 검증은 미실행이다.

- 2026-09-16 / 고객별 청구 작성: `/invoices/customers`는 공장이 판매자인 주문의 고객만 반환한다. `/invoices/orders`는 `buyerOrgId`를 필수로 받고 판매자·고객 FK를 함께 검사한다. 화면은 고객 선선택 → 주문 검색/선택 → 기존 주문/생산 기반 초안 작성이다. 고객 전환 시 목록·페이지·선택을 초기화하고 이전 요청 응답은 무시한다. 조직 변경은 전체 상태를 새로 만든다. 초안 원본 조회에도 선택 고객을 전달해 교차 고객 조회를 막는다.
- 2026-09-16 / 정책 반영: 해제는 판매자 제조사(공장) ADMIN만 가능하고 고객사는 참고용이다. 금액/주소 등 청구서 정정 저장은 주문·배정 잠금을 유지한다. 잘못 만든 발행본 삭제 시 해제 방향이며 물리 삭제 대신 이력 보존 취소를 권장한다. 과소·초과 마감 기준 및 복수 발행본의 일부 삭제/첫 분할 발행 적용은 미확정이다. 발행/잠금 로직 자체는 이번에 구현하지 않았다.
- 검증: 청구서 검사 21건(신규 고객 범위/API 검사 3건, 화면 상태·지연 응답 검사 2건 포함), 프론트/백엔드 빌드, 메시지 키·인코딩 검사 통과. 화면 검사는 JSX와 hooks를 실행하는 더블이며 실제 브라우저 클릭·운영 API·DB·PDF 출력·배포 확인은 미실행이다. 현재 정식 발행/수정 발행/삭제·잠금은 아직 미구현이며 고객 선택 흐름만 앱에 반영했다.

- 2026-09-15 / B1.5b 신규 주문 통합: `createOrReuseSharedOrder`의 생성/재사용/소유 조직 교정 성공 분기 모두 `rebuildOrderPartyCardsTx`를 같은 Serializable 트랜잭션으로 실행한다. POST의 커밋 이후 별도 재구성을 제거했다. 이 저장 경로에서 기존 배정 CT/ST를 자동 재작성하지 않는다. P2002에서 트랜잭션 밖 기존 주문을 반환하던 폴백은 제거하고 제한된 전체 재시도로 바꿨다. 지속 충돌은 오류로 남긴다.
- 검증: 백엔드 빌드와 관련 57건 통과. `test:order-card-transaction` 7건을 전체 회귀 명령에 연결했다. 컴파일된 실제 생성/양쪽 조직 조정 함수를 실행하는 트랜잭션 더블로 두 번째 조직 실패의 전체 롤백, 레거시 소유자 복원, 재사용, 경쟁 재시도/상한, POST 후속 별도 저장 제거를 검증했다. 카드 재구성은 더블이며 실제 Prisma/PostgreSQL 통합·운영 화면·배포 검증은 미실행이다. 수정 저장·미잠금 카드 노출/드래그 전환은 아직 미완료다.

- 2026-09-15 / B1.5b 재구성 트랜잭션 분리: `rebuildAssignmentCardsForOrgTx`가 전달받은 Prisma 트랜잭션으로 원본 조회부터 카드 저장까지 수행한다. `getAccessibleStyleOwnerOrgIds`도 DB 인자를 받도록 변경했다. 기존 재구성 래퍼에 연결했고 기존 최대 3회 충돌 재시도는 유지한다. 주문 저장과 양쪽 조직 통합은 아직 미완료다.
- 검증: 백엔드 빌드, 관련 검사 50건 통과. 추가 검사는 컴파일된 실제 Tx 함수를 의존성 더블로 실행해 조직 범위/카드/관계/공정/ST 조회와 저장에 동일 클라이언트가 전달되는지, 카드 실패가 전파되는지를 확인한다. 기존 트랜잭션 더블의 롤백/경쟁 검사도 통과했다. 실제 PostgreSQL 통합 롤백·동시 요청·대량 재구성 성능/30초 제한·운영 배포 검증은 미실행이다. 트랜잭션 범위가 넓어졌으므로 성능 확인은 후속 필수다.

- 2026-09-15 / B1.5b 카드 저장 경쟁 보호: `rebuildAssignmentCardsForOrg`에서 원본 카드 조회 전 revision을 캡처하고 저장 직전 검증한다. 기존 보드 저장과 같은 Serializable 격리 수준으로 검사 이후 경쟁도 처리한다. STALE_EDIT이면 전체 재구성을 최대 3회 시도하고 계속 충돌하면 오류를 반환한다. 실제 카드 재구성 호출부에 연결 완료. 주문 저장 트리거/잠금 정책/CT·ST 계산은 전환하지 않았다.
- 검증: 백엔드 빌드 및 편집 revision·주문 영향·항목 ID·배정 스냅샷·청구 초안/금액 총 49건 통과. 신규 3건은 오래된 후보의 쓰기 차단, 최신 후보 허용, 실패 롤백·직렬화 충돌의 더블 검증, 실제 재구성 호출부와 전체 재시도 연결을 확인한다. 실제 PostgreSQL 동시 요청·운영 배포 검증은 미실행. ① 전체를 완료로 표시하지 않는다.

- 2026-09-15 / B1.5b 준비 모듈: `analyzeOrderAssignmentImpact`와 `test:order-assignment-impact` 구현, 전체 회귀 명령에 연결. 새 7건과 기존 청구/항목/접근/배정 45건 총 52건 통과, 백엔드 빌드 통과. 스키마/운영 데이터/앱 저장 트리거 변경 없음. 다음은 트랜잭션 경계와 실제 호출부 통합이다. 이 항목과 위 진행 요약을 기준으로 이어간다.

- 2026-09-15 / 현재 환경 대조: 시작 기준 `main`, `d4b69ecf`, 작업 트리 깨끗함. 청구 메뉴·3종 초안·판매자 범위/INVOICE 권한·주문 항목 차등 저장을 확인했다. Prisma 스키마에 청구 원장 모델은 없고, 주문 수정은 아직 카드 갱신을 하지 않으며 생성은 잠금 필터가 있는 재구성을 호출한다. 카드 조회/드래그/보드 저장/진행 상태의 기존 잠금 의존은 그대로다.
- 2026-09-15 / 작은 수정: `scripts/assignment-snapshot-integrity-regression.test.mjs`의 소스 읽기에 CRLF 정규화를 추가했다. 기존 `reviewReason` 구간 검색은 LF 문자열을 사용해 Windows에서 빈 문자열을 검사하고 있었다. 운영 로직 변경은 없으며 B1.5b 본 전환은 미완료다.
- 2026-09-15 / 검증: 백엔드·프론트 빌드 통과. 청구 초안/금액 16건, 주문 항목 5건(PGlite 포함), 접근 정책 13건, 배정 스냅샷 11건으로 총 45건 통과. 최초 배정 검사 1건 실패는 위 CRLF 수정 후 해소했다. 프론트 큰 번들 경고와 Node 모듈 유형 경고는 남아 있다. 전체 회귀·기존 판매단가 실패 재확인·운영 API/DB·브라우저/PDF·배포 검증은 미실행. 커밋/푸시 미실행.

- 2026-09-13 / `d9a9f15a`: A단계 코드 커밋·origin/main 푸시 완료. 프론트·백엔드 빌드, 청구서 7건+주문/접근권한 16건 통과. 합성 샘플 Edge PDF 생성. 운영 API/UI·배포 확인 미실행.
- 기존 판매단가 검사 11건 중 1건 실패: `sales-price-regression.test.mjs`의 `prices: dirtyPriceChanges.map` 문자열 기대가 CustomerPricingBoard와 불일치. 해당 화면/검사는 A단계에서 수정하지 않았다. 새 작업에서도 기존 실패와 신규 실패를 구분한다.
- 2026-09-13 / `6ab107f8`: 기존 주문 잠금 전환 및 조직 ADMIN 전용 해제 방향 문서화. 당시 수동 완전 잠금/발행 즉시 잠금 제안은 아래 사용자 정정으로 대체한다.
- 2026-09-14 / 사용자 정정: 별도 청구서 메뉴와 분할 청구 필수. 누적 수량 검토로 해당 주문을 청구 완료 처리할 때만 주문·배정 완전 잠금. 수동 주문 잠금/부분 발행 즉시 잠금 계획을 폐기했다. **이번 변경은 문서만이며 다음 작업은 새 B단계다.**
- 2026-09-14 / B1 개발: `/invoices/orders`, `/invoices/order-source/:orderId`와 독립 INVOICE 권한 구현. 구 `/orders/:orderId/invoice-source`도 같은 INVOICE 권한을 요구한다. 정책 버전 11 미만은 제조사 ADMIN/ACCOUNTANT에 신규 메뉴를 추가하며 버전 11에서 명시적으로 제거한 권한은 보존한다. OPERATOR는 접근 권한에서 별도 허용할 수 있다.
- B1 검증: 프론트·백엔드 빌드 및 청구서/금액/API 더블/접근권한/주문/탭 이동 35건 통과. 실제 운영 API·브라우저 클릭 검증과 배포 확인은 미실행. 다음 작업은 B2 서버 초안 저장 및 C의 누적 원장·선금 정산 설계다.
- 2026-09-14 후속 조사: 주문 저장 기반 카드 갱신 방향 확정. 잠금 필터·드래그/보드 저장 가드·진행 상태·배정 ST 동기화·주문 항목 PK 재생성의 결합을 확인했다. 코드/DB는 수정하지 않았으며 B1.5를 B2보다 앞선 선행 작업으로 추가했다.
- 2026-09-14 B1.5a: 주문 수정의 전체 항목 삭제/재생성을 ID 기반 차등 저장으로 교체했다. 주문 갱신과 같은 트랜잭션에서 적용한다. 스키마 변경/운영 데이터 보정 없음. 다음 환경은 B1.5b부터 진행한다. 식별자 누락 신규 행은 새 행으로 간주하고 기존 중복 식별자는 임의 복구하지 않는다.
- 추천 확장(아직 개발 범위 아님): 입금/미수금·지급기한, 패킹리스트. 재고/BOM/매입/수익성 개발은 별도 기존 정책을 따른다.

다음 환경에 전달할 요청 예시:

> AGENTS.md와 이 문서의 진행 요약을 읽고 C 발행 원장·회차별 정산부터 진행해. B1.5a/B1.5b와 B2 서버 초안 저장·재개는 구현됐다. 검증 범위는 상단 최신 기록을 따르고 운영 DB/브라우저 검증과 최종 잠금 시점은 별도 미완료다. 최신 정산 정책과 기존 배정·CT/ST·실적을 보존하고 자동 청구 완료는 금지해. 완료 항목과 검증 결과를 갱신한 뒤 자동 커밋·푸시해.
