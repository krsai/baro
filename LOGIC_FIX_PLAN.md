# BARO 로직 점검 수정 계획

기준일: 2026-06-16  
목적: 최근 검토 요청에서 나온 이슈들 중, 현재 코드 기준으로 실제로 문제가 될 가능성이 큰 항목을 쉽게 설명하고 우선순위와 개선 방향을 정리한다.

## 한눈에 결론

1. 가장 먼저 손봐야 할 것은 인증/조직 식별 구조다.
2. 그 다음은 배정 보드 동시 저장 충돌이다.
3. 그 다음은 작업기록 저장 후 자동 완료/진행도 스냅샷 갱신의 경쟁 조건이다.
4. 진행률 계산식은 돌아가고 있지만, 분모 기준이 데이터 상태에 따라 흔들릴 여지가 있다.
5. 휴일 관리 이슈는 예전 보고서와 현재 코드가 일부 달라서, 구조 문제라기보다 재검증과 문서 정리가 먼저다.

---

## 1. 인증/조직 식별 헤더 신뢰 구조

관련 위치:
- `frontend/src/utils/apiClient.js`
- `backend/src/middleware/access.ts`

문제점:
- 프론트가 모든 요청에 `x-user-email`, `x-org-id`를 붙인다.
- 백엔드의 `getRequesterEmail`, `getRequestedOrgIdText`도 이 값을 직접 읽는다.
- 지금 확인된 코드만 보면, "이 헤더 값이 로그인한 실제 사용자 정보와 같은지"를 강하게 검증하는 구조가 눈에 띄지 않는다.

이 상황에서 생길 수 있는 오류:
- 브라우저 개발자 도구로 헤더를 바꾸면 다른 조직 데이터 조회/수정 시도가 가능해질 수 있다.
- 권한 체크가 사용자 토큰이 아니라 헤더 값에 기대면, 회계사나 일반 계정이 더 높은 권한처럼 보일 위험이 있다.
- 감사 로그의 `createdBy`, `updatedBy`도 잘못된 사용자로 기록될 수 있다.

개선 방식:
- 보호 API는 모두 서버에서 JWT를 검증하고, 사용자 이메일과 조직 정보는 토큰 claim에서만 꺼내 쓰도록 바꾼다.
- `x-user-email`, `x-org-id`는 인증 근거가 아니라 보조 정보로만 취급하거나 제거한다.
- 헤더 값과 토큰 정보가 다르면 바로 `401` 또는 `403`을 반환한다.
- `requireSystemAdmin` 같은 권한 판정도 검증된 사용자 컨텍스트만 기준으로 돌린다.

내 의견:
- 이건 단순 버그가 아니라 보안/구조 문제라서 최우선이다.
- 다른 기능 수정 전에 이 부분부터 정리해야 이후 권한 관련 버그를 덜 헷갈리게 추적할 수 있다.

---

## 2. 배정 보드 동시 저장 충돌

관련 위치:
- `frontend/src/pages/App/assign/AssignBoard.jsx`
- `backend/src/index.ts`

문제점:
- 배정 카드 재배치와 reflow의 핵심 판단이 프론트에 많이 있다.
- 서버 저장 경로에서는 버전 충돌 계산 흔적은 있지만, 실제 저장 단계에서 마지막 저장이 덮어쓰는 흐름이 남아 있다.
- 즉, 두 사용자가 동시에 같은 보드를 열고 수정하면 나중에 저장한 사람이 앞선 사람의 변경을 조용히 덮어쓸 수 있다.

이 상황에서 생길 수 있는 오류:
- A 사용자가 옮긴 카드가 B 사용자의 저장으로 사라진다.
- 저장은 성공했는데 배치가 다시 바뀌어서 "누가 잘못했는지" 찾기 어려워진다.
- 운영팀은 간헐적으로 "보드가 마음대로 바뀐다"라고 느끼게 된다.

개선 방식:
- 1차로는 보드 전체에 `version` 또는 `updatedAt` 기반 optimistic locking을 붙인다.
- 저장 시 버전이 다르면 `409 conflict`를 반환하고, 프론트는 재조회 후 다시 저장하게 만든다.
- 2차로는 서버가 최종 queue/reflow를 결정하는 canonical 구조로 옮기는 게 맞다.

내 의견:
- 당장 현실적인 해법은 서버 전체 재작성보다 optimistic locking부터 넣는 것이다.
- 이 단계만 해도 "조용히 덮어쓰기" 문제를 많이 줄일 수 있다.

---

## 3. 작업기록 저장 후 자동 완료/진행도 스냅샷 경쟁 조건

관련 위치:
- `backend/src/index.ts`
- `persistAssignmentPlanProgressSnapshot()`
- `trySyncAssignmentPlanSideEffectsAfterWorkLogMutation()`
- `runAutoAtSyncWithDbLock()`

문제점:
- WorkLog 저장, 수정, 삭제 이후 진행도/완료 상태를 다시 계산하는 흐름이 있다.
- 그런데 "현재 상태 읽기 -> 완료 여부 판단 -> 결과 저장" 전체 구간이 같은 잠금 단위로 묶여 있지 않다.
- 동시에 두 요청이 들어오면 서로 비슷한 예전 상태를 읽고 다른 결론을 저장할 수 있다.

이 상황에서 생길 수 있는 오류:
- 어떤 배정 카드가 완료됐다가 다시 미완료처럼 보일 수 있다.
- 급여 잠금과 연결된 완료 상태가 뒤엉켜서, 이미 마감된 달 데이터가 다시 열리는 것처럼 보일 수 있다.
- 사용자는 같은 데이터를 저장했는데 결과가 매번 다르게 보여 신뢰를 잃는다.

개선 방식:
- `persistAssignmentPlanProgressSnapshot()`의 읽기/판정/쓰기 전체를 하나의 트랜잭션과 DB lock 범위 안으로 넣는다.
- 이미 있는 `runAutoAtSyncWithDbLock()` 패턴처럼 advisory lock을 재사용하는 방식이 가장 현실적이다.
- 잠금 단위는 assignment별 또는 org/month별로 좁혀서 성능 부담을 줄인다.

내 의견:
- 운영 초기에 자주 터지지 않아도, 데이터가 쌓이고 사용자가 늘면 꼭 문제화될 가능성이 높다.
- 이런 종류는 증상이 랜덤해 보여서 늦게 고칠수록 추적 비용이 커진다.

---

## 4. 진행률 분모 기준 흔들림

관련 위치:
- `backend/src/index.ts`
- `buildAssignmentPlanProgressRows()`

문제점:
- 현재 진행률은 `sum(WorkRecord.quantity) / (planQuantity x processCount)` 방식이다.
- 그런데 `processCount`를 어떤 데이터에서 가져오느냐가 상황에 따라 달라진다.
- 스냅샷이 있으면 그 값을 쓰고, 없으면 작업기록 기준으로 fallback하는 구조라 데이터 상태에 따라 분모가 달라질 수 있다.

이 상황에서 생길 수 있는 오류:
- 같은 배정인데 어떤 시점에는 60%, 다른 시점에는 75%처럼 보일 수 있다.
- 운영자는 "실제 생산은 안 바뀌었는데 퍼센트가 왜 달라졌지?"라고 느끼게 된다.
- 보고서나 급여 보조 지표로 쓸 때 신뢰도가 떨어진다.

개선 방식:
- 진행률 분모의 소스오브트루스를 하나로 고정한다.
- 가장 무난한 방식은 assignment 생성 시점의 공정 수 스냅샷을 기준으로 삼는 것이다.
- 과거 데이터 중 스냅샷이 비어 있는 건 migration 또는 backfill로 보강하고, 예외 fallback은 로그를 남기도록 바꾼다.

내 의견:
- 치명도는 보안이나 동시성보다 낮지만, 숫자 화면은 한번 신뢰를 잃으면 회복이 어렵다.
- 운영 보고용 화면이 많아질수록 빨리 정의를 고정하는 편이 좋다.

---

## 5. 휴일 관리 이슈는 재검증 우선

관련 위치:
- `frontend/src/utils/holidayApi.js`
- `frontend/src/hooks/useHolidayCalendar.js`
- `backend/src/index.ts`

문제점:
- 예전 보고서에는 "휴일이 localStorage에만 저장된다"고 적혀 있었지만, 현재 코드는 `/holidays` API와 서버 저장 로직이 이미 들어와 있다.
- 다만 legacy localStorage fallback이 남아 있어서, 문서와 팀 인식이 현재 코드 상태를 못 따라온 흔적이 있다.

이 상황에서 생길 수 있는 오류:
- 실제 구조보다 더 심각한 문제로 오해하고 잘못된 대응 우선순위를 잡을 수 있다.
- 반대로, 서버 저장은 되지만 일부 계산 경로가 예전 가정대로 남아 있으면 특정 화면만 날짜가 다르게 보일 수 있다.

개선 방식:
- 휴일 저장/조회/스케줄 반영이 모두 같은 데이터셋을 쓰는지 end-to-end 테스트를 한 번 명확히 돌린다.
- `AGENTS.md`와 운영 문서의 휴일 설명도 현재 구조 기준으로 맞춘다.
- legacy fallback은 마이그레이션 완료 후 제거 시점을 따로 잡는다.

내 의견:
- 이건 지금 당장 "대형 구조 결함"으로 보긴 어렵다.
- 우선순위는 낮지만, 문서와 코드가 다르면 다음 점검 때 계속 혼선을 만든다.

---

## 추천 작업 순서

### 1단계
- 인증/조직 식별 구조를 JWT 기준으로 고정
- 권한 판정이 헤더 신뢰에 기대지 않도록 정리

### 2단계
- 배정 보드 저장에 optimistic locking 추가
- 충돌 시 `409` 반환과 재조회 UX 정리

### 3단계
- 진행도/완료 상태 스냅샷 계산에 advisory lock 또는 동일 수준 직렬화 적용

### 4단계
- 진행률 분모 기준을 고정하고 과거 데이터 backfill

### 5단계
- 휴일 관리 흐름 재검증
- 관련 문서 최신화

---

## 최종 의견

- 지금 가장 위험한 건 "사용자가 동시에 만졌을 때 조용히 데이터가 덮어써지는 문제"와 "서버가 요청 주체를 얼마나 믿을 수 있는가"다.
- 반대로 휴일 이슈는 보고서 기준과 현재 코드가 이미 조금 달라졌기 때문에, 구현보다 재확인과 문서 정리가 먼저다.
- 구현 리소스가 제한되어 있다면, 보안 구조와 동시성 제어부터 잡는 것이 가장 투자 대비 효과가 크다.
