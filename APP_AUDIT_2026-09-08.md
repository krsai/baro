# BARO 전체 앱 감사 보고서 (2026-09-08 최초 작성 → 2026-09-13 전면 재감사 → 2026-09-23 후속 개발)

> 진행 원칙(2026-09-08 사용자 결정, 유지): 항목별로 상의 후 수정하고, 검증을 마친 항목은 이 목록에서 삭제한다. 보안은 후순위로 둔다. 심각도·날짜 표현은 조사 당시 평가이며 실행 순서와는 구분한다. 완료된 변경의 계약은 `AGENTS.md`와 커밋 이력에 남긴다.

## 2026-09-23 후속 개발 상태

이번 회차에서 아래 항목은 해결되어 더 이상 남은 조치가 아니다. 상세 계약은 `AGENTS.md`의 같은 날짜 항목을 따른다.

- 조직 ADMIN의 구독 자가승급 차단, 관리 역할에서 WORKER 제외, 급여·수량정산·주문·배정·생산완료·QC·작업기록·거래처 mutation의 역할 게이트 보강
- 조직 접근의 프로세스 전역 60초 캐시 제거와 시스템 관리자 기본 조직의 실제 테넌트 범위 제한
- Employee·AttendanceEntry·WorkLog·AtTrainingBucket의 `(factoryId, orgId)` 복합 FK 및 시작 필수 스키마 검사
- AT 자체 관측의 assignmentPlanId 유효성 필터와 소량 경계 단일화
- 공용 VND/USD/KRW 포맷터 도입, 미저장 경고의 베트남어 톤부호, QC 저장 버튼 테마, 급여 오류 다국어, 공장 월간 배정 팔레트 통합
- `QcReview.jsx`와 `ProcessVersionManager.jsx`의 주요 사용자 문구를 한/영/베트남어로 분리하고, 직원·작업 목록의 검색/필터 헤더를 공용 sticky `PageToolbar`로 통일
- 직원 생성 세 경로에서 활성 기본 직급을 애플리케이션 레벨에서 명시적으로 조회하며, 기본 직급이 없으면 409로 중단해 DB 트리거에만 의존하지 않도록 보강
- `TENURE_YEARS` 정수 절사와 전역 컬러 저장의 배정 스냅샷 갱신 경로 문서화

전용 보안·통화 회귀, AT 34건, 관계 무결성 12건, 영향을 받은 주문/청구 회귀와 양쪽 빌드·번역·인코딩은 통과했다. 전체 회귀에서 `RELATIONSHIP_BUCKET_TEST_DATABASE_URL` 미설정 1건은 계속 미실행이며 운영 DB·브라우저 검증도 남아 있다. 아래 2026-09-22 상세 조사 문단 중 위 목록과 충돌하는 설명은 해결 전 근거 기록이며, 최종 남은 목록은 문서 하단 로드맵을 갱신해 관리한다.

## 현재 상태와 읽는 방법 (2026-09-22)

이 문서는 남은 감사 항목을 관리한다. 아래 보안·DB·UI의 상세 근거와 줄번호는 별도 표기가 없으면 2026-09-13 조사 당시 기준이며 최신 코드 전체를 다시 감사했다는 뜻이 아니다. 9월 14~21일 변경은 기준 커밋 `2c466d3d`까지 증분 확인했다.

2026-09-22 수정으로 해결한 회귀 실행 중단·누락 스위트·낡은 AT 기대값·레거시 Line 참조·수량 재검증 필드명 항목은 목록에서 제거했다. 변경 계약과 원인은 `AGENTS.md`의 2026-09-22 항목에 기록한다. 보안 후순위 결정은 유지한다.

## 0. 남은 검증 제한

- 로컬 전체 회귀는 33개 스위트를 끝까지 실행해 32개 통과했다. 테스트 assertion은 354개 통과·0개 실패였고, 이후 통합 픽스처의 레거시 참조 방지 테스트를 추가한 공장 스위트도 별도로 28/28건 통과했다.
- `test:relationship-bucket-integration`은 전용 `RELATIONSHIP_BUCKET_TEST_DATABASE_URL` 미설정으로 실행되지 않았다. 테스트 픽스처의 `db.line/lineId`는 `factoryId`로 수정했지만 실제 PostgreSQL 성공은 아직 확인하지 않았다. 새 CI는 일회용 PostgreSQL 16을 제공한다. CI 실행 결과 확인이 남아 있으며 미실행 검사를 성공으로 취급하지 않는다.
- 백엔드·프론트 빌드와 번역·인코딩 검사는 통과했다. 프론트 큰 청크 경고는 남아 있다. 브라우저 조작, 운영 DB의 Line 제거 적용 여부는 이번에 검증하지 않았다.
- 소스 문자열 검사 전부를 동작 검사로 전환한 것은 아니다. 이번에는 AT 계약, 급여 저장 경계·단가·근로기간, 생산 진행률 등 일부를 실제 함수 호출 검사로 보강했다.

---

## 1. 보안 취약점 재확인 — 전부 불변, 범위는 더 넓음

2026-09-08에 발견된 6개 항목을 코드 줄번호까지 다시 대조했다. **6개 전부 바이트 단위로 동일하게 남아있다.** 사용자가 명시적으로 후순위 처리를 결정한 항목이라 이번에도 코드를 고치지 않았지만, 재조사 과정에서 **원래 목록보다 범위가 넓다는 사실**이 추가로 드러났으므로 갱신해 기록한다.

### [CRITICAL] `PUT /organizations/:id` 구독 자가승급 — 불변
- `backend/src/organizations/organization.routes.ts:218-356`. `code`/`type`만 시스템 관리자 전용(264-269, 343-344)이고, 353행은 호출자 역할과 무관하게 `applySubscriptionPayload(organization, req.body ?? {})`를 실행한다. 조직 ADMIN이 `{subscriptionStatus:"ACTIVE", activeEndsAt:"2099-01-01"}`를 보내면 여전히 그대로 반영된다.

### [HIGH] `ORG_MANAGEMENT_ROLES === ORG_ACCESS_ROLES`(WORKER 포함) — 불변, 사용처는 26곳으로 확인(기존 "20개+"보다 구체화)
- `backend/src/index.ts:1013-1019`, 바이트 단위로 동일. 이번에 실제 호출부를 전수 재검색해 **26개**의 구체적 라우트를 확인했다. 기존 목록에 없던 것들: `PATCH /assignment-plans/:externalId/ct-review`, `POST/PUT/DELETE /customers`, `GET/PUT /customers/:id/sales-currencies`, `GET/PUT /process-master-options`, `POST /at-sync/reset`.

### [HIGH] 급여 스냅샷 저장/잠금/해제/삭제/단가수정 — 불변
- `backend/src/payroll/payroll.controller.ts:126-195`(줄번호만 소폭 이동), 5개 컨트롤러 전부 `requireOrgRole(req, res)`를 인자 없이 호출해 WORKER도 통과한다. 조회 계열(`GET /payroll` 등)은 역할 검사 자체가 없어 계좌번호까지 그대로 노출되는 것도 불변.

### [HIGH] 주문(WorkOrder) 생성/수정/잠금/삭제 — 불변
- `backend/src/index.ts`: `POST /orders`(30412), `PUT /orders/:orderId`(30455), `POST /orders/:orderId/modification-lock`(30570), `DELETE /orders/:orderId`(30710) 전부 동일.

### [MEDIUM] 수량 정산 저장 — 불변
- `backend/src/quantity-settlement/quantitySettlement.controller.ts:22-24`, 완전히 동일.

### [MEDIUM] 조직 접근 캐시 60초 미무효화 — 불변
- `backend/src/middleware/access.ts:35`(`ORG_ACCESS_CACHE_TTL_MS = 60_000`)와 캐시 맵(37행)에 대한 무효화 호출은 `access.ts` 바깥 어디에도 없음을 재확인.

### `requireOrgRole` 기본값 — 불변
- `backend/src/middleware/access.ts:519-528` — `allowedRoles = ORG_ACCESS_ROLES` 기본값 그대로. 인자를 생략하면 여전히 WORKER 포함 전체 통과.

### [신규 발견, HIGH] "역할 검사 자체가 아예 없는" 엔드포인트가 원래 알려진 6건보다 훨씬 넓다

이번 재조사에서 배정 보드·생산완료·작업기록의 핵심 mutation 엔드포인트 다수가 `requireOrgRole`을 아예 호출하지 않고 `getOrganizationByQuery(req)`(시스템 관리자이거나 "이 조직의 ACTIVE 직원이면 역할 무관 통과")만으로 게이트되어 있음을 확인했다. 즉 "allowedRoles 생략 → WORKER 통과"라는 원래 문제보다 한 단계 더 넓은, **"역할 게이트 자체가 아예 없는 것이 이 파일의 사실상 기본 태도"**에 가까운 패턴이다.
- `PUT /assignment-board-state`(index.ts:28212) — 배정 보드 저장 전체(라인/수량 이동, CT/ST 스냅샷 재생성 트리거). 역할 검사 없음.
- `DELETE /assignment-board-state/assignment/:assignmentId`(28078) — 개별 배정 취소. 역할 검사 없음(반면 같은 파일의 일괄 삭제 `DELETE /assignment-board-state`는 `ORG_MANAGEMENT_ROLES`를 쓰고 있어 같은 파일 안에서도 일관성이 없다).
- `PATCH /assignment-plans/:externalId/production-complete`(25088), `POST /assignment-plans/:externalId/close`(25034), `PATCH /assignment-plans/:externalId/final-quantity`(24935), `PATCH /assignment-plans/:externalId/reopen`(25328) — 생산완료 확정 4종 전부 역할 검사 없음.
- `POST /work-logs`(27194), `POST /work-logs/import`(26003), `PUT/DELETE /work-logs/:id`(27825) — 역할 검사 없음.

반대 사례(정상 패턴)로 `/employee-compensation-policies`, `/salary-system*`는 `requireSalarySystemManager`/`requireGradeManager`(`ROLE_ACCESS_POLICY` 기반, fail-closed)로 보호되어 있음을 확인했다 — 이 방식이 이미 이 코드베이스에 존재하는데, `index.ts`의 주문/배정/작업기록 대부분은 이 방식을 쓰지 않는다.

**권고**: 후순위 결정은 존중하되, 조치 시점에는 §0(원래 6건)뿐 아니라 이 26개+ 라우트 및 "역할 검사 자체가 없는" 배정/생산완료/작업기록 계열까지 함께 전수 재검토해야 실질적으로 의미 있는 수정이 된다.

---

## 2. DB 스키마 · 외래키 정합성 재확인

`schema.prisma`를 2026-09-08 이후 건드린 커밋은 단 1개(`6b09c4c4 refactor: remove legacy line domain and use factory ownership`, `+2/-64`, 순수 삭제 위주)뿐이었다. 10개 항목을 전부 재확인한 결과:

### 1~10번 항목 전부 — 불변 (line-removal 커밋과 무관한 영역)
- **[HIGH]** `Employee.factory`(477행)/`AttendanceEntry.factory`(726)/`WorkLog.factory`(1529)/`AtTrainingBucket.factory`(1111) 전부 여전히 단일 컬럼 FK. `(factoryId, orgId)` 복합 FK 미적용.
- **[HIGH]** `Employee.gradeId`(456행, `@default(dbgenerated())`)의 실제 기본값은 여전히 `migration_fix.sql`의 `BEFORE INSERT` 트리거(`baro_assign_default_employee_grade`, 4914-4925행 부근, 4899-4900행에서 부착)에만 존재. Prisma 레벨 기본값 없음.
- **[MEDIUM]** `Organization.representativeEmployeeId`/`Factory.managerEmployeeId` 여전히 조직 경계 미검증 단일 FK.
- **[MEDIUM]** `WorkOrder`의 `[orgId, customerId, orderNumber]`(customerId nullable) 및 `[buyerOrgId, sellerOrgId, orderNumber]`(둘 다 nullable) 유니크 제약, Postgres NULL 처리 특성상 완전한 유일성 미보장 — 후자는 이번 재조사에서 추가로 확인한 동일 부류다.
- **[MEDIUM]** `WorkOrderItem.style`(SetNull) vs `WorkRecord.style`(Restrict) `onDelete` 정책 불일치 — 불변.
- **[LOW]** `StyleProcess.atStFallbackSourceAssignmentPlanId`(1010행) 여전히 FK 없는 고아 컬럼.
- **[LOW]** `QcPassEvent.assignmentPlan`(1486행) 여전히 `onDelete: Cascade`.
- **[LOW]** `AtTrainingBucketProcess.style/styleProcess`(1142-1143행) 여전히 org 범위 검증 없는 단일 FK(`WorkRecord.styleProcess`는 3중 복합 FK를 쓰는 것과 대비).
- **[LOW]** `AssignmentPlan.assignmentCardId`(1444행) 여전히 `SetNull`(예고된 `Restrict` 승격 미시행).

### 라인 도메인 제거 — 코드/스키마 레벨은 이번에 "완료"로 재확인, 운영 DB 반영만 여전히 미확인
- `schema.prisma` 전체에서 `model Line`/`model LineAssignment`/`lineId` 어떤 흔적도 남아있지 않음을 grep으로 직접 재확인(`AssignmentPlan.lineId`+관계, `Employee.lineAssignments`/`managedLines`, `WorkRecord.lineId`, `OutsourcedWorkRecord.lineId`, `Factory.lines`, `Organization.lines` 전부 한 커밋에서 일괄 제거됨 — 절반만 지워진 흔적 없음).
- `AssignmentPlan.factoryId`가 이번에 `Int?`→`Int`(필수)로, `factory` 관계도 `Factory?`→`Factory`(필수)로 강화됐고, 이미 §1(원 감사)이 "올바르게 되어 있다"고 분류했던 것과 동일한 `(factoryId, orgId)` 복합 FK 패턴을 그대로 유지한다 — 퇴보 아님.
- `migration_fix.sql`에도 `Line`/`LineAssignment`를 되살리는 죽은 블록(2026-08-21 BusinessPartner 사고의 원인이었던 패턴)이 이번엔 전혀 없음을 확인 — 그 사고의 재발 방지가 실제로 지켜졌다.
- **다만 여전히 미확인**: 운영(Railway) DB에 이 스키마가 실제로 반영됐는지, `npm --prefix backend run audit:line-removal`의 실제 실행 결과는 이 개발 환경에 운영 DB 접속 정보가 없어 이번에도 확인하지 못했다. `audit-line-removal.js`/`audit-assignment-snapshot-integrity.js` 두 스크립트 모두 `SET TRANSACTION READ ONLY`를 직접 실행해 진짜 읽기 전용임을 코드로 확인했다 — 운영 확인 시 안전하게 실행 가능하다.

### AT 공통 사전 추정 기능 — 신규 스키마 표면 없음(확인됨)
- `atSharedPrior.ts`는 `schema.prisma`에 `SharedPrior` 관련 테이블/컬럼을 전혀 추가하지 않았다(AGENTS.md의 "저장 정규화에서 제거, 읽기 전용 응답" 주장과 일치) — grep으로 0건 확인.

---

## 3. 공정 연결 무결성 검증 참고

AGENTS.md 2026-09-09~09-10에 걸쳐 기록된 "공정 연결 무결성 방어" 관련 7개 커밋의 실제 동작을 코드로 재추적했다.

### 2026-09-13 확인된 방어 — 수량 필드명 결함은 수정 완료

- **[확인됨]** `PUT /styles/:styleId/process-version-boundaries`의 "저장 전 스타일 전체 사전 검사" 2단계가 실제로 모두 제거됐다(`index.ts:31755-31909` 전체에서 `assertAssignmentProcessRefs`/`hasValidAssignmentProcessRefs` 호출 0건, 제거 사유를 설명하는 주석이 31874-31881행에 남아있음).
- **[확인됨]** 완료/급여잠금 배정은 재계산에서 제외되고(`index.ts:31853`), 재계산은 배정 자신의 기존 스냅샷이 아니라 **확정 버전의 살아있는 공정 목록**(`version.processSnapshot`)을 기준으로 삼으며, 기존 값 재사용은 `styleProcessId` 정확 일치일 때만 허용된다(`buildEditableAssignmentCtSnapshotFromLiveStyle`, 13627-13826행).
- **[확인됨]** `reviewReason.code`가 `hasInvalidProcessReferences` 여부로 `PROCESS_REFERENCE_INVALID`/`PROCESS_QUANTITY_MISMATCH`로 정확히 분리되고(`index.ts:23864-23866`), 프론트 두 곳(`AssignBoard.jsx:7706-7721`, `FactoryMonthCapacityBoard.jsx:296-306`)이 이 값으로 실제 분기한다.
- **[확인됨]** 단일 버전 스타일의 저장 버튼 활성화 조건에 `needsSnapshotRefresh`가 정상 반영됐다(`ProcessVersionManager.jsx:112, 253`).
- **[확인됨]** REVIEW_REQUIRED 표/잔여 ST 계산이 배열 인덱스가 아니라 `style-process:${styleProcessId}` 키로 전 구간 일관되게 매칭된다(`resolveAssignmentPlanRequiredProcessGroups` → `calculateRemainingStTotalSecondsFromProcessProgress` → 표시용 `reviewProcessTotals`, index.ts 21041~23548행 전체 추적, 남아있는 배열 인덱스 매칭 없음).
- **[확인됨]** 손상된 스냅샷을 가진 배정이 자동/수동 어느 경로로도 몰래 `완료 확정`되거나 급여에 반영될 수 없다: 자동 완료 경로(`persistAssignmentPlanProgressSnapshot`, 24073-24234행)도 결국 같은 `scheduleStatus` 계산(23748-23756행, `!hasInvalidProcessReferences`가 아니면 `PRODUCTION_COMPLETED` 불가)을 공유하고, `updateMany`의 `where`에 `isCompleted: false`가 있어 이중 안전. `payroll.service.ts`는 애초에 `assignmentCtSnapshot`을 전혀 참조하지 않고 저장 시점에 얼린 `WorkRecord.ctSeconds`만 쓰므로, 배정 스냅샷이 나중에 손상돼도 이미 계산된 급여를 소급 오염시킬 수 없음을 코드로 확인.

---

## 4. AT 공통·카테고리 사전 추정 및 신뢰도 점수 — 신규 심층 감사

`backend/src/services/atSharedPrior.ts`(공통·카테고리 AT 사전 추정)와 `frontend/src/utils/processTime.js`(반복 생산 검증 기반 AT 근거 점수, 스타일 전체 AT 신뢰도)는 2026-09-10~11에 새로 도입된, 통계적으로 가장 복잡하고 금액/일정에 간접적으로 영향을 주는 코드다. AGENTS.md가 명시한 모든 수학적 계약을 코드로 직접 재도출해 대조했다.

### 검증 결과: 명시된 계약 대부분이 코드와 정확히 일치함 (좋은 소식)

다음은 전부 코드를 직접 손으로 재계산하며 확인한 것으로, 재조사가 불필요하다:
- "서로 다른 배정 수·수량 각각 2개 이상"(`atSharedPrior.ts:15`) — 두 `Set.size >= 2` 조건의 AND로 정확히 구현.
- "수량비 2 이상인 관측 쌍만 사용"(17-23행) — 정확히 구현, 0으로 나눌 위험 없음.
- 스타일별 중앙값 재집계로 공정 수 많은 스타일 과대 가중 방지(41-53행) — 확인, 대상 스타일 자기 자신은 사전 학습 후보에서 제외됨도 확인.
- `categoryWeight = count/(count+3)`(53행), `ownWeight = ownCount/(ownCount+5)`(61행, 공통 근거 없으면 자동으로 1) — 문서 그대로.
- 단일 수량 스타일의 절편 고정·기울기만 보정, 실패 시 `b=0`으로 폴백(64-70행) — 문서 그대로.
- 조직·생산단계·성별 범위 격리 및 학습 seed가 PT만 사용(38-40행) — 정확.
- 경계 m: 참고 스타일 최소관측수량의 스타일별→전체 2단계 중앙값, 100 하한, 자체 최소관측으로 하향(74-82행) — "타 스타일 소량 하나가 안전장치를 해제하지 않는다"까지 정확히 구현.
- 극소량 접선식 `a + (b/m)*(2 - q/m)`(processTime.js:612-634) — 미분으로 직접 재계산해 경계 연속성·단조성(장당 시간 비증가, 총시간 비감소)이 수학적으로 정확히 성립함을 확인.
- 구 "관측 최대치 4배 상한"·"최소관측 1/2 하한"이 코드에서 완전히 제거됐음을 확인(주석으로만 흔적 남음), 2026-09-10 정책 변경과 일치.
- 홀드아웃(leave-one-assignment-out) 검증: 배정별 그룹화, 3개 그룹 이상 요구, held-out 그룹을 제외한 나머지로만 재적합(누출 없음), 오차 `min(1, 상대오차)`의 동일가중 평균 — 문서와 정확히 일치, 저장된 `atParams`/`atSharedPrediction`을 검증 학습에 재사용하지 않음도 확인.
- 3/4/5건 상한 70/80/90(그 이상 95 클램프), 25%/75% 점수 블렌딩, 근거점수 상한 60(5점/건)·수량다양성 상한 20(5점/건)·관측범위 상한 5, 개별 수량 외삽 감점 `min(60, 15*log2(거리))`(스타일 전체 점수는 이 감점 없음) — 전부 문서에 명시된 상수와 정확히 일치함을 직접 대입으로 확인.
- 저장된 예측값(`atParams.a/b`, `atSharedPrediction`)이 근거/검증 점수 계산 어디에도 유출되지 않음, 중복 배정이 검증 건수를 부풀리지 못함(같은 `assignmentPlanId`는 그룹당 1건으로 합산) — 둘 다 확인.

### 발견된 낮은 심각도 이슈 2건

- **[LOW]** `processTime.js`(프론트, `resolveAtV2Points`)의 "자체 관측" 유효성 필터가 백엔드 `atSharedPrior.ts`의 `observations()`가 요구하는 `assignmentPlanId > 0` 조건을 검사하지 않는다. 정상 데이터에서는 문제되지 않지만, `assignmentPlanId`가 비정상적으로 비어있는 레코드가 있으면 프론트와 백엔드가 "자체 최소 관측 수량"을 서로 다르게 계산해 경계값이 어긋날 수 있다.
- **[LOW]** `atSharedPrior.ts:619-622`(프론트 소비 지점) 근처에서 백엔드가 이미 100 미만으로 낮춰 보낸 경계값을 프론트가 `Math.max(100, ...)`로 다시 100 이상으로 되돌렸다가, 바로 다음 줄에서 프론트가 독립적으로 재계산한 자체 최소 관측치로 다시 낮추는 이중 계산 구조가 있다. 현재는 두 계산이 우연히 서로 상쇄돼 결과가 맞지만, 한쪽만 수정되면 깨지기 쉬운 구조다.
- **[정보]** 적합 오차 허용치(`error <= 0.5`), PT 대비 속도 검사 범위(`[0.2, 5]`), `ownWeight < 0.8` 잠정 판정 임계값 등은 AGENTS.md에 정성적으로만("적합 오차와 PT 대비 속도 검사를 통과한 공정만") 기술되어 있어 정확한 숫자를 문서와 대조할 수 없었다 — 오류는 아니지만 향후 이 임계값을 조정할 때 근거를 남기기 위해 문서화를 권한다.

**결론**: 이 감사 대상 중 가장 복잡한 신규 로직임에도 실제 구현 품질은 높았다. 발견된 2건은 모두 낮은 심각도이며 현재 실제 오작동 사례를 재현하지 못했다.

---

## 5. 프론트엔드 일관성 · 디자인 재확인

2026-09-08의 14개 항목을 전부 재확인했다. **14개 전부 실질적으로 불변**이다(2개 항목에서 코드가 옮겨가며 표면적 수치만 바뀜).

1. `EmployeeBoard.jsx` 검색창이 `AppPageContainer`의 sticky 영역 밖(`Paper` 내부, 1871-1945행)에 있어 스크롤 시 사라짐 — 불변.
2. `WorkList.jsx`가 `PageToolbar`/`LastUpdaterLabel` 없이 손으로 3분할 레이아웃을 구현 — 불변. 현재 `PageToolbar`를 쓰는 16개 화면 목록에서 여전히 유일하게 빠져 있다.
3. `labelPalette.js` 사용 파일이 1→**2개**로 늘었으나(`PayTypeScheduleSettingsDialog.jsx`가 신규 사용), 이는 §"급여 타입별 근무 기준 재배치"로 `SalarySystem.jsx`의 기존 코드가 그대로 옮겨간 것일 뿐 — 실질적으로 여전히 죽은 표준.
4. `QcReview.jsx` 저장 버튼 `#0d6efd` 하드코딩 — 불변(1133-1134행).
5. `HolidayBoard.jsx`/`OrderList.jsx`/`OrganizationDetail.jsx` 각자 다른 팔레트 — 불변, **그리고 더 나빠짐**(아래 신규 발견 참고).
6. `QcReview.jsx`는 여전히 다국어 처리 0건(1,335줄 전체). `ProcessVersionManager.jsx`는 §3의 공정 연결 무결성 작업 중 안내 배너·칩 1개에만 `useLanguage`가 추가됐을 뿐(258줄 중 4곳) — 다이얼로그 제목·저장 버튼·빈 상태·토스트 등 나머지는 여전히 한국어 고정. 실질적으로 불변으로 판정.
7. `StyleInfo.jsx` 상단 절반(사진/기본정보) 하드코딩, 하단 절반(세부정보)만 정상 — 불변.
8. `PayrollBoard.jsx:516` "급여 계산 오류" 다이얼로그 제목 하드코딩 — 불변.
9. 베트남어 톤부호 누락(`uiMessages.js`의 `Luu`/`Huy`/`Dong` 등) — 불변. **게다가 이번 재조사에서 같은 결함을 가진 신규 파일 1건을 추가로 발견**(§6 참고).
10. VND 통화 포맷터 5곳 이상 독립 구현 — 불변, 통합 없음.
11. 퍼센트 소수점 규칙 불일치 — 불변(파일명만 `lineMonthCapacity.js`→`factoryMonthCapacity.js`로 개명).
12. `SaveButton` 로딩 스피너 미사용 5개 화면 — 불변(3곳 재확인).
13. 모바일 카드 뷰가 있는 화면 여전히 2개뿐 — 불변(8개 재확인, 전부 데스크톱 표만 유지).
14. `apiClient.js`의 `CACHE_INVALIDATION_MAP` 좁은 범위 — 불변(키 이름만 라인→공장 개명에 맞춰 갱신).

### [신규 발견] `FactoryMonthCapacityBoard.jsx` 한 파일 안에 서로 다른 하드코딩 팔레트 2벌이 공존
- `resolvePlanTone()`(90-118행)은 MUI 표준 색(`#90CAF9`, `#D32F2F`, `#ED6C02`, `#1976D2`)을, 같은 파일의 배정 카드 상태 색(308-321행)은 전혀 다른 Tailwind 계열 색(`#15803D`/`#B45309`/`#B91C1C`/`#2563EB`)을 쓴다. 둘 다 "상태를 색으로 표현한다"는 같은 개념인데 서로 재사용하지 않고, `labelPalette.js`도 쓰지 않는다. §5-5(팔레트 분산 문제)가 파일 간 문제였다면 이번엔 **파일 하나 안에서도** 재현됨을 확인했다.
- 추가로 `MainLayout.jsx:229`/`NotificationIndicator.jsx:14`의 `#d97706`, `WorkDetail.jsx`의 `#fff5f5`/`#9fb3c8` 등도 각자 국소적인 하드코딩 색을 쓰고 있어, 이 문제가 보고된 3개 파일보다 훨씬 광범위함을 확인했다.

### [신규 발견] `useUnsavedChanges.js`(새 공용 훅)에도 베트남어 톤부호 누락
- `frontend/src/hooks/useUnsavedChanges.js:10` — `vi: 'Ban co thay doi chua luu. Roi trang ma khong luu?'`(정확히는 `Bạn có thay đổi chưa lưu. Rời trang mà không lưu?`여야 함). 이 훅은 §"탭 단위 미저장 변경 통합"(2026-09-10~11) 작업으로 새로 만들어진 공용 인프라라, 기존 문제(9번)와 같은 결함이 새 코드에도 그대로 재생산됐다는 뜻이다 — "베트남어 번역 리뷰 없이 코드 작성"이 구조적 습관임을 시사한다.

**결론**: 프론트엔드 트랙은 지난 감사 이후 **실질적으로 아무것도 개선되지 않았고**, 팔레트·베트남어 문제는 새로 작성되는 코드에도 계속 재생산되고 있다.

---

## 6. AGENTS.md 문서-코드 드리프트

### 재확인: 기존 2건 모두 불변
- **[MEDIUM]** `refreshUnlinkedAssignmentPlanSnapshotsForOrg`가 전역 컬러 마스터 저장(`PUT` 컬러/카테고리/직무 통합 저장 라우트, `index.ts:32475-32489`)에서도 발동하는 경로 — 여전히 그대로이며, `rebuildAssignmentCardsForOrgIds(assignmentOrgIds)`가 시스템의 **모든** MANUFACTURER/BRAND 조직을 대상으로 호출되고 `refreshExistingAssignmentSnapshots`를 `false`로 넘기지 않아 스냅샷 재계산까지 함께 실행된다. AGENTS.md 어디에도 이 트리거 경로는 기록돼 있지 않다(재확인).
- **[MEDIUM]** `getSystemAdminDefaultOrganization`(access.ts:300-319)이 자매 함수 `getPrimaryOrganization`과 달리 여전히 `type: {in:['MANUFACTURER','BRAND']}` 필터가 없음 — 재확인, 불변.

### [신규 발견, MEDIUM] `TENURE_YEARS`(근속연수) 계산 방식이 연속 소수값→정수(만 나이) 절사로 바뀌었는데 AGENTS.md에 정책 결정으로 기록되지 않음
- 커밋 `56c84655`(2026-09-11)가 `backend/src/payroll/payroll.service.ts`의 `tenureYears` 계산에 `Math.floor(...)`를 추가했다. 그 결과 근속 11개월째 직원은 이제 `TENURE_YEARS=0`으로 계산되며, `GRADE_RATE × TENURE_YEARS` 같은 계산식을 쓰는 근속수당류 급여 항목의 실제 지급액이 달라진다. 이 저장소의 자체 원칙("새 결정이 확정되면 관련 문서를 같은 작업에서 갱신")에 따르면 급여 산정에 영향을 주는 정책 변경이므로 AGENTS.md에 날짜가 있는 결정 항목으로 남아야 하는데, 실제로는 `TENURE_YEARS`를 언급하는 기존 3곳(§급여 파라미터 설명) 어디에도 "정수 절사"라는 언급이 추가되지 않았다.
- 기존에 확정 잠금된 급여 스냅샷은 이 변경과 무관하다(스냅샷 불변 원칙에 따라 재계산되지 않음) — 계산 자체의 정합성 문제는 아니지만, **문서화 누락**은 이 저장소가 스스로 정한 규율 위반이다.
- **권고**: AGENTS.md에 "2026-09-11 근속연수 정수(만 년) 계산" 항목을 추가하고, 왜 절사가 맞는 선택인지(근속수당처럼 만 1년 단위로 끊어 지급하는 항목의 의도와 일치) 근거를 남긴다.

---

## 7. 이미 잘 해결되어 있음을 재확인한 것들

과잉 보고를 피하기 위해, 이번에도 검증했지만 문제없이 확인된 항목을 남긴다.

- 인증 신원 판별은 여전히 검증된 Bearer 토큰(JWT)에서만 온다. 헤더/쿼리 기반 신원 대입 경로 없음 — 재확인.
- 라인 도메인 제거는 스키마·코드 레벨에서 이번에 **완전 완료**로 재확인(§2). 남은 것은 운영 DB 반영 확인뿐이다.
- 공정 연결 무결성 방어의 핵심 계약 6/7건이 코드와 정확히 일치(§3).
- AT 공통·카테고리 사전 추정의 모든 핵심 수학적 계약(가중치 공식, 경계 연속성, 홀드아웃 검증, 반복 배정 부풀리기 방지, 저장값 비유출)이 코드와 정확히 일치(§4) — 이번 감사에서 가장 복잡한 신규 로직이 가장 깨끗했다는 점은 명시적으로 좋은 신호로 기록한다.
- 배정 CT 스냅샷 재계산 시 서로 다른 배정 간 수기 조정값이 새어 들어갈 수 있는 레이스 컨디션 여부를 별도로 검증했고, per-plan 순수 함수 구조라 유출 불가능함을 확인(§3).
- 주문 상세 스타일 검색 필터 수정(커밋 `04d20cb4`)은 실제 필드명(`styleCode`)과 `matchesAutocompleteSearch`의 동작을 직접 대조해 정확히 구현됐음을 확인.
- 백엔드(`tsc`)·프론트엔드(`vite build`) 빌드 모두 경고 없이 성공(프론트 일부 청크 500KB 초과 경고만 있음, 기능 문제 아님).

---

## 8. 우선순위 조치 로드맵 (갱신)

### 우선 확인

1. 새 CI의 PostgreSQL 통합 검사 실행 결과를 확인한다(§0). 실제 DB에서 관계별 버킷 격리·롤백·기존 배정 ST 보존을 검증해야 한다.
2. 안전한 운영 읽기 전용 감사로 Line 제거 마이그레이션 적용 여부를 확인한다(§2). 코드의 레거시 제거와 운영 DB 적용은 별개다.

### 보안 (사용자 요청으로 후순위, 유지)

2026-09-23에 열거된 취약 경로와 동일 패턴의 QC·수동완료·거래처 mutation까지 조치했다. 새 mutation은 명시적 역할 게이트를 사용하고 `requireOrgRole` 기본값도 WORKER를 제외한다.

### 단기
9. 배포 후 라인 제거 마이그레이션의 운영 DB 적용 확인(`audit:line-removal`) — 스키마 자체는 이번에 완료 확인됨(§2).
10. 베트남어 톤부호가 빠진 기존 화면 문자열을 전수 교정한다. 공용 미저장 경고와 이번에 다국어화한 QC·공정 버전 화면은 교정 완료했으며, 기존 전체 화면 전수 교정은 남아 있다.

### 중기
14. **[완료]** `QcReview.jsx`와 `ProcessVersionManager.jsx`의 주요 사용자 문구를 한/영/베트남어로 분리했다.
15. **[완료]** `EmployeeBoard.jsx` 검색·필터를 sticky 도구 영역으로 옮기고 `WorkList.jsx`의 헤더를 공용 `PageToolbar`로 통일했다. `FactoryMonthCapacityBoard.jsx` 이중 팔레트도 통합했다.
16. 모바일 카드 뷰를 8개 목록 화면으로 확대 적용.
17. **[완료]** 직원 생성 세 경로가 활성 기본 직급을 명시적으로 조회하고 없으면 409로 중단하도록 보강했다.

---

*해결된 항목은 AGENTS.md와 커밋 이력에서 추적한다. 정책 미확정·운영 검증·후순위 보안 항목은 임의로 완료 처리하지 않는다.*
