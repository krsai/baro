# BARO 전체 앱 감사 보고서 (2026-09-08)

> 진행 원칙(2026-09-08 사용자 결정): 항목별로 상의 후 수정하고, 검증을 마친 항목은 이 목록에서 삭제한다. 보안은 후순위로 둔다. 아래 심각도와 날짜 표현은 최초 감사 당시 평가이며 실행 순서와는 구분한다. 완료된 변경의 계약은 `AGENTS.md`와 커밋 이력에 남긴다.

이 문서는 코드 변경 없이 진행한 **읽기 전용 종합 감사**의 결과다. 스키마/외래키 정합성, 백엔드 로직·동시성 위험, 프론트엔드 일관성/디자인, 그리고 `AGENTS.md`가 스스로 기록한 정책과 실제 코드 사이의 드리프트, 네 영역을 독립적으로 조사한 뒤 종합했다.

**가장 먼저 볼 것**: §0(치명적 보안 취약점)은 오늘 중 조치를 권한다. 나머지는 심각도순으로 정리했다.

- 조사 대상: `backend/prisma/schema.prisma`(1,788줄 전문), `backend/src/**`(index.ts 및 도메인 라우터), `frontend/src/pages/App/**`, `AGENTS.md`(전문) 대조
- 방법: 4개 독립 조사 트랙(스키마, 백엔드 로직, 프론트, 문서-코드 드리프트)을 병렬 수행 후 교차 확인
- 원칙: 모든 항목은 실제로 파일을 열어 확인한 근거(파일:라인)만 기록. 추측/짐작은 배제하고 "코드 확인 필요"로 명시된 것만 미확정으로 남김
- 코드는 전혀 수정하지 않았다. 이 문서는 순수 리뷰 산출물이다.

---

## 목차

- [0. 치명적 보안 취약점 — 오늘 조치 권장](#0-치명적-보안-취약점--오늘-조치-권장)
- [1. 권한 제어 구조 문제 (앱 전반)](#1-권한-제어-구조-문제-앱-전반)
- [2. DB 스키마 · 외래키 정합성](#2-db-스키마--외래키-정합성)
- [3. 백엔드 로직 · 동시성 위험](#3-백엔드-로직--동시성-위험)
- [4. 프론트엔드 일관성 · 디자인](#4-프론트엔드-일관성--디자인)
- [5. AGENTS.md 문서-코드 드리프트](#5-agentsmd-문서-코드-드리프트)
- [6. 이미 잘 해결되어 있음을 확인한 것들](#6-이미-잘-해결되어-있음을-확인한-것들)
- [7. 우선순위 조치 로드맵](#7-우선순위-조치-로드맵)

---

## 0. 치명적 보안 취약점 — 오늘 조치 권장

### [CRITICAL] `PUT /organizations/:id` — 조직 ADMIN이 시스템 관리자 승인 없이 자기 조직 구독을 ACTIVE로 자가승급 가능

- **위치**: `backend/src/organizations/organization.routes.ts:218-269, 353` → `applySubscriptionPayload` (`backend/src/index.ts:1528` 이하)
- **문제**: 이 라우트는 시스템 관리자이거나 해당 조직의 ADMIN이면 통과한다. `code`/`type` 필드만 시스템 관리자 전용으로 막혀 있고(`:264-269`), 그 외 필드는 제한이 없다. `:353`에서 호출자 권한과 무관하게 무조건 `applySubscriptionPayload(organization, req.body ?? {})`를 실행하는데, 이 함수는 `payload.subscriptionStatus`, `trialEndsAt`, `activeEndsAt`을 그대로 받아 `OrganizationSubscription`을 갱신한다.
- **실제 공격 시나리오**: TRIAL/GRACE/SUSPENDED 상태인 고객 조직의 ADMIN 계정이 `PUT /organizations/:id`에 `{ name: "...", subscriptionStatus: "ACTIVE", activeEndsAt: "2099-01-01" }`를 보내면 결제 없이 영구 ACTIVE 상태가 된다. `PATCH /organizations/:id/subscription`은 `requireSystemAdmin`으로 정상 보호되어 있지만, 같은 효과를 내는 우회 경로가 별도로 열려 있다.
- **권고**: `PUT /organizations/:id`에서 `req.body`의 `subscriptionStatus`/`trialEndsAt`/`activeEndsAt`/`suspendedAt` 등 구독 관련 필드를 명시적으로 화이트리스트 제외(`code`/`type`과 동일하게 시스템 관리자 전용으로 승격)하거나, `applySubscriptionPayload` 호출 자체를 이 라우트에서 제거하고 `PATCH /organizations/:id/subscription` 하나로 일원화한다.

### [HIGH] `isBaroOrganization` 이름 기반 자동 ACTIVE 백도어 — `name` 필드가 자유롭게 변경 가능해 결합 악용 가능

- **위치**: `backend/src/middleware/access.ts:21, 52-62, 238-301`
- **문제**: 조직의 `name`이 대소문자 무관 "baro"이거나 `code`가 "BARO"이면 `isBaroOrganization`이 true가 되고, `ensureOrganizationSubscription`이 무조건 `status: ACTIVE`로 강제한다. 그런데 `organization.routes.ts:264-269`는 `code`/`type` 변경만 시스템 관리자로 제한하고 **`name`은 아무나(자기 조직 ADMIN) 바꿀 수 있다**(`:314`).
- **실제 공격 시나리오**: 일반 ADMIN이 조직명을 "Baro"로 바꾸기만 하면 다음 요청부터 자동으로 ACTIVE가 된다. 위 CRITICAL 항목과 별개로 독립 성립하는 하드코딩된 백도어다.
- **권고**: 이름 기반 특수 취급 로직을 제거하거나, 최소한 이 판정에 쓰이는 조직 식별자(`code`)를 시스템 관리자만 바꿀 수 있는 필드로 한정하고 `name`은 판정 기준에서 제외한다.

### [HIGH] `ORG_MANAGEMENT_ROLES`가 실제로는 "전체 역할 허용"과 동일 — 이름과 동작이 정반대

- **위치**: `backend/src/index.ts:1020-1027`
```ts
const ORG_ACCESS_ROLES: OrgUserRole[] = ["ADMIN", "OPERATOR", "ACCOUNTANT", "WORKER"];
const ORG_MANAGEMENT_ROLES: OrgUserRole[] = ORG_ACCESS_ROLES; // WORKER 포함!
```
- **문제**: "관리자 전용"으로 보이는 이름과 달리 WORKER까지 포함한 전체 역할 배열과 동일하다. 이 상수가 `PUT /holidays`, `GET/PUT /attendance-entries`, `DELETE /assignment-board-state`, 고객 판매단가 저장, 스타일 CRUD/대량 임포트, 공정 버전 확정, `POST /at-sync/run-now` 등 20개 이상의 엔드포인트에 `allowedRoles`로 쓰인다. 게다가 `POST/PUT/DELETE /orders`, `POST /orders/:orderId/modification-lock`은 `allowedRoles` 인자 자체를 생략해 `requireOrgRole`의 기본값(역시 WORKER 포함)이 적용된다.
- **비교 근거**: `payroll.controller.ts:18,24`의 급여 설정 API만 `allowedRoles: ["ADMIN","OPERATOR","ACCOUNTANT"]`를 직접 하드코딩해 WORKER를 제외하고 있다 — 즉 이 코드베이스에 "WORKER 제외" 패턴이 이미 존재하는데 `ORG_MANAGEMENT_ROLES`만 실수로 전체 허용으로 정의된 것으로 보인다.
- **실제 공격 시나리오**: 로그인 가능한 WORKER 계정(이메일이 선택 사항이라 실제로 이메일을 가진 WORKER 계정이 존재할 수 있음) 하나로 브라우저 devtools/curl을 이용해 UI 메뉴 숨김과 무관하게 휴일 삭제, 출퇴근 기록 임의 수정, 배정 보드 상태 삭제, 고객 판매단가 변경, 스타일 생성/수정/삭제, 주문 생성/삭제, AT 재학습 강제 실행이 전부 가능하다.
- **권고**: `ORG_MANAGEMENT_ROLES`를 `["ADMIN","OPERATOR","ACCOUNTANT"]`로 재정의하거나(이름이 뜻하는 대로), 이 상수를 쓰는 20개 이상 엔드포인트를 전수 재검토해 실제 필요한 최소 역할로 좁힌다. `requireOrgRole`의 기본값 자체도 "명시하지 않으면 관리자만 허용"으로 바꾸는 fail-closed 설계를 검토할 가치가 있다.

### [HIGH] 급여(payroll) 스냅샷 저장/잠금/해제/삭제/단가수정 API에 역할 제한이 전혀 없음

- **위치**: `backend/src/payroll/payroll.controller.ts:126-195`
- **문제**: `savePayrollSnapshotController`, `deletePayrollSnapshotController`, `updatePayrollEmployeeRatesController`, `unlockPayrollSnapshotController`, `lockPayrollSnapshotController` 전부 `requireOrgRole(req, res)`만 호출하고 `allowedRoles`를 넘기지 않아 WORKER도 통과한다. 게다가 조회 계열(`getPayrollController` 등)은 `requireOrgRole`조차 안 쓰고 "활성 조직 멤버이기만 하면" 통과시킨다.
- **실제 공격 시나리오**: WORKER 계정으로 회사 전체 급여 스냅샷을 잠금/해제/삭제하거나 특정 직원의 초당 단가(override)를 임의로 바꿀 수 있다. `GET /payroll?month=...&factoryId=...`로 전 직원의 급여명세·**계좌번호**(`bankName`/`bankAccountNumber`)까지 그대로 열람 가능하다.
- **권고**: 이 5개 컨트롤러와 조회 API 전체에 `allowedRoles: ["ADMIN","OPERATOR","ACCOUNTANT"]`(payroll.controller.ts의 급여 설정 API와 동일 패턴)를 명시적으로 적용한다. 계좌번호처럼 민감한 필드는 역할별로 마스킹하는 것도 고려한다.

### [HIGH] 주문(WorkOrder) 생성/수정/잠금/삭제 API에도 역할 제한 없음

- **위치**: `backend/src/index.ts:31748, 31791, 31906, 32046`
- **문제**: 위와 같은 뿌리(`requireOrgRole(req, res)` 단독 호출)의 문제. WORKER 계정이 주문을 생성/삭제하거나 `modification-lock`을 토글해 배정 카드/`AssignmentPlan` 재계산을 임의로 유발할 수 있다(§39-40이 문서화한 민감한 트리거).
- **권고**: `allowedRoles: ["ADMIN","OPERATOR"]` 수준으로 명시적 제한.

> 위 6건은 서로 다른 파일에서 발견됐지만 근본 원인은 하나로 수렴한다 — **이 백엔드는 "관리자 전용 API"라는 이름의 상수와 실제 동작이 여러 곳에서 어긋나 있고, 라우트 작성 시 `allowedRoles`를 생략하면 자동으로 안전한 기본값이 아니라 전체 허용으로 떨어지는 구조**다. 전수 재검토를 권한다.

---

## 1. 권한 제어 구조 문제 (앱 전반)

### [MEDIUM] 수량 정산(quantity-settlement) 저장에도 역할 제한 없음
- `backend/src/quantity-settlement/quantitySettlement.controller.ts:22-24` — 동일 패턴(`requireOrgRole(req, res)`만 호출). 현재 화면 노출도가 낮아 MEDIUM으로 분류했으나 위와 같은 방식으로 함께 정리할 것.

### [MEDIUM] 조직 접근 캐시가 60초간 퇴사/정지/역할변경을 반영하지 않음
- **위치**: `backend/src/middleware/access.ts:36, 474-516` (`ORG_ACCESS_CACHE_TTL_MS = 60_000`)
- **문제**: `employee.status === "ACTIVE"` 판정 결과가 최대 60초간 캐시된다. 관리자가 직원을 `TERMINATED`/`SUSPENDED`로 바꾸거나 역할을 강등해도, 직전 60초 내 접근 이력이 있으면 캐시가 만료될 때까지 이전 권한이 유지된다. 상태 변경 API에서 이 캐시를 무효화하는 호출을 찾지 못했다.
- **권고**: 퇴사/정지/역할변경 mutation 직후 해당 `(orgId, email)` 캐시 키를 명시적으로 무효화. 긴급 차단이 필요한 업무 특성상(급여 조작 가능성과 결합 시 더 심각) 최소한 정지/퇴사 처리 API에는 즉시 무효화를 붙여야 한다.

---

## 2. DB 스키마 · 외래키 정합성

### 높음

**[HIGH] `Factory` 참조 시 `(factoryId, orgId)` 복합 FK 적용이 모델별로 절반만 되어 있음**
- `Warehouse`, `FactoryProductionAllowanceRate`, `SalaryItem`, `SalaryItemRate`, `SalarySystemVersion`, `PayrollSnapshot`, `AssignmentPlan.factory` 7곳은 `Factory`의 `@@unique([id, orgId])`를 이용한 복합 FK로 조직 경계를 DB 레벨에서 강제한다. 반면 **`Employee.factory`(schema.prisma:509), `Line.factory`(459), `AttendanceEntry.factory`(764), `WorkLog.factory`(1585), `AtTrainingBucket.factory`(1163)는 단일 FK뿐**이다.
- 실패 시나리오: 코드 버그나 배치 스크립트 실수로 `Employee.orgId=A`인데 `factoryId`가 조직 B 소유인 행이 DB 제약 위반 없이 저장되면, 공장 재직 인원 기반 생산능력 계산·급여 출퇴근 집계가 다른 조직 데이터와 섞일 수 있다(멀티테넌시 유출).
- 권고: 5개 관계 모두 복합 FK로 전환.

**[HIGH] `AssignmentPlan` 내부에서 `factory`는 복합 FK, `line`은 단일 FK — 같은 모델 안의 불일치**
- `Line`에 `@@unique([id, orgId])`가 아예 없어(현재는 `@@unique([factoryId, name])`만 존재) 구조적으로 복합 FK 전환이 불가능한 상태. `WorkRecord.line`, `OutsourcedWorkRecord.line`, `Employee.line`도 동일하게 단일 FK. AGENTS.md는 "라인은 항상 단일 조직 소유"라고 명시하므로(브랜드 소유 `Style`처럼 교차 조직이 정상인 경우와 다름) 이는 설계 의도가 아니라 누락으로 보인다.
- 권고: `Line`에 `@@unique([id, orgId])`를 먼저 추가한 뒤 위 4개 관계를 복합 FK로 전환.

**[HIGH] `Employee.gradeId`(NOT NULL) 기본값이 Prisma가 아니라 순수 Postgres 트리거로만 존재**
- `schema.prisma:488`은 `@default(dbgenerated())`로만 표시되고, 실제 기본값 로직은 `migration_fix.sql:4760-4780`의 `BEFORE INSERT` 트리거다. 이 프로젝트는 `prisma db push` + `migration_fix.sql` 조합 배포라(`prisma migrate deploy` 미사용), `migration_fix.sql`이 실행되지 않는 환경(예: predeploy가 꺼진 배포 — AGENTS.md §43이 실제로 겪은 사고)에서는 `gradeId` 생략 시 NOT NULL 위반으로 직원 생성이 실패한다.
- 권고: 스키마 주석에 트리거 의존성 명시, 또는 애플리케이션 코드가 항상 명시적으로 `gradeId`를 채우도록 보강.

### 중간

- **`Line`에 `(id, orgId)` 복합 유니크 키 부재** — 위 HIGH 항목의 선행 조건.
- **`Organization.representativeEmployeeId`/`Factory.managerEmployeeId`/`Line.managerEmployeeId`가 조직 경계 검증 없는 단일 FK** — 계산에 직접 영향은 없지만 조직 간 오연결 가능.
- **`AssignmentPlan.factoryId`가 nullable** — `lineId`로부터 항상 유도 가능한 값인데 nullable이라 `factoryId IS NULL` 레거시 행이 공장 단위 집계에서 조용히 누락될 위험(코드 확인 필요).
- **`WorkOrder`의 `@@unique([orgId, customerId, orderNumber])` 등이 nullable 컬럼을 포함** — Postgres UNIQUE는 NULL을 서로 다르게 취급하므로 완전한 유일성 보장이 안 됨(현재 실제 null 대입 사례는 못 찾음, 리스크만 존재).
- **`WorkOrderItem.style`이 `onDelete: SetNull`인데 `WorkRecord.style`은 `onDelete: Restrict`** — "스타일 삭제 불가" 전제를 두 모델이 다르게 구현. Style DELETE 라우트 자체가 현재 없어 즉시 위험은 낮음.
- **급여 타입(`payType`) 등 canonical 값들이 여러 테이블에서 검증되지 않는 자유 문자열(enum 아님)** — 오탈자 값이 저장되면 급여 계산이 조용히 건너뛸 수 있음.

### 낮음
- `StyleProcess.atStFallbackSourceAssignmentPlanId`가 FK 없이 방치된 정수 컬럼(문서가 이미 레거시로 규정, 실사용 0 확인되면 컬럼 제거 권장).
- `QcPassEvent.assignmentPlan`이 `onDelete: Cascade`라 `AssignmentPlan` 삭제 시 QC 이력이 조용히 사라질 수 있음(다른 자식 테이블은 Restrict/SetNull).
- `AtTrainingBucketProcess.style/styleProcess`에 org 범위 검증 없음(진단용 데이터라 영향 제한적).
- `AssignmentPlan.assignmentCardId`가 §43이 예고한 "안정성 확인 후 Restrict로 강화" 후속 조치가 아직 반영되지 않은 채 SetNull로 남음.

### 검토 후 문제없음으로 확인
AGENTS.md §39~§46이 완료를 주장한 FK+Join 재설계, JSON 필드 신규 쓰기 중단, `OrgMembership` 완전 제거, `PayrollSnapshot`의 공장별 유니크, `Employee` 유니크 제약의 NULL 처리, `WorkRecord.style` 등 교차조직 FK 설계는 코드로 재검증한 결과 문서 주장과 실제로 일치함을 확인했다.

---

## 3. 백엔드 로직 · 동시성 위험

### AGENTS.md §37이 이미 지적했던 항목 재검증 결과
9개 항목 중 **8개는 실제로 해소됨**을 코드로 확인했다(완료 API의 완전한 낙관적 락 도입, processCode 문자열 fallback을 styleProcessId FK로 전환, 프론트 reflow의 급여 잠금 카드 anchor 처리, updatedAt 클라이언트 신뢰 제거, 드롭 핸들러/정렬 키/오늘 인덱스의 NaN 가드 3곳, 급여 breakdown 그룹핑 키의 FK 전환). 1개(`AssignBar.jsx`의 `getDurationDays`)는 컴포넌트 자체 NaN 방어는 여전히 없지만 상류 정규화 함수가 광범위하게 적용되어 실질 위험이 낮아짐(LOW로 하향).

### 신규 발견 (심각도별)

**[HIGH]** 위 §0/§1에서 다룬 권한 제어 문제 3건(ORG_MANAGEMENT_ROLES, payroll API, orders API)이 이 트랙에서도 독립적으로 발견됨.

**[LOW]** `AssignBar.jsx`의 `getDurationDays` NaN 방어 부재(defense-in-depth 차원), 고객 매출 버킷 버전 전환 시 리스트×가격 조합마다 개별 upsert하는 N+1 루프(저빈도 관리 작업이라 영향 제한적).

---

## 4. 프론트엔드 일관성 · 디자인

### 공용 컴포넌트 미채택 [높음]
- **`EmployeeBoard.jsx`**: 검색/필터가 `AppPageContainer`의 sticky `toolbar`가 아니라 본문 카드 안에 있어, 스크롤하면 검색창이 사라진다. 다른 목록 화면(`CustomerList.jsx`, `StyleBoard.jsx`, `FactoryBoard.jsx`, `WorkList.jsx`)은 전부 `toolbar` prop을 쓴다.
- **`WorkList.jsx`**: `PageToolbar`를 안 쓰고 `Box` 레이아웃을 손으로 재구현해 3분할 구조가 되고, `LastUpdaterLabel`(다른 화면엔 있는 "마지막 업데이트" 표시)이 빠져 있다.
- 그 외 `Employee.jsx`(시스템관리자 조직 선택기 여백 불일치), `BusinessPartners.jsx`(LastUpdaterLabel 누락), `InventoryBoard.jsx`(프로토타입이라 영향 낮음).

### 라벨/색상 불일치 [중간]
- 공용 `labelPalette.js`(저채도 5색)가 전체 앱에서 **`SalarySystem.jsx` 한 파일에서만** 쓰인다 — 사실상 죽은 표준.
- `QcReview.jsx`의 저장 버튼이 부트스트랩 블루(#0d6efd)로 하드코딩되어 테마/다크모드와 무관하게 항상 같은 색.
- `HolidayBoard.jsx`, `OrderList.jsx`, `OrganizationDetail.jsx`가 각자 별도의 유사하지만 다른 색상 팔레트를 재정의.

### 다국어(ko/en/vi) 커버리지 갭 [높음]
- **`QcReview.jsx`(1,333줄)와 `ProcessVersionManager.jsx`에 다국어 처리(`getUiMessage`/`useLanguage`)가 전무** — 언어를 바꿔도 이 두 화면은 한국어 그대로 나온다. 특히 QC 검수 관리는 실사용 핵심 화면이다.
- `StyleInfo.jsx`는 같은 파일 안에서 위쪽 절반(스타일 사진/기본정보)은 하드코딩, 아래쪽 절반(세부 정보)은 정상 다국어 처리 — 부분 로컬라이제이션 누락.
- `PayrollBoard.jsx`는 전체적으로 잘 되어 있으나 "급여 계산 오류" 다이얼로그(사용자가 가장 당황하기 쉬운 순간)만 하드코딩.
- **베트남어 번역 품질이 항목마다 들쭉날쭉**: `uiMessages.js`에서 `save→'Luu'`(정확히는 'Lưu'), `cancel→'Huy'`('Hủy'), `close→'Dong'`('Đóng') 등 성조 부호가 빠진 항목과 정확한 항목이 한 파일 안에 섞여 있다. `ShipmentReview.jsx`의 문장 안에서도 단어별로 성조 유무가 다르다 — 베트남어 사용자에게는 오탈자처럼 보일 수 있다.

### 통화/숫자 포맷 불일치 [높음]
- VND 통화 포맷터가 **최소 5곳에서 독립적으로 재구현**되어 표기 방식이 전부 다르다: `AssignBoard.jsx`("동"/"dong"), `PayrollEntry.jsx`(항상 "VND" 고정), `WorkDetail.jsx`(언어별 상이), `WorkList.jsx`(로케일 `en-US` 하드코딩 + fallback "동"), `RevenueAnalysis.jsx`(브라우저 로케일 의존 + "₫"). 같은 금액이 화면마다 "12,345 VND" / "12,345 dong" / "12,345 ₫"로 다르게 보인다. `constants/currencies.js`에 `currencySymbol()` 헬퍼가 이미 있는데도 통합 포맷터가 없다.
- 퍼센트 소수점 표기 규칙도 화면군마다 다름(`.toFixed(1)` 고정 vs "정수면 정수, 아니면 1자리" 규칙).

### 죽은 코드 [높음]
- **`ScheduleTimeline.jsx`(946줄), `AssignBar.jsx`(571줄), `StyleCard.jsx`(167줄) — 합계 약 1,684줄이 앱 어디서도 도달 불가능한 완전한 고아 코드**다. AGENTS.md는 "운영 UI 기본으로는 안 쓴다"고만 적었지만 실제로는 import하는 곳이 0건이다. 유지보수 시 실수로 이 파일을 수정/디버깅하며 시간을 낭비할 위험이 있으므로 삭제를 권한다.

### 접근성/UX [중간]
- `CustomerPricingBoard.jsx`, `AccessPolicyBoard.jsx`, `ProcessVersionManager.jsx` 등 5개 화면이 저장 시 `disabled` 가드(중복 제출 방지)는 정확하지만 공용 `SaveButton`의 로딩 스피너를 안 써서, 같은 "저장" 동작인데 화면마다 시각 피드백이 다르다.
- Dialog의 배경 클릭/Esc 닫힘은 전수 확인 결과 문제없음(의도된 예외만 존재).

### 반응형/모바일 불일치 [높음]
- 모바일 카드 뷰가 있는 화면은 `WorkList.jsx`/`WorkMonthlyBoard.jsx` **딱 2개**뿐이고, `PayrollBoard.jsx`/`CustomerList.jsx`/`StyleBoard.jsx`/`EmployeeBoard.jsx`/`OrderList.jsx`/`QcReview.jsx`/`AttendanceList.jsx`/`BusinessPartners.jsx` 등 8개 이상의 유사한 목록 화면은 데스크톱 테이블을 그대로 노출해 좁은 화면에서 가로 스크롤에 의존한다.

### API 캐시 무효화 [중간, 실영향 제한적]
- `apiClient.js`의 `CACHE_INVALIDATION_MAP`이 `/customers`, `/orders`, `/styles` 등 매핑은 되어 있지만 무효화 범위가 좁아, 관련 화면(`/assignment-cards`, `/customer-production-reports`, `/line-month-capacity` 등)이 최대 45초간 stale할 수 있다. 다만 주요 화면 다수가 `forceRefresh: true`로 이 캐시를 우회하고 있어 실사용 체감 영향은 제한적임을 확인했다.

---

## 5. AGENTS.md 문서-코드 드리프트

### [HIGH] "라인 개념 제거 전환" 과도기 호환 — 실제 파손 지점 특정
- `backend/src/lines/line.routes.ts:1097-1109`: 배정 보드 요약 조회가 **조직 내 공장 중 단 하나라도 호환 라인이 정확히 1개가 아니면 조직 전체 요청이 409로 중단**된다(다른 정상 공장까지 포함해서). 즉 향후 정상적인 사업 확장으로 공장 하나에 라인을 2개 추가하는 순간, 그 조직의 배정 보드 API 전체가 죽는다.
- `backend/src/index.ts:23070-23087`: 스케줄링 계산의 물리적 키는 여전히 `Line.id`이며, "배정의 실제 식별자는 공장"이라는 2026-09-03 문서의 전략 선언과 현재 구현 사이에 괴리가 있다. 문서 스스로 "완전 제거 아님"을 명시하고 있어 모순은 아니지만, 실패 파급 범위(공장당 라인 수 1이 아니면 전체 API 다운)는 문서에 구체적으로 적혀 있지 않다.
- 권고: 공장별로 개별 판정하도록(`factories.filter(...).forEach`) 수정해 한 공장의 이상이 다른 정상 공장까지 막지 않게 한다.

### [HIGH] `assignmentCtSnapshot.processes[].styleProcessId` — 한 번 손상되면 자가치유 안 되는 구조
- 정상 신규 저장 경로(`buildStyleProcessMirrorFromRows`)에서는 이제 null이 될 수 없음을 확인했으나, `refreshIncomingAssignmentCtSnapshotsFromStyles`가 `styleProcessVersionId`가 이미 설정된 배정에 대해 기존 DB 스냅샷을 무조건 보존하는 구조라서, **과거에 한 번이라도 null이 섞여 저장된 배정은 그 스타일 공정 버전을 명시적으로 재확정하지 않는 한 영구히 고쳐지지 않는다.** §52의 백필은 일회성 정리였을 뿐 재발 방지 장치가 아니다.
- 권고: 공정 버전 재확정 시 같은 버전을 쓰는 배정 전체를 일괄 재생성하거나, 저장 시점에 styleProcessId null을 발견하면 경고 로그를 남기는 모니터링을 추가.

### [MEDIUM] `refreshUnlinkedAssignmentPlanSnapshotsForOrg`가 문서 미기재 경로(전역 컬러 마스터 동기화)에서도 발동
- `backend/src/index.ts:33755-33763`: 색상 마스터 하나만 저장해도 시스템 전체 조직의 미연결 배정 CT 스냅샷 재계산이 실행된다. 실제 재계산 대상 조건 자체는 안전하지만, 이 트리거 경로는 AGENTS.md 어디에도 기록되어 있지 않다.

### [MEDIUM] `getSystemAdminDefaultOrganization`이 문서가 "추가했다"고 주장한 타입 필터를 실제로는 안 가짐
- `backend/src/middleware/access.ts:349-368`: 자매 함수 `getPrimaryOrganization`은 타입 필터가 있지만 이 함수는 없다. 2차 방어선(`attachOrganizationSubscription`)이 있어 보안 침해로 이어지지는 않지만, 벤더 조직이 시스템 관리자 기본 조직으로 잘못 선택되면 엉뚱한 403을 유발하는 기능 버그가 될 수 있다.

---

## 6. 이미 잘 해결되어 있음을 확인한 것들

과잉 보고를 피하기 위해, 검증했지만 문제없음으로 확인된 주요 항목도 남긴다.

- 인증 신원 판별은 오직 검증된 Bearer 토큰(JWT)에서만 오며, 헤더/쿼리 기반 신원 대입 경로는 없음.
- `ROLE_ACCESS_POLICY` 기반 권한 체크가 `EMPLOYEE` 기능 등 실제로 정책 저장값을 읽어 동작함(하드코딩 우회 아님).
- `SYSTEM_ADMIN_EMAIL` 하드코딩 폴백 없음, 환경변수 미설정 시 안전하게 실패.
- AGENTS.md §39~§46의 FK+Join 재설계, JSON 이중저장 제거, `OrgMembership→Employee` 통합 주장은 코드로 재검증한 결과 실제와 일치.
- `AssignmentPlan` 완료 확정 API의 낙관적 락, NaN 가드 3종, 급여 breakdown FK 전환 등 §37이 지적했던 8/9개 항목은 실제로 해소됨.
- Dialog 배경 클릭/Esc 닫힘 동작은 전수 확인 결과 정상.
- 급여 공제/특근/지각 정책은 "미확정"이라는 문서 주장과 코드가 정확히 일치(화면에 확정된 것처럼 잘못 노출되는 곳 없음).

---

## 7. 우선순위 조치 로드맵

### 즉시 (보안, 오늘~이번 주)
1. `PUT /organizations/:id`의 구독 필드 자가승급 취약점 차단 (§0)
2. `isBaroOrganization` 이름 기반 백도어 제거 또는 `name` 변경을 시스템 관리자 전용으로 제한 (§0)
3. `ORG_MANAGEMENT_ROLES` 재정의 및 이를 쓰는 20개+ 엔드포인트 역할 재검토 (§0)
4. 급여/주문/수량정산 API에 명시적 `allowedRoles` 부여 (§0, §1)

### 단기 (2~4주)
5. `Line`에 `(id, orgId)` 복합 유니크 추가 후 `AssignmentPlan.line`/`WorkRecord.line`/`Employee.line` 복합 FK 전환
6. `Employee`/`AttendanceEntry`/`WorkLog`/`AtTrainingBucket`의 Factory 참조 복합 FK 전환
8. 조직 접근 캐시 무효화 로직 추가(퇴사/정지/역할변경 mutation 직후)
9. 라인 개념 제거 전환의 "공장당 라인 1개 아니면 조직 전체 다운" 파손 지점 수정
10. 공용 통화 포맷터(`formatCurrency(amount, currencyCode, languageCode)`) 신설 후 5곳 통합

### 중기 (다음 분기)
11. 죽은 코드 3파일(`ScheduleTimeline.jsx`/`AssignBar.jsx`/`StyleCard.jsx`, ~1,684줄) 삭제
12. `QcReview.jsx`/`ProcessVersionManager.jsx` 다국어 처리 추가, 베트남어 성조 부호 전수 교정
13. `EmployeeBoard.jsx`/`WorkList.jsx` 등 공용 `AppPageContainer`/`PageToolbar` 패턴으로 정리, `labelPalette.js` 적용 확대
14. 모바일 카드 뷰를 8개 목록 화면으로 확대 적용
15. `Employee.gradeId` 트리거 의존성 문서화 및 애플리케이션 레벨 방어 보강
16. `assignmentCtSnapshot.styleProcessId` 재발 방지(버전 재확정 시 일괄 재생성) 및 모니터링 추가

---

*이 문서는 2026-09-08 시점 코드 스냅샷을 기준으로 작성됐다. 코드가 이후 변경되면 재검증이 필요하다.*
