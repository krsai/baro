import React from 'react';
import { Typography } from '@mui/material';
import AppPageContainer from '../../../components/AppPageContainer';

const ProductionResultBoard = () => {
  return (
    <AppPageContainer
      header={<Typography variant="h6">생산 결과</Typography>}
    />
  );
};

export default ProductionResultBoard;
