import React from 'react';
import AppPageContainer from '../../components/AppPageContainer';
import PayrollBoard from './payroll/PayrollBoard';

const Payroll = () => {
  return (
    <AppPageContainer>
      <PayrollBoard />
    </AppPageContainer>
  );
};

export default Payroll;
