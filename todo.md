# TODO

## 2026-07-25 매출·지급 버킷 동기화

- [ ] 고객 매출 버킷 추가·삭제와 연결 스타일의 지급 시간 버킷 변경을 하나의 트랜잭션으로 동기화한다.
- [ ] 버킷 변경 시 유지되는 수량 구간의 판매단가와 ST를 새 활성 버전으로 승계하고, 삭제된 구간만 제외한다.
- [ ] 신규 구간의 판매단가는 빈칸으로 두고, 신규 ST는 가장 가까운 하위 기존 버킷의 공정별 ST를 실제 행으로 복사해 `BUCKET_INHERITED_REVIEW` 및 빨간색 검토 대상으로 표시한다.
- [ ] 신규 구간의 AT는 기존 곡선을 해당 수량에 평가해 추정 가능할 때 표시한다.
- [ ] 과거 주문 판매단가 스냅샷, 배정 ST/CT 스냅샷, WorkRecord CT, 확정·잠금 급여가 버킷 변경으로 재계산되지 않는 회귀 테스트를 추가한다.
- [ ] 버킷 변경 고객·추가/삭제 구간·영향 스타일/공정·복사 원본/신규 ST·미입력 판매단가·변경자·변경시각을 FK 기반 변경 이력으로 저장한다.
- [ ] 대시보드에 버킷 변경 영향과 단가/ST 검토 필요 항목을 알리고, 검토 완료를 기록하는 알림 기능을 구현한다.

## 2026-07-25 관계형 정합성 마이너 정리

- [x] 주문 잠금 동기화에서 완료·급여잠금 배정도 스타일별 총수량과 FK 검증에 포함했다. 잠긴 배정을 제외한 나머지 한 건에 주문 전체 수량을 덮어쓰는 경로를 제거했다.
- [x] `AssignmentPlan.assignmentQuantity = null`을 0으로 해석하지 않고 409 정합성 오류로 드러내도록 했다.
- [x] WorkRecord 정규화 중 스타일 FK를 확정하지 못했을 때 요청의 `styleCode`/`styleName`을 표시·진단값으로 되살리던 우회를 제거했다.
- [x] `AssignmentPlan.updatedAt @updatedAt`과 중복되던 수동 갱신 및 문자열 `cardId`를 FK처럼 보이게 하던 진단 필드명을 정리했다.
- [ ] `AssignmentPlan.cardId` 전체 제거, WorkLog line 메타데이터 정규화, WorkRecord-Line 복합 FK는 별도 구조 변경으로 남긴다. 현재 변경에 섞어 임의 DROP하지 않는다.

## 2026-07-25 폴백·동시성 후속

- [x] 스케줄 화면에 오늘이 없을 때 첫 표시일을 오늘로 취급하던 `getTodayDayIndex → 0` fallback을 제거했다.
- [x] WorkRecord 저장 시 AssignmentPlan에서 Style FK를 확정하지 못하면 요청 `styleId`를 대신 쓰지 않고 저장을 거부한다.
- [x] split 배정의 합계와 변경 주문 수량이 다르면 조용히 건너뛰지 않고 409로 명시 조정을 요구한다.
- [x] `AssignmentPlan.updatedAt`을 Prisma `@updatedAt`으로 자동 관리한다.
- [ ] WorkLog 저장 후 진행률 snapshot 동기화 실패를 영속 기록하고 재시도하는 구조를 추가한다. 원본 WorkLog 저장 성공을 되돌리는 방식은 사용하지 않는다.
- [ ] `AssignmentPlan.cardId` 사용처를 외부 식별자/UI key와 관계 매칭 용도로 분류하고 후자만 `assignmentCardId` FK로 전환한다.
- [ ] `WorkRecord.lineId -> Line.id` FK는 이미 존재한다. 후속 검토에서는 제조사 `orgId` 복합 FK와 `onDelete: SetNull` 정책만 판단한다.

## 2026-07-25 FK·우회 경로 강화

- [x] 라인·공장 삭제가 연결된 `WorkRecord.assignmentPlanId`를 null로 만드는 경로를 제거하고 409로 차단했다.
- [x] `WorkRecord.assignmentPlanId/styleId/styleProcessId`를 필수 FK + `RESTRICT`로 변경했다.
- [x] WorkRecord의 assignment/styleProcess 연결에는 제조사 `orgId` 복합 FK를, 고객 소유일 수 있는 Style에는 단일 `styleId` FK를 적용했다.
- [x] 배정 카드가 없을 때 `cardId/originOrderId` 문자열을 파싱해 synthetic card를 만드는 병합 경로를 제거했다.
- [x] 서버 응답에 없는 월별 라인 생산능력을 프론트가 임의 계산하는 fallback을 제거했다.
- [x] `npm run verify:relational-integrity` 진단 명령을 추가했다.
- [x] Railway 운영 DB를 직접 진단해 `WorkRecord.orgId ↔ Style.orgId` 차이가 고객 Style을 제조사가 사용하는 정상 관계임을 확인했다. Style 조직을 덮어쓰지 않고 잘못 설계된 복합 FK를 단일 Style FK로 바로잡았다.
- [x] WorkRecord 저장 정규화의 Style 조회에서 제조사 `orgId` 필터와 요청 문자열 fallback을 제거하고, 교차 조직 Style의 `OrgRelationship` 진단을 추가했다.

## 2026-07-25 매출 단가 정밀 리뷰 후속

- [x] 단가 저장을 변경 셀만 전송하고 서버에서 일괄 검증·일괄 upsert하도록 변경했다.
- [x] 단가표/버킷 버전 복합 FK를 Prisma schema와 시작 시 constraint drift 검사에 명시했다.
- [x] 잠긴 레거시 주문의 단가 스냅샷 누락을 현재 단가로 복원하지 않고 응답 상태와 진단 API로 드러냈다.
- [x] 스타일 예외 버킷 숨김 안내, 로딩 중 입력 잠금, 셀 단위 숫자 검증, 미저장 변경 경고를 추가했다.
- [ ] Railway 운영 DB 배포 후 `GET /orders/sales-price-diagnostics`로 잠긴 주문의 누락 스냅샷을 확인하고 운영 정책에 따라 개별 처리한다.
- [ ] 실고객 대량 스타일 표에서 변경분 저장, 삭제, 주문 잠금 스냅샷을 직접 검증한다.

## 최우선: 고객·스타일별 가변 수량 버킷과 ST/AT 표시 기준 통합

상태: **구현 및 로컬 검증 완료 — 운영 DB 적용 및 실데이터 검증 대기**

### 2026-07-23 구현 결과

- [x] 고객 기본 버킷과 스타일 예외 버킷을 버전형 관계 테이블로 만들었다.
- [x] 단가 관리 화면에서 고객 기본/스타일 예외 버킷을 실제 저장할 수 있게 연결했다.
- [x] 스타일 ST/AT 화면은 해당 스타일에 연결된 버킷만 표시한다.
- [x] 새 버킷 ST는 조회 fallback이 아니라 `StyleProcessStandard` 실제 행으로 만들며, PT가 없으면 변경 전체를 거부한다.
- [x] AT는 버킷별 값을 복사하지 않고 각 수량에 기존 `AT(q)=a*q+b` 곡선을 계산한다.
- [x] 배정 생성 시 버킷 버전·수량·공정별 ST를 `assignmentStSnapshot`에 동결하고 과거 생산/잔여시간 계산은 이 스냅샷만 사용한다.
- [x] PT에서 새로 만든 ST가 CT의 근거가 된 배정은 CT 검토 전 급여 잠금을 거부한다.
- [x] 신규 테이블/컬럼을 Prisma, `migration_fix.sql`, 시작 시 schema drift 필수 목록에 함께 반영했다.
- [ ] Railway 운영 DB에 migration을 적용하고 고객/스타일/배정 백필 건수와 제약을 직접 검증한다.
- [ ] 실제 고객 1곳에서 기본 버킷 변경, 스타일 1개 예외 전환, 중간 버킷 추가 후 ST/AT/새 배정/과거 배정 불변을 확인한다.
- [ ] 고객 버킷 세트를 바꾸면 기존 버킷 값은 유지하고 신규 버킷은 가장 가까운 하위 ST를 `BUCKET_INHERITED_REVIEW`로 복사한다. 신규 ST를 사람이 다시 검토·조정할 수 있는 절차/화면을 추가한다.

### 2026-07-25 Claude 코드 리뷰로 발견·수정한 버그 (커밋 `629635d`)

- [x] `PUT /styles/:styleId` 저장 응답이 `timeBucketSetVersion`을 include 안 해서, 기존 스타일을 저장할 때마다 `timeBucketQuantities`가 빈 배열로 돌아오고 ST/AT 매트릭스 컬럼이 저장 직후 사라지던 버그 수정.
- [x] `POST /styles/import`가 신규 스타일에 `timeBucketSetVersionId`를 아예 안 붙이던 버그 수정(`POST /styles`와 같은 로직을 공용 함수로 추출해 재사용). 응답 include 누락도 같이 수정. 이 엔드포인트는 현재 프론트에서 호출하는 곳이 없어 실사용 영향은 없었음.
- [x] `단가 관리` 화면 상단 안내 문구/칩이 "전체가 UI 시안"처럼 보여서 버킷 저장(실제 동작)까지 미구현으로 오해하게 만들던 문제 — 문구를 "버킷 설정은 저장됨 / 가격 입력만 아직 시안"으로 범위 분리.
- 상세 내용은 `AGENTS.md`의 "2026-07-25 구현 검증 및 발견된 버그" 항목 참고. 이후 매출 단가 구현 검토에서 매출 버킷과 ST 시간 버킷이 스타일 예외 경로에서 결합된 구조적 문제가 추가로 확인됐고, 아래 매출 단가 구현에서 별도 관계로 분리했다.
- [x] 매출 버킷을 ST/AT 시간 버킷에서 분리하고 고객 기본/고객×스타일 예외 관계로 저장한다.
- [x] 관계형 매출 단가표와 Decimal 버킷별 단가, 조회·일괄 저장 API를 구현했다.
- [x] 단가 관리 화면의 CMT/FP·통화별 가격 조회/입력/저장을 실제 API에 연결했다.
- [x] 주문 헤더에 판매방식·통화를 저장하고 잠금 시 품목별 매출 단가 스냅샷을 원자적으로 동결한다.
- [ ] Railway 운영 DB에 `migration_fix.sql`을 적용하고 신규 테이블·복합 FK·CHECK 제약을 직접 확인한다.
- [ ] 실제 고객/스타일로 매출 버킷 예외, CMT/FP, USD/VND/KRW 단가 저장과 주문 잠금 스냅샷을 검증한다.
- [ ] 기존 미잠금 주문은 가격표가 준비되지 않으면 잠금이 409로 거부되므로 운영 전 필요한 고객·스타일·방식·통화 가격을 입력한다.

### 목표

- 고객의 매출 단가표와 스타일의 ST/AT 화면이 필요하면 같은 수량 구간을 명시적으로 선택할 수 있게 한다.
- 고객 기본 버킷은 예를 들어 `1·3·5·10...`, 특정 스타일 예외 버킷은 `1·2·5·7·10...`처럼 다르게 설정할 수 있게 한다.
- 운영 중 `2,000` 같은 수량 구간을 추가하거나 불필요한 구간을 제거할 수 있게 한다.
- AT는 저장 버킷을 만들지 않고 선택된 각 수량 `q`에 `AT(q) = a*q + b`를 계산해 표시한다.
- 새 ST 구간의 최초 값은 가장 가까운 하위 기존 버킷의 ST로 **실제 `StyleProcessStandard` 행을 생성**한다. 조회 시 PT나 인접 버킷을 임시 fallback으로 사용하는 방식은 금지한다.

### 코드에서 확인한 현재 상태

- `StyleProcessStandard`는 이미 `(styleProcessId, bucketQuantity)` 유니크 구조라 `2,000` 같은 임의 수량 저장 자체는 가능하다.
- 그러나 프론트와 백엔드의 `ST_STANDARD_BUCKETS` 및 `resolveStBucketQuantity` 호출부가 고정 버킷을 기준으로 동작한다.
- 공정 생성/저장 시 누락 ST를 고정 버킷 전체에 PT 값으로 채우는 코드도 현재 고정 상수를 사용한다.
- AT의 `{a,b}`는 수량과 독립된 곡선 파라미터이므로 버킷 구성 변경이나 중간 수량 추가에 별도 학습행 생성이 필요하지 않다.
- 과거 배정에는 ST/CT 총합 및 CT 스냅샷이 있지만, 실제 생산 ST·생산 분석 중에는 현재 수량을 현재 버킷으로 다시 판정하는 경로가 남아 있다. 버킷 경계 변경 시 과거 계산이 새 구간으로 재분류될 위험이 있다.

### 먼저 확정할 데이터 모델

- 수량 세트는 JSON 배열이 아니라 관계형 `QuantityBucketSet`과 `QuantityBucketEntry`로 만든다.
- 세트 소유 조직은 `orgId` FK로 연결하고, 수량은 entry 행으로 저장한다.
- 고객 기본값은 `OrgRelationship`과 버킷 세트의 명시적 FK 관계로 저장한다.
- 특정 고객·스타일 조합의 예외는 `orgRelationshipId + styleId + quantityBucketSetId` FK로 저장하고 유니크 제약을 둔다.
- 매출 단가표와 스타일 시간 기준은 같은 버킷 세트를 선택할 수 있지만 각각 명시적 FK를 저장한다. 한쪽 변경이 다른 쪽을 숨은 fallback으로 따라가게 만들지 않는다.
- 같은 스타일이 여러 고객에게 사용될 가능성을 확인한다. 가능하다면 `StyleProcessStandard`가 스타일 하나에만 묶인 현재 구조로는 고객별 서로 다른 ST를 표현할 수 없으므로, 고객별 ST가 정말 필요한지 먼저 정책을 확정한다.
- 세트 변경 이력 또는 버전을 관계형으로 보존한다. 이미 주문·배정·작업기록에 사용된 세트를 제자리에서 변경해 과거 구간 판정을 바꾸지 않는다.

### 운영 DB 사전 조사

- Railway `DATABASE_PUBLIC_URL`로만 조사한다. `backend/.env`의 Supabase DB 결과를 운영 데이터라고 판단하지 않는다.
- 모든 `StyleProcess`에 대해 PT 존재 여부, ST 버킷 개수, 버킷별 ST 값, `setBy`, 수동 수정 여부를 집계한다.
- 한 공정 안의 모든 ST가 PT와 같은 공정, 일부만 다른 공정, PT/ST 누락 공정을 각각 집계하고 샘플을 남긴다.
- 현재 고정 목록 밖의 `bucketQuantity`가 이미 존재하는지 확인한다.
- 고객 하나에 연결된 스타일인지, 한 스타일이 여러 고객/주문 당사자에게 사용된 사례가 있는지 확인한다.
- 과거 배정·작업기록·급여 잠금 데이터가 어느 수량 구간 판정에 의존하는지 표본이 아니라 전체 건수로 확인한다.
- 위 결과 없이 “모든 ST가 PT와 같다”고 가정해 일괄 변환하지 않는다.

### 구현 순서

1. `QuantityBucketSet`/`QuantityBucketEntry`와 고객 기본/고객×스타일 예외 FK를 Prisma와 `migration_fix.sql`에 추가한다.
2. 새 필수 컬럼을 시작 시 schema drift 검사 목록에도 함께 추가한다.
3. 현재 고정 세트를 조직별 기본 세트로 백필하고, 기존 스타일에 사용 중인 세트 버전을 명시적으로 연결한다.
4. 백엔드 공통 resolver를 `버킷 수량 배열을 인자로 받는 순수 함수`로 만든다. 전역 상수를 내부에서 읽지 않게 한다.
5. 백엔드·프론트의 모든 `ST_STANDARD_BUCKETS`/`resolveStBucketQuantity` 호출부를 분류해 가격, 스타일 편집, 배정 ST, 생산 분석, 작업기록, AT 표시가 올바른 세트 또는 저장된 스냅샷을 사용하게 바꾼다.
6. 단가 관리 UI에서 고객 기본 세트를 선택하고 특정 스타일만 별도 세트로 전환할 수 있게 한다.
7. 스타일 ST/AT 화면은 해당 스타일에 명시적으로 연결된 시간 기준 세트의 열을 표시한다. AT는 각 열의 수량으로 즉시 계산한다.
8. 새 수량을 추가할 때 트랜잭션 안에서 새 세트 버전과 entry를 만들고, 대상 스타일의 모든 공정에 `ST(newQ)=가장 가까운 하위 버킷 ST`, `setBy=BUCKET_INHERITED_REVIEW` 행을 생성한다.
9. PT가 없거나 0인 공정이 하나라도 있으면 새 ST를 임의 생성하지 않고 전체 변경을 409로 거부하며 해당 공정을 알려준다.
10. 기존 버킷의 수동 ST는 그대로 복사하고 새 버킷은 가장 가까운 하위 ST로 초기화한다. 단조 제약 조정은 사용자가 실제 ST를 편집할 때만 적용한다.
11. 과거 주문·배정·작업기록은 당시 버킷 세트 버전 또는 확정된 `bucketQuantity` 스냅샷으로 계산한다. 현재 세트로 재판정하지 않는다.
12. 새 세트 적용 이후 생성·재확정되는 주문과 배정부터 새 버킷을 사용한다. 급여 잠금 및 완료 확정 데이터는 재계산하지 않는다.

### 버킷 추가 정책

- 예: 기존 `1·3·5·...·1,000·3,000` 세트에 `2,000`을 추가한다.
- 새 주문수량 `2,500`은 새 버전에서 `2,000` 버킷을 선택한다.
- 각 공정의 `ST(2,000)`은 가장 가까운 하위 기존 버킷의 ST로 명시 저장하고 빨간색 검토 대상으로 표시한다.
- `AT(2,000)`은 곡선에서 계산하고 저장된 ST로 위장하지 않는다.
- 기존 `1,000`, `3,000`의 수동 ST 값은 변경하지 않는다.
- 변경 전에 확정된 주문·배정과 과거 생산 분석은 예전 버전의 `1,000` 판정을 유지한다.

### 버킷 삭제 정책

- 가격이나 ST가 입력된 entry를 즉시 물리 삭제하지 않는다.
- 새 버전에서 제외하는 방식으로 처리하고 이전 버전과 과거 스냅샷은 보존한다.
- 제외 후 새 주문에 필요한 가격/ST가 비어 있으면 다른 구간 값으로 대신하지 않고 미설정 오류를 표시한다.
- 마지막 버킷 삭제, 중복 수량, 0 이하 수량, 정렬되지 않은 입력은 DB 제약과 API 검증으로 거부한다.

### CT와 급여 보호

- AT는 공임 지급값이 아니며 CT를 자동 대체하지 않는다.
- 새 ST 구간을 추가했다고 과거 `WorkRecord.ctSeconds`나 `AssignmentPlan.assignmentCtSnapshot`을 수정하지 않는다.
- 새 버킷에서 CT를 어떻게 확정할지는 현재 CT 스냅샷 생성 경로를 별도로 조사해 정책을 확정한다.
- 유효한 CT를 만들 수 없으면 배정 저장을 거부하고 PT/ST를 CT로 조용히 대체하지 않는다.

### 필수 테스트

- 임의 수량 세트의 경계 판정: `1·3·5` 및 `1·2·5·7`, 정확한 경계값과 경계 사이 수량
- 고객 기본 세트와 특정 스타일 예외 세트가 서로 섞이지 않는 테스트
- `2,000` 추가 시 모든 공정에 PT 기반 ST 행이 정확히 한 번 생성되는 트랜잭션 테스트
- PT 누락 공정이 있을 때 부분 저장 없이 전체 롤백되는 테스트
- 기존 수동 ST 보존 및 새 버킷만 `BUCKET_INHERITED_REVIEW`가 되는 테스트
- 과거 배정/작업기록은 이전 세트, 새 배정은 새 세트를 사용하는 테스트
- AT가 임의 수량에서 같은 `{a,b}` 곡선으로 계산되는 테스트
- 버킷 삭제가 과거 데이터 물리 삭제나 급여/완료 데이터 재계산을 일으키지 않는 테스트
- 다고객·다스타일·다공장·다라인 환경에서 조직/고객 FK 범위가 섞이지 않는 통합 테스트
- 기존 고정 버킷 회귀 테스트 전체 통과

### 완료 기준

- 코드 전체의 핵심 ST 계산 경로가 전역 고정 버킷에 의존하지 않는다.
- 고객 기본 및 스타일 예외 버킷을 관계형 FK로 정확히 추적할 수 있다.
- 새 버킷은 가장 가까운 하위 ST 기반 실데이터가 생성되고 AT는 곡선값으로 표시된다.
- 버킷을 추가·제외해도 과거 배정, 실제 생산 ST, 급여, 완료 확정 결과가 바뀌지 않는다.
- Railway 운영 DB 검증 결과와 전환 건수, 예외 건수를 배포 기록으로 남긴다.

## 스타일 ST표 인쇄 화면 (신규)

- [ ] 생산관리 메뉴 그룹(현재 라인/배정/작업기록/출퇴근이 있는 `menu.production` 그룹)에 메뉴를 하나 추가한다.
- [ ] 화면 기능: 스타일 + 수량을 입력하면 그 스타일의 공정별 ST를 표로 인쇄한다.
- [ ] ST 값은 그 스타일에 연결된 버킷 세트에서 입력 수량이 판정되는 버킷 값을 쓴다. "고객·스타일별 가변 수량 버킷" 기능과 같은 resolver를 재사용하고 별도 계산 로직을 새로 만들지 않는다.
- [ ] 직원(현장)이 참고용으로 보는 화면이므로 인쇄 레이아웃(용지 크기, 공정 정렬 순서 등)을 확정한다.

## 재고·수익성 0단계 후속 확인

- [ ] Railway 운영 DB에서 `OrgRelationship.pricingDefaultTradeType/pricingMatrix` 값 존재 여부를 확인한다.
- [ ] 값이 있으면 신규 계산에 사용하지 않는 참고용 JSON/CSV로 내보낸다.
- [ ] 운영 확인 후 레거시 두 컬럼을 `migration_fix.sql`과 Prisma schema에서 제거한다.
- [ ] `단가 관리` UI 확정 후 관계형 가격 저장소와 연결한다.
- [x] 운영 관리 아래 `단가 관리` UI 시안과 고객 목록 연결 버튼을 만든다. 현재 입력값은 저장되지 않는다.
- [x] 단가표 헤더에 고객 기본/스타일 예외 매출 버킷의 선택·추가·삭제 UI 시안을 만든다. 현재 설정은 저장되지 않는다.
- [ ] UI 확정 후 관계형 고객 단가 테이블·API·주문 가격 스냅샷을 구현한다.
- [ ] UI 확정 후 매출 버킷 세트의 관계형 저장 구조를 설계한다.

## 최우선: AT 출퇴근 누락일 8시간 대체 검증

수정한 사항:
- 출퇴근 행이 없는 유효 근무일은 작업자당 8시간으로 대체하며 해당 작업자/공정을 학습에서 제외하지 않는다.
- 실측 행이 있는 날짜는 실측시간을 사용하고, 명시적인 0초 행은 결근으로 존중한다.
- `workedSeconds = null`인 불완전 출퇴근 행은 진단에 남기고 8시간 대체를 적용한다.
- 일요일, 조직 휴일, 입사 전, 퇴사 후, 휴직 기간은 8시간 대체에서 제외한다.
- 월 경계 coverage의 실측 출퇴근이 누락되지 않도록 조회 범위를 실제 coverage 최소일~최대일로 확장했다.
- `laborInputSeconds`와 `eventCount`가 같은 날짜별 실측/대체 판정을 사용하도록 맞췄다.
- 실제 출퇴근과 대체 노동시간/worker-day 진단을 추가하고 `attendanceCoverage` 하드코딩을 제거했다.

예상되는 문제와 부족한 점:
- 배포 후 AJ1972를 초기화·재학습해 모든 유효 공정에 최소 provisional AT가 생성되는지 확인해야 한다.
- `AtTrainingBucket`을 `WorkLog × worker` 단위로 바꿨다. 각 작업자의 노동시간은 그 작업자가 기록한 공정에만 배분되고 실제/대체 출퇴근 비율도 worker bucket별로 분리된다. 배포 migration은 기존 파생 bucket을 비운 뒤 재생성한다.
- 출퇴근 매칭에서 factoryId를 제거하고 조직 내 `workerId + workDate` 실측을 합산한다. 공장 이동 때문에 실측이 8시간 대체로 바뀌지 않는다.
- 작업자의 status/orgRole/현장 role 부적격을 각각 `WORKER_STATUS_NOT_ELIGIBLE`, `WORKER_ORG_ROLE_NOT_ELIGIBLE`, `WORKER_FIELD_ROLE_NOT_ELIGIBLE`로 진단한다.
- 과거 월의 WorkLog/출퇴근이 수정된 경우 전체 재생성 대신 변경된 월의 AT bucket만 다시 만드는 증분 갱신이 필요하다. 현재 일반 갱신은 대상월과 누락 bucket만 재생성하므로 과거 월 수정은 `AT 초기화` 후 재학습해야 정확히 반영된다.
- 같은 작업자의 같은 유효 근무일이 여러 WorkLog에 겹치면 노동시간을 어느 WorkLog에 귀속할 근거가 없으므로 관련 `WorkLog × worker` bucket을 모두 fail-closed한다. `ambiguousOverlappingWorkerDayCount`와 샘플로 원인을 노출하며 첫 WorkLog에 임의 귀속하지 않는다.
- 배포 후 `AtTrainingBucket.workerId` migration 적용과 worker-scoped 재생성 결과를 운영 DB에서 확인해야 한다.
- AT 초기화 버튼을 누른 뒤에는 관계형 `StyleProcess.atParams`, `AtTrainingBucket/AtTrainingBucketProcess`, 레거시 `Style.processes` JSON 안의 `atParams`/`at`가 모두 비워졌는지 확인해야 한다.
- 출퇴근 대체 비율이 높은 공정을 운영자가 화면에서 바로 구분하는 UI는 아직 없다. `/at-sync/run-now` diagnostics 또는 신뢰도 정보를 사용자 친화적으로 노출하는 작업이 필요하다.
- 운영 DB 확인 결과 AJ1979는 WorkLog 23에 정상 FK로 연결된 작업기록 22행이 있어 AT가 재생성되는 것이 맞다. 작업기록 응답의 평면 `styleName`이 재정규화 중 지워지던 문제를 수정했으므로, 배포 후 상세 검색에서 `1979`로 22행이 조회되는지 확인해야 한다.
- 누락 출퇴근을 사후 입력하면 8시간 대체 대신 실측값을 반영하도록 AT 초기화/갱신을 다시 돌려야 한다.

이 문서는 "지금 남은 일"만 적는다.
각 항목은 아래 3가지만 바로 보이게 쓴다.

- 무슨 문제가 남아 있는가
- 실제로 뭘 고치거나 확인해야 하는가
- 지금 기준으로 어떤 판단이 끝났는가

## 1. 먼저 고칠 후보

### [높음] 라인/공장 삭제가 작업기록을 고아로 만드는 문제

문제:
라인이나 공장을 삭제할 때, 연결된 `WorkRecord`가 `AssignmentPlan` 없이 남는 경로가 있다. 이렇게 되면 진행률, 정산, 추적이 틀어질 수 있다.

해야 할 일:
- [ ] `WorkRecord.assignmentPlanId = null`로 만든 뒤 `AssignmentPlan`을 삭제하는 경로 제거
- [ ] 연결된 작업기록이 있으면 라인/공장 삭제를 `409`로 막기

현재 판단:
- [ ] 삭제 편의보다 데이터 무결성을 우선
- [ ] "기록이 있으면 삭제 금지" 정책으로 가는 방향

### [중간] 보드 범위 밖 날짜를 첫날로 치는 fallback 제거

문제:
보드가 오늘 날짜를 포함하지 않는데도 `AssignBoard.jsx`의 `getTodayDayIndex`가 `0`을 반환하면, 시스템이 보드 첫날을 기준일처럼 오해할 수 있다. 이런 식의 fallback은 잘못된 자동 배치나 초기 위치 선정으로 이어질 수 있다.

해야 할 일:
- [ ] 보드 범위 밖이면 `0`으로 fallback하지 않기
- [ ] `null`, `-1`, 또는 "기준일 없음" 같은 명시적 상태로 처리하기

현재 판단:
- [ ] fallback으로 땜빵하지 않기
- [ ] 잘못된 자동 배치보다 "기준일 없음" 상태를 드러내는 쪽이 맞음

### [중간] 프론트가 없는 assignment card를 가짜로 만들어 숨기는 문제

문제:
프론트가 `cardId` / `originOrderId` 문자열을 파싱해서 synthetic assignment card를 만들면, 실제 데이터가 비정상이어도 화면에서 문제를 숨겨버린다.

해야 할 일:
- [ ] synthetic assignment card fallback 제거
- [ ] 실제 `AssignmentCard` FK row가 없으면 화면에서 이상 상태를 드러내기

현재 판단:
- [ ] 화면이 덜 매끈해져도 데이터 이상을 숨기지 않는 쪽이 맞음

### [중간] 작업기록 duplicate 검증과 수량 경고가 섞여 헷갈리는 문제

문제:
중복 입력 오류와 주문 수량 부족/초과 같은 운영 경고가 한 덩어리처럼 보이면, 사용자가 왜 막혔는지 판단하기 어렵다.

해야 할 일:
- [ ] 비중복 group-level validation이 계속 혼란을 주는지 확인
- [ ] 필요하면 group anchor 표시와 row-level 표시를 분리
- [ ] 주문 수량 부족/초과는 별도 경고 또는 가시화 경로로 분리
- [ ] 위 경고를 duplicate 검증과 섞지 않기

현재 판단:
- [ ] "입력 오류"와 "생산 편차"는 다른 문제로 보여줘야 함

## 2. 맨 마지막에 개발할 것

### [높음] assignment 저장 시 마지막 저장이 앞선 저장을 덮어쓰는 문제

문제:
두 사람이 거의 동시에 assignment를 저장하면, 나중 저장이 먼저 저장한 내용을 조용히 덮어쓸 수 있다.

해야 할 일:
- [ ] `PUT /assignment-board-state`에 `updatedAt` 또는 version 비교를 넣어서 optimistic locking 적용

현재 판단:
- [ ] 실제로 발생 가능한 문제로 인정
- [ ] 다만 다른 정합성/구조 정리를 먼저 끝내고 맨 마지막에 개발
- [ ] 저장 충돌 시에는 조용히 덮어쓰기보다 에러를 보여주고 다시 불러오게 하는 방향

### [중간] 완료 확정 카드의 급여 정산 후 운영 보드 제외

문제:
지금은 `완료 확정` 카드가 급여 정산이 끝난 뒤에도 운영 배정 보드에 계속 남을 수 있다. 이 카드는 운영상 더 이상 다룰 대상이 아니므로, 급여 기능이 붙으면 운영 보드에서는 빠져야 한다.

해야 할 일:
- [ ] 급여 기능 구현 시, 배정에 연결된 모든 `WorkRecord`의 월이 전부 정산/잠금됐는지 계산
- [ ] 그 조건을 만족한 `완료 확정` 카드를 `/assignment`, `/production-plan` 같은 운영 보드에서 제외
- [ ] 이력/급여/조회 화면에서는 계속 조회 가능하게 유지

현재 판단:
- [ ] 숨김 처리라기보다 "운영 보드 제외"로 보는 쪽이 맞음
- [ ] 단일 완료월이 아니라 연결된 모든 `WorkRecord` 월 기준으로 판단해야 함
- [ ] 급여 정산 전까지는 `완료 확정` 목록에 남아 있어도 됨

## 3. 배포 후 확인할 것

이 섹션은 "코드를 더 짜야 하는 일"이 아니라, 이미 반영된 변경이 운영에서 진짜로 맞게 보이는지 확인하는 체크리스트다.

### [검증] AT 표시와 새로고침 버튼 확인

확인할 것:
- [ ] 스타일 보드에서 AT 새로고침 버튼 재테스트
- [ ] AT가 rounded seconds로 보이는지 확인
- [ ] provisional / extrapolated 힌트가 의도대로 보이는지 확인

왜 보나:
AT 계산 로직과 표시 방식이 바뀌었기 때문에, 숫자 자체보다도 사용자가 "이 값이 확정치인지 임시치인지" 구분할 수 있어야 한다.

### [검증] `/assignment` 진행률/부하 계산이 과대 표시되지 않는지 확인

확인할 것:
- [ ] `/assignment`의 2026-07 LINE #1에서 planned load가 100% / 9월에 고정되지 않는지 확인
- [ ] `/assignment` 새로고침 후 authenticated progress API 기준으로 assignment 진행 상태가 정상 반영되는지 확인
- [ ] order unlock / relock 또는 assignment cancel / recreate 없이도 상태가 정상인지 확인
- [ ] LINE #1의 2026-10 backlog 과대 표시가 사라졌는지 확인
- [ ] ST-unknown / progress-unknown 배지가 다른 라인에서 과하게 뜨지 않는지 확인

왜 보나:
최근 수정은 "남은 ST"와 "진행률" 계산이 실제 작업기록을 제대로 반영하도록 바꾸는 작업이었다. 숫자가 다시 과장되면 수정 효과가 없는 것이다.

### [검증] capacity overlap 진단값이 실제 운영 데이터와 맞는지 확인

확인할 것:
- [ ] 운영 데이터에서 `capacityOverlapCount`를 직접 조회해 실제 겹침이 있는지 확인

왜 보나:
진단 카운트는 추가했지만, 운영 데이터에서 실제로 값이 나오는지는 아직 확인하지 못했다.

### [검증] 운영 환경 변수와 API 응답 상태 확인

확인할 것:
- [ ] Railway 백엔드 환경변수에 `SUPABASE_URL`, `SYSTEM_ADMIN_EMAIL`이 실제로 설정되어 있는지 확인
- [ ] 운영 배포 후 `GET /assignment-cards?orgId=1&includeProcesses=1`가 비어 있지 않은 `styles`를 반환하는지 확인

왜 보나:
코드가 맞아도 운영 환경 변수나 실제 API 응답이 비정상이면 화면은 여전히 깨질 수 있다.

### [검증] CT snapshot 관련 운영 반영 상태 확인

확인할 것:
- [ ] 운영 배포 후 `/assignment`에서 기존 CT `409`가 나던 카드들을 다시 드래그 저장해 재현이 사라졌는지 확인
- [ ] `migration_fix.sql`의 `AssignmentCard.payload` legacy key cleanup이 운영 DB에 실제 적용됐는지 확인

왜 보나:
이 항목은 코드 수정만으로 끝나지 않고, 운영 DB 상태까지 맞아야 진짜로 해결된다.
### [검증] 배정 상태 개편이 기존 데이터까지 반영됐는지 확인

확인할 것:
- [ ] `PATCH /assignment-plans/:externalId/production-complete`를 누르면 카드가 즉시 `완료 확정 목록`으로 이동하는지 확인
- [ ] 서버 재배포/재시작 후 예전에 수동으로 완료 확정했던 카드가 `작업 완료`에 남지 않고 `완료 확정 목록`으로 이동했는지 확인
- [ ] 자동 작업완료(`closedBy = system:auto-worklog`) 카드까지 같이 `완료 확정`으로 올라가지 않았는지 확인

왜 보나:
이번 수정은 새 저장 로직과 기존 수동 확정 데이터 백필을 함께 건드린다. 둘 중 하나라도 빠지면 화면이 다시 섞여 보인다.
### [검증] 검토 필요 수동 전환이 작업 완료까지만 가는지 확인

확인할 것:
- [ ] 공정 수량이 서로 안 맞는 카드(A=101, B=100, C=100 같은 케이스)는 `검토 필요`로 보이는지 확인
- [ ] `검토 필요` 카드에서 수동 확인을 누르면 `작업 완료`로만 바뀌고 `완료 확정`으로는 가지 않는지 확인
- [ ] 모든 required 공정 수량이 같은 값으로 끝난 카드(A=B=C=101 같은 케이스)는 `검토 필요`가 아니라 `작업 완료`로 보이는지 확인

왜 보나:
이번 정리는 "주문 수량 초과"가 아니라 "공정끼리 수량 불일치"를 검토 기준으로 삼고, 수동 조작도 `검토 필요 -> 작업 완료`까지만 허용하는 쪽으로 맞춘다.
