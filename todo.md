# TODO

## 2026-09-03 BARO_APP_ANALYSIS_2026-08-31.md 재검토 후속 (코덱스 검토 반영)

`BARO_APP_ANALYSIS_2026-08-31.md`의 완료 배정 삭제 레이스(1-2)와 `buildDateKey` invalid date 가드(3-3)는 코드 수정을 완료했다(원인·구현 메모는 해당 문서와 커밋 참고). 아래는 코덱스가 "문제의식은 맞지만 코드부터 바꾸면 위험하다"고 판단한, 조사·설계가 먼저 필요한 항목이다. 스키마·성능 변경은 이 조사 결과가 나온 뒤에 범위를 확정한다.

- [ ] `LineAssignment`에 `orgId`가 없고 기간 중복을 막는 제약도 없다(`schema.prisma:794-807`). 스키마를 바꾸기 전에 read-only 감사 쿼리로 교차 조직 연결·겹치는 기간 데이터가 실제로 있는지 먼저 확인한다.
- [ ] `Line.factory`/`Employee.factory·line`/`AttendanceEntry.factory·worker`/`WorkRecord.worker·line` 등 단일 컬럼 FK를 복합 FK로 통일하는 작업은 급여·근태와 직결된 `AttendanceEntry`/`WorkRecord`부터 교차 조직 데이터 감사 후 단계적으로 진행한다.
- [ ] `Employee.payType`, `EmployeeCompensationPolicy.payType`, `SalaryItemRate.payType`, `SalaryItem.category/payCycle`, `AssignmentPlan.scheduleStatus`의 String→enum 전환은 운영 DB distinct 값 감사·백필부터 하고, 급여 필드와 `scheduleStatus`는 서로 다른 도메인이므로 별도 마이그레이션으로 나눈다.
- [ ] `WorkOrder`의 `[buyerOrgId, sellerOrgId, orderNumber]` 유니크가 카운터파티 미정 상태의 중복을 막지 못하는 문제는, 현재 저장 경로(`resolveOrderPartiesOrThrow`)가 항상 양쪽을 확정한 뒤 저장하므로 실제 null 행이 존재하는지부터 확인한 뒤 대응한다.
- [ ] 배정 보드 저장(`createPlanRows`)과 주문 잠금 동기화(`syncAssignmentPlansForOrderLock`)의 스타일별 순차 `ensureInitialStyleProcessVersion` 호출이 실제로 30초 트랜잭션 타임아웃의 원인인지, 실제 스타일 수·쿼리 시간·타임아웃 로그를 먼저 측정한다.
- [ ] `createPlanRows` 경로의 ST 총합 인라인 재계산을 공용 헬퍼로 통합한다. 이 경로는 버전 스냅샷의 `applicableQuantity`를 쓰므로 기존 `calculateAssignmentStTotalSecondsFromStyleRows`를 그대로 재사용하지 말고 snapshot 전용 공용 헬퍼를 새로 만든다.
- [ ] `LineAssignment`/`AssignmentPlan.scheduleStatus`/`WorkOrder.status·confirmationStatus`에 인덱스를 추가하기 전에 실제 쿼리 플랜(`EXPLAIN ANALYZE`)으로 필요성을 확인한다.
- [ ] `PayrollBoard.jsx`의 백그라운드 새로고침 실패 시 stale 배지/최근 성공 시각 표시를 추가한다. 기존 값은 지우지 않고 유지한 채로 "오래됐을 수 있음"만 표시한다.
- [ ] CI에서 `npm run test:regression`과 프론트/백엔드 빌드가 실제로 PR/push 게이트로 걸려 있는지 확인한다. 저장소에는 `.github/workflows`가 없음을 확인했으므로, 없다면 별도 작업으로 추가한다.
- [ ] `AssignBoard.jsx`의 reflow/forecast/dateKey 같은 순수 계산 함수부터 export해 단위 테스트를 붙인다. "프론트 테스트 0개"가 아니라 "컴포넌트 단위 테스트 러너가 없고 현재 테스트 상당수는 소스 문자열 회귀 검사에 의존한다"가 정확한 현재 상태다.
- [ ] 크로스탭(다른 브라우저 탭·창) 실시간 동기화가 실제 제품 요구사항인지 먼저 확인한다. 필요하면 `BroadcastChannel` 도입 시 조직 전환·이벤트 중복·탭 종료까지 함께 설계한다.

## 2026-08-07 연계 페이지·열린 탭 갱신 지도

상태 표시는 `[x] 구현`, `[~] 일부 구현`, `[ ] 미구현`이다. 여기서 탭은 BARO 화면 상단의 keep-alive 업무 탭을 뜻하며, 별도 브라우저 탭·창은 따로 표시한다.

### 공통 탭 갱신 기반

- [~] 현재 공통 주제는 `STYLES`, `ORDERS`, `ASSIGNMENT_BOARD`, `EMPLOYEES` 네 개다. 라인 소속, 작업기록, 출퇴근, 공장, 휴일, 생산수당 등은 공통 주제가 없다.
- [ ] `window` 이벤트를 별도 브라우저 탭·창에도 전달하도록 `BroadcastChannel` 또는 동등한 수단을 추가한다. 현재 구현은 같은 브라우저 창 안의 BARO 업무 탭에만 적용된다.
- [ ] 모든 구독 화면이 저장 전 변경 여부를 `isBlocked`로 정확히 전달하는지 점검하고, 차단 중에는 탭 표시와 안내를 남긴 뒤 저장·취소 후 한 번만 다시 조회하도록 통합 테스트한다.

### 현재 구현된 연계

| 변경 원천 | 발행 주제/이벤트 | 현재 갱신되는 화면 | 상태와 한계 |
|---|---|---|---|
| 스타일 생성·수정·삭제 | `STYLES` | 스타일 목록, 주문 상세의 스타일 자료, 배정, 생산계획 | [x] 같은 창의 열린 업무 탭 갱신/표시. 배정·생산계획은 현재 들고 있는 스타일 ID와 관련된 변경만 선별한다. |
| 스타일 공정 변경의 배정 카드 적용 | `ASSIGNMENT_BOARD` | 배정, 생산계획, QC, 대시보드 | [x] 영향 카드에서 적용일·적용수량을 확정한 뒤 공정 리비전을 저장하고 같은 창의 연계 탭을 갱신/표시한다. |
| 주문 저장 | `ORDERS` | 주문 목록, 대시보드 | [~] 주문에 의해 생성·변경되는 배정 카드까지 모든 소비 화면이 직접 `ORDERS`를 구독하는 구조는 아니다. |
| 배정 저장·완료·CT 검토 | `ASSIGNMENT_BOARD` | 배정, 생산계획, QC, 대시보드 | [x] 같은 창의 열린 업무 탭 갱신/표시. 배정 화면은 저장 전 변경이 있으면 자동 덮어쓰지 않는다. |
| 작업기록 저장 | `ASSIGNMENT_BOARD` + `ORDERS` | 배정 진행률, 주문/대시보드 관련 조회 | [~] 진행률 연계는 있으나 작업기록 목록·AT·생산수당 준비 상태를 나타내는 전용 주제와 구독은 없다. |
| 직원 저장·승인 상태 변경 | `EMPLOYEES` + `baro:org-memberships-updated` | 라인 화면, 좌측 직원 승인 대기 건수, 봉제 라인 미배정 건수 | [~] 라인 화면은 갱신/표시하지만 배정·작업기록·생산수당 등 다른 연계 화면은 아직 연결하지 않았다. |
| 라인 배정·이력·라인 저장 | `baro:line-assignments-updated` | 좌측 봉제 라인 미배정 건수 | [~] 건수만 갱신한다. 배정·작업기록·생산수당 등 연계 화면의 열린 탭 표시는 하지 않는다. |

### 직원 → 라인 후속 업무

- [ ] `/line-assignment-history-candidates`와 `POST /line-assignments/history`도 `WORKER_SEWING`을 강제한다. 현재 두 경로는 `orgRole=WORKER`만 확인하므로 퇴사한 다림질·검수·포장 직원이 과거 라인 후보가 되거나 라인 이력이 저장될 수 있다.
- [~] 직원 저장 이벤트에 공통 `EMPLOYEES` 주제와 직원 ID를 발행한다. 공장 ID, 이전/이후 직무, 이전/이후 재직 상태까지 이벤트 상세에 추가하는 일은 남아 있다.
- [~] 신규 활성 봉제 직원 저장 성공 후 `라인 배정 필요` 확인창과 라인 메뉴 이동 버튼을 표시한다. 신규 등록은 완료했고 기존 직원을 비봉제→봉제로 변경하는 경우의 동일 안내는 남아 있다.
- [ ] 봉제→비봉제, 공장 변경, 퇴사 처리 때 기존 활성 라인 소속을 어떻게 종료할지 서버 트랜잭션 규칙을 확정한다. 조용히 과거 이력을 삭제하지 않고 유효 종료일을 남겨야 한다.
- [ ] 직원 저장 화면과 좌측 라인 메뉴가 사용하는 `라인 배정 필요` 판정을 하나의 서버 응답/요약 API로 통일하여 서로 다른 건수를 표시하지 않게 한다.

### 라인 변경의 연계 화면

- [ ] 공통 `LINE_ASSIGNMENTS` 주제를 추가하고 직원 ID, 이전/이후 라인 ID, 적용 시작·종료일, 공장 ID를 전달한다.
- [ ] 배정 화면은 라인 인원과 근로시간 총량/Capacity가 달라질 수 있으므로 `LINE_ASSIGNMENTS`를 구독한다. 저장 전 배정 편집이 있으면 자동 갱신하지 않고 탭 변경 표시를 남긴다.
- [ ] 작업기록 입력·업로드 화면은 작업일 당시의 라인 후보와 검증 결과가 달라질 수 있으므로 해당 기간과 겹치는 라인 변경만 갱신 대상으로 삼는다.
- [ ] 생산수당 목록은 월×공장×라인의 작업기록 준비 상태와 대상 인원이 달라질 수 있으므로 해당 월과 겹치는 라인/직원 변경을 반영한다. 잠긴 계산본 금액을 자동 재계산하거나 바꾸지는 않는다.
- [ ] 출퇴근 및 직원 이력 화면은 직원의 소속 라인 표시가 있는 경우 해당 직원/기간 변경을 반영한다.

### 다음 확장 시 등록할 변경 주제

- [ ] `WORK_RECORDS`: 작업기록 목록/상세, 배정 진행률, AT 동기화 상태, 생산수당 준비 상태.
- [ ] `ATTENDANCE`: 출퇴근 목록, AT 관측 신뢰도, 근무 현황. 생산수당 금액 재계산 사유로는 사용하지 않는다.
- [ ] `FACTORIES`: 직원 공장 선택, 라인, 배정, 작업기록, 공장 관리 시작일 및 생산수당 준비 상태.
- [ ] `HOLIDAYS`: 배정 일정/Capacity와 생산수당 근무일 준비 상태.
- [ ] `PRODUCTION_ALLOWANCE`: 생산수당 목록·상세의 생성, 라인 재계산, 잠금/해제, 직원별 단가 변경.

### 완료 판정 기준

- [ ] 각 저장 API/화면마다 `무엇이 변경됐는지 → 어떤 주제를 발행하는지 → 어떤 열린 화면이 영향을 받는지`가 표에 등록되어 있다.
- [ ] 활성·비활성·저장 전 변경 중인 BARO 업무 탭과 별도 브라우저 탭에서 각각 자동 갱신, 변경 표시, 입력 보존이 기대대로 동작한다.
- [ ] 직원 봉제 신규 등록, 봉제↔비봉제 변경, 공장 이동, 입사·퇴사, 라인 이동·과거 이력 수정 시나리오를 통합 테스트한다.
- [ ] 자동 갱신은 화면 전체 강제 reload가 아니라 필요한 서버 데이터를 다시 조회하고, 실패하면 변경 표시를 유지해 사용자가 다시 시도할 수 있게 한다.

> 앞으로 해야 할 일, 운영 검증, 보류된 미래 계획을 이 파일 하나에서 관리한다.
> 현재 강제 업무 규칙과 완료된 판단은 `AGENTS.md`, 완료 이력은 Git에서 확인한다.
> 재고 세부 규칙은 개발을 시작한 뒤 각 단계 구현 직전에 논의해 확정한다. 사용자의 재고 구현 시작 지시 전에는 코딩하지 않는다.

## 2026-08-07 라인 소속 이력 화면 개선 검토안 (미확정)

- 현재 드래그 중심의 `현재 배치` 화면은 당일 라인 구성 변경에 유지하고, 같은 메뉴 안에 별도 `소속 이력` 보기를 추가하는 방향을 사용자와 논의한다.
- 소속 이력 기본 후보는 직원별 한 행 표(`직원·재직상태·라인·시작일·종료일·기간·정합성`)이며 직원명/사번, 라인, 재직상태, 기준기간 필터와 현재 소속/퇴사자 포함 조건을 제공한다. 행을 펼치면 직원의 전체 라인 이동 타임라인과 이력 편집을 한곳에서 처리한다.
- 이력 공백, 입·퇴사 범위 초과, 동일 기간 복수 라인 겹침을 상태로 드러내고 해당 행에서 바로 보정할 수 있게 한다. 일괄 이동은 직원 여러 명과 적용일을 선택해 처리하되 기존 이력을 덮어쓰지 않고 이전 행 종료와 신규 행 생성을 한 작업으로 수행한다.
- Capacity의 소스오브트루스와 계산식은 계속 `LineAssignment` 기간 이력으로 유지한다. 화면 개선은 이력 가시성과 편집 동선만 바꾸며 `Employee.lineId`를 과거 이력 근거로 승격하지 않는다.

### 2026-08-06 생산 공정 작업 종류 후속

- [ ] 다림질·검품·포장 공정의 실제 기록·capacity·진행률·수당 활성화는 `AGENTS.md`의 "2026-09-03 docs/ 설계 문서 흡수 및 정리 > 생산 단계 확장(다림질 등) 설계안" 절에 정리된 확정 원칙과 미해결 현장 확인 사항, 그리고 라인 제거 계획과의 충돌 조율을 먼저 마친 뒤 진행한다.

### 2026-08-06 작업 종류별 AT 학습 후속

- [ ] WorkRecord에 저장 당시 작업 종류와 capacity group snapshot을 보존하고 AT 원천 조회가 공정의 현재값이 아닌 기록 snapshot을 사용하게 한다.
- [ ] 노동시간 풀의 `Organization × Worker × coverage × ProductionStage` 분리는 완료했다. `CapacityGroup` 도입과 단계 간 교차 배분 운영 검증을 추가한다.
- [ ] 다림질·검품·포장 실측 표본이 없어 `v4-stage-aware` 분리 배분을 봉제 외 데이터로 아직 검증하지 못했다. 비봉제 공정·작업기록이 실제로 등록되면 봉제-only v3 대비 결과와 다림질 실측 표본을 대조한다.

---

## 1. 급여 (2026-09-03 기준: 아래는 대부분 완료됨, 남은 것만 추림)

이 섹션은 원래 "생산수당 계산만 하고 전체 급여는 회사 정책 확정 후 진행"이라는 축소 범위로 적혀 있었다. 그 이후 실제로는 급여 타입 3종(GENERAL/OUTPUT_FIXED/OUTPUT), 급여 항목·직급별 단가, 공장별 완전 독립 계산, 급여 계산 설정(급여 타입별 만근 기준·상시 만근 직원·급여 예외 직원), 급여명세서까지 포함한 통합 급여 계산이 전부 구현됐다(자세한 내용과 결정 근거는 `AGENTS.md`의 2026-08-23 이후 급여 관련 항목들 참고). 예전에 여기 적혀 있던 "고정급/성과급 2종", "기본급+고정수당+변동수당+생산수당 4항목 고정" 같은 초기 설계는 이후 더 유연한 급여 항목 방식으로 대체됐으므로 별도 후속 작업으로 남기지 않는다.

- [ ] 배포된 운영 조직에서 실제로 급여 확정 해제 → 재계산 → 재잠금 흐름과 공장별 독립 계산이 기대대로 동작하는지 한 번 더 확인한다.
- [ ] 급여 체계나 급여 계산 설정이 비어 있는(직급별 단가 미등록 등) 테스트 월에서 계산 차단과 오류 메시지가 정확한지 확인한다.

---

## 2. 재고 개발 단계에서 논의할 정의와 계산 규칙

이 섹션은 재고 개발을 시작한 뒤 관련 단계 구현 직전에 사용자와 검토한다. 확정되지 않은 값에 임의 기본값을 넣지 않는다.

- 재고 관리 범위는 원재료와 부자재 전체다. 구체적인 품목 분류와 규격은 사용자가 제공할 현행 재고표를 기준으로 정한다.

### 2026-08-03 재고 논의 결정과 보류

- 2026-08-04 현행 `STORAGE STATUS.xlsx`를 완성 양식이 아니라 실제 자재 식별 사례로 검토했다. 자재 마스터는 `자재 대분류 → 자재 종류 → 자재 품목 → SKU`로 구성한다. 초기 대분류는 `원단류`, `부자재`, `포장재`이고 대분류와 종류는 모두 필수 선택 마스터다. 종류 목록은 선택한 대분류로 필터링한다. 품명·제조사·외부 고유번호는 수동 입력 선택값이며 품명을 모르는 경우와 폴리백처럼 종류 자체로 식별되는 경우에는 품명 NULL을 허용한다.
- 규격과 세부 구분은 자재 종류에 연결된 선택 옵션 마스터로 관리하고 각각 NULL을 허용한다. 일반 스냅·가시 스냅은 별도 자재 종류, 10mm·13mm는 규격, B·C·D는 세부 구분으로 본다. 색상은 기존 `AttrColor`를 선택 연결하되 NULL을 허용한다.
- 재고 단위는 별도 단위 마스터로 만들고 자재 품목에 필수 연결한다. 단위별 소수 허용 자릿수는 관리하지만 단위 간 자동 변환식은 만들지 않는다.
- 자재 검색기는 자재 종류, 품명, 제조사, 외부 고유번호, 규격, 세부 구분, 색상, 단위 등 모든 식별·표시 항목을 통합 키워드로 검색한다. 반복 사용 시 기존 품목/SKU 선택을 우선하고 동일 식별 조합의 중복 등록을 막는다.
- 자재 품목 활성 상태는 초기 구현 범위에서 제외한다. 이력이 있는 품목의 사용중지 정책이 필요해질 때 별도로 결정한다.
- 소수 허용 자릿수는 재고 단위 마스터의 기본값을 따른다. 품목별 override가 실제로 필요한지는 현행 데이터 확인 후 결정한다.
- 단위 변환은 수동 입력만 허용하는 것으로 확정한다. 자동 환산 기능이나 고정 환산 규칙 테이블을 만들지 않으며, 단위가 다른 수량은 사용자가 실제 측정·확인한 값을 입력한다. kg·m처럼 단위가 다른 재고는 별도 자재 품목으로 관리한다.
- `자재 대분류`는 원단 전체처럼 넓은 범위를 검색·집계하는 필수 선택 마스터이고 초기값은 원단류·부자재·포장재다. `자재 종류`는 각 대분류 아래의 겉감·안감·일반 스냅·가시 스냅·폴리백처럼 품목이 소속되는 필수 선택 마스터다. 종류 아래에 추가 계층은 두지 않는다. `자재 품목`은 종류에 FK로 연결되고 실제 입출고 재고는 품목의 색상·규격·세부 구분 조합인 정확한 SKU를 기준으로 계산한다.
- BOM은 자재 마스터·창고·입출고 원장 이후로 미룬다.
- 회사가 취득하는 입고 거래는 별도 유형과 화면 라벨 `구매`로 관리한다. 동작은 지정한 도착 창고의 회사 소유 재고를 증가시키는 것이며, 향후 구매 조회·집계와 발주 연결에 사용할 수 있게 문자열 메모가 아닌 거래 유형으로 저장한다. 구매 자체가 구매발주 문서를 뜻하지는 않는다. 공급처·단가·통화·증빙번호 범위는 입출고 원장 설계 때 확정한다.
- 거래 유형별 창고 방향을 구분한다. `구매`·고객 지급 입고·생산 반환은 `destinationWarehouseId`만, 납품·생산 투입·고객 반환·손실은 `sourceWarehouseId`만 필요하다. 창고 이동만 출발·도착 Warehouse FK를 모두 가지며 한 트랜잭션에서 양쪽 수량 변동을 기록한다. 실사 조정은 대상 창고 하나와 증감 방향을 기록한다.

### 재고 논의 재개 지점

재고 논의는 현재 일시 중단하고 급여 논의를 먼저 진행한다. 재개할 때는 아래 순서로 이어간다.

1. 사용자가 회사의 현행 재고표를 제공한다.
2. 확정한 `자재 대분류 → 자재 종류 → 자재 품목 → SKU → lot → 소유자 → 창고` 경계를 기준으로 나머지 현행 품목을 검증한다.
3. 화면 용어는 `자재 대분류`와 `자재 종류`를 사용한다. 대분류는 원단류·부자재·포장재로 시작하고, 종류는 각 대분류 아래에서 겉감·안감·일반 스냅·가시 스냅·폴리백 등을 평면 마스터로 관리한다.
4. 품목 규격·단위·lot 설정을 확정한 뒤 BOM 세부 규칙 논의로 넘어간다. 단위 변환은 수동 입력으로 확정했다.

이미 완료한 공장 하위 창고 관리를 제외한 재고 기능은 업무 규칙이 확정될 때까지 설계 논의와 문서 정리만 하며 구현하지 않는다.

### 확정한 판매방식과 자재 조달 구분

- 판매방식은 `CMT · 임가공`과 `FP · 완제품` 두 개만 사용한다.
- `MIX · 혼합`은 저장하거나 표시하지 않는다.
- CMT에서도 박스, 폴리백, 실 같은 일부 자재를 회사가 구매할 수 있다. 이것만으로 FP나 MIX가 되지 않는다.
- FP에서도 특정 라벨 등 일부 자재를 고객이 지급할 수 있다. 판매방식과 자재 조달 방식은 별개의 축이다.
- 자재 조달 방식은 BOM 자재 항목마다 `고객 지급`, `회사 구매`로 구분한다. 재고 소유자는 조달 출처와 별도로 정확한 조직을 기록하며, 별도 `위탁 보관` 분류가 필요한지는 보류한다.
- 고객 지급 자재는 수량을 관리하지만 회사 매입원가에 포함하지 않는다. 원가 0원으로 저장해 회사 구매 재고의 평균단가를 오염시키지 않는다.
- CMT 주문에서 회사가 구매한 포장재·소모품 비용은 회사의 실제 원가와 주문 수익성에 포함한다.

### BOM 사용량 표현 방식

BOM에는 단순한 “한 벌당 숫자”만 저장하지 않는다. 기준 생산량과 자재 사용량을 함께 기록한다.

- 자재 등록은 수량 계산의 기반이 되는 `재고 기준 단위`, `소수 허용 자릿수 또는 분할 가능 여부`, `기본 반올림 규칙`, `기본 소비 유형`을 필수로 갖는다. 소비 유형은 최소한 `생산수량당 사용량`, `자재 1단위당 포장 가능 생산수량`, `주문당 고정 사용량`을 표현할 수 있어야 한다.
- 자재 등록의 소비 유형과 값은 BOM 작성 시 제안값이다. 같은 가시 스냅도 스타일에 따라 한 벌당 8개 또는 10개를 쓸 수 있고, 같은 박스도 스타일·포장 조건에 따라 수용 수량이 달라질 수 있으므로 실제 적용값은 `BOM 버전의 자재 항목`에 저장한다.
- 예를 들어 가시 스냅 품목은 재고 단위 `EA`, 정수 관리, 기본 소비 유형 `생산수량당 사용량`으로 등록하고 스타일 BOM에서 `옷 1벌당 10 EA`를 확정한다. 박스 품목은 재고 단위 `BOX`, 정수·올림 관리, 기본 소비 유형 `1단위당 포장 가능 생산수량`으로 등록하고 BOM에서 `1 BOX당 옷 60벌`을 확정한다.
- 주문 필요량 계산 결과에는 적용한 자재 품목 FK, BOM 버전과 항목 FK, 기준 생산수량, 기준 자재 사용량, 손실률, 반올림 규칙을 추적할 수 있어야 한다. 현재 자재 마스터의 기본값을 다시 읽어 과거 확정 주문을 바꾸지 않는다.

| 자재 | 재고 기준 단위 | 사용 규칙 | 100장 필요량 | 수량 처리 |
|---|---|---|---:|---|
| 박스 | BOX | 옷 50장당 1 BOX | 2 BOX | 정수 올림 |
| 폴리백 | EA | 옷 1장당 1 EA | 100 EA | 정수 |
| 이밴드 | M | 옷 1장당 0.5 M | 50 M | 소수 허용 |
| 실 | CONE | 옷 1장당 0.1 CONE | 10 CONE | 소수 허용 |

기본 계산:

`기초 필요량 = 주문수량 × 기준 자재 사용량 ÷ 기준 생산수량`

`최종 필요량 = 명시된 손실률을 반영한 기초 필요량에 자재별 반올림 규칙 적용`

- 박스처럼 나눌 수 없는 자재는 올림한다. 51장이면 2 BOX다.
- m, kg, yard처럼 연속 수량을 관리하는 자재는 허용 자릿수까지 소수를 유지한다.
- cone은 현장에서 실제로 소수 잔량을 셀 수 있는 경우에만 소수 단위로 관리한다. 그렇지 않으면 더 정확한 기본 단위를 정한다.
- 손실률이 비어 있으면 시스템이 임의로 3%나 5%를 적용하지 않는다.
- BOM 계산에는 실제 주문수량을 사용한다. 수량 버킷 대표값으로 바꾸지 않는다.

확인할 일:

- [ ] 사용자가 현행 재고표와 BOM 조사 결과를 제공하면 실제 자재 종류·단위·사용량 사례를 기준으로 아래 규칙을 다시 검토한다.
- [ ] 박스 필요량을 전체 주문수량 기준으로 계산할지, 부분 출고 묶음마다 다시 계산할지 정한다.
- [ ] 자재별 소수 허용 자릿수와 `올림`, `반올림`, `절사`, `정확값 유지` 중 허용할 규칙을 정한다.
- [ ] 실을 cone 소수로 실사할 수 있는지, 아니면 g 또는 m 같은 단위가 필요한지 확인한다.
- [ ] 손실률 적용 후 반올림 순서와 자재별 예외가 있는지 현장 사례로 검증한다.

### 재고 단위와 구매 단위

- 재고 기준 단위는 BOM, 현재고, 투입량, 원가 계산에 사용하는 공통 단위다.
- 구매 단위는 향후 발주와 구매 문서에서 표시할 수 있지만 재고 기준 단위로 자동 환산하지 않는다.
- 예를 들어 단추를 BOX로 구매하고 재고를 EA로 관리한다면, 입고 담당자가 실제 확인한 EA 수량을 수동 입력한다.
- kg↔m, yard↔m를 포함해 시스템이 환산값을 계산하거나 제안하지 않는다.

확인할 일:

- [ ] 현행 재고표를 받아 원단과 부자재의 기준 단위 및 구매 문서 표시 단위를 정한다.
- [ ] 단추·스냅·지퍼·라벨·포장재의 구매 포장 단위와 재고 기준 단위를 정한다.
- [ ] 공급처별 포장 수량이 다를 때 구매 문서에 포장 수량과 실제 재고 기준 수량을 어떤 입력란으로 각각 받을지 정한다.

### 정확한 자재 품목

- 자재 대분류는 원단류·부자재·포장재를 우선 사용하고, 자재 종류는 각 대분류 아래의 겉감, 안감, 실, 일반 스냅, 가시 스냅, 단추, 지퍼, 라벨, 폴리백 등을 품목 등록 시 필수 선택하는 평면 마스터다.
- 재고 품목은 BOM, 발주, 입고, 재고를 연결하는 정확한 규격 단위다.
- 색상, 폭, 규격처럼 재고를 구분하는 값은 입고 메모에만 적지 않는다.
- 같은 재고 품목이라도 입고일, 공급처, 염색 배치, 단가가 다르면 별도 lot가 될 수 있다.
- 자재 코드는 시스템이 유일하게 자동 생성하고 사용자가 수정할 수 있게 한다. 관계 연결은 수정 가능한 코드가 아니라 불변 PK를 사용한다.
- 품목별 소수 허용 자릿수와 lot 관리 여부는 자재 마스터에서 설정한다.

확인할 일:

- [ ] 현행 재고표의 나머지 품목을 검토해 초기 자재 종류와 종류별 규격·세부 구분 선택값을 확정한다.
- [ ] 공급처 자재코드와 고객 지급 자재 품번을 별도로 관리할지 정한다.
- [ ] 동일 품목 판정에 포함할 필드와 단순 참고 필드를 구분한다.

### 소유권·lot·위치·원가

- 공급처와 소유자는 별개로 관리한다. 같은 창고에 있어도 회사 소유와 고객 등 제3자 소유 재고를 소유 조직으로 분리한다.
- 원단 염색 lot처럼 생산에서 섞으면 안 되는 구분은 lot로 관리한다.
- 위치의 운영 단위는 창고이며 한 공장에 여러 창고를 둔다. 공장 내부 선반·라인까지 세분하지 않는다.
- lot 원가가 원본이고, 보유 회사 소유 lot의 가중평균단가는 참고값이다.
- 예상 자재원가는 원가 출처를 표시하고, 실제 자재원가는 생산에 실제 투입한 lot 원가를 사용한다.
- 고객 지급 자재도 모든 수량 이동과 잔량을 추적하고, 수출입 서류 등을 근거로 직원이 입력한 참고단가를 보존한다.

확인할 일:

- [ ] 같은 자재의 고객 지급분과 회사 구매분을 생산 투입 시 어떻게 선택할지 정한다.
- [ ] 자재 마스터의 품목별 lot 관리 설정에 따라 실제 입고·투입 흐름을 어떻게 나눌지 정한다.
- [ ] 실제 생산 투입 시 현장에서 lot를 선택할 수 있는지 확인한다.
- [ ] 직원이 입력한 환율의 기준일·통화·근거 문서를 어떻게 보존할지 정한다.
- [ ] 실제 공동 운송 사례를 확인하고 운송비 배분이 필요한지와 배분 기준을 정한다.
- [ ] 운송비 배분 근거가 없을 때 `미배부 비용`으로 남기는 운영 절차를 정한다.
- [ ] 이동평균 참고값과 실제 lot 원가를 화면에서 어떻게 구분할지 정한다.

### BOM 버전과 주문 동결

- 반복 사용하는 기본 BOM은 스타일에 둔다.
- 특정 주문에서만 다른 자재를 쓰면 주문별 변경본을 만든다.
- 주문이 확정되면 적용한 BOM 버전을 동결해 나중의 기본 BOM 변경이 과거 주문을 바꾸지 않게 한다.
- BOM 항목은 자유 JSON의 품목 문자열이 아니라 정확한 자재 품목 FK를 사용한다.

확인할 일:

- 기본 BOM의 소유 단위는 스타일로 확정했다.
- [ ] 사용자의 BOM 조사 결과를 받은 뒤 나머지 규칙을 다시 논의한다.
- [ ] 주문별 색상·규격 변경을 BOM 변경본으로 관리할 범위를 정한다.
- [ ] BOM 버전을 생성하고 주문에 동결하는 시점을 정한다.
- [ ] 필수 자재, 선택 자재, 대체 가능 자재의 범위를 정한다.

---

## 3. 재고 개발 단계

아래는 설계 확정 후의 개발 순서다. 각 단계는 DB 관계와 계산식 확정, 구현, 자동 테스트, 운영 데이터 검증까지 끝난 뒤 다음 단계로 넘어간다.

### 3-1. 자재 마스터와 단위

- [ ] 자재 종류와 정확한 재고 품목/SKU를 관계형 테이블로 설계한다.
- [ ] 재고 기준 단위와 구매 문서 표시 단위를 설계한다. 품목별 자동 변환식은 만들지 않는다.
- [ ] 자재 등록에서 분할 가능 여부·소수 허용 자릿수·기본 반올림 규칙과 `생산수량당 사용량 / 자재 1단위당 포장 수량 / 주문당 고정 사용량` 기본 소비 유형을 필수로 관리한다.
- [ ] 자재의 기본 소비 설정은 BOM 입력 제안값일 뿐이며, 스타일·주문에 실제 적용되는 소비값은 버전이 있는 BOM 항목에 저장하도록 경계를 고정한다.
- [ ] 자재 규격, 공급처 코드, 고객 품번의 관계를 설계한다.
- [ ] 공급처(Supplier)는 새 테이블을 만들지 않고 `Organization`(`type=MATERIAL_SUPPLIER`, `거래처` 메뉴에서 이미 등록·조회 가능. 구 `BusinessPartner` 테이블은 2026-08-20에 `Organization`으로 흡수·삭제됐다)을 FK로 참조한다. `PROCESS_OUTSOURCING`이 `OutsourcedWorkRecord`로 타입별 확장 기능을 붙인 것과 같은 패턴으로, 자재/발주 테이블에서 `MATERIAL_SUPPLIER` 확장 기능을 붙인다.
- [ ] 회사 구매와 고객 지급 조달 방식 및 실제 소유 조직을 관계형 FK로 설계한다.
- [ ] 기존 InventoryBoard 목업은 화면 참고만 하고 mock 구조를 DB 구조로 복사하지 않는다.

완료 기준:

- BOM과 입출고가 같은 자재 품목과 단위를 참조할 수 있다.
- 시스템 자동 단위 변환이 없으며 다른 단위 수량은 실제 확인값만 수동 입력할 수 있다.
- 같은 품목의 소유자와 위치가 정확히 분리된다.
- 가시 스냅처럼 벌당 소비하는 품목과 박스처럼 포장 수용량으로 소비하는 품목을 자재 등록에서 구분하고 BOM 작성 시 올바른 계산 방식을 제안할 수 있다.

### 3-2. BOM과 주문별 필요량

- [ ] 스타일 기본 BOM, 주문별 변경본, BOM 버전과 동결 관계를 설계한다.
- [ ] 기준 생산수량, 기준 자재 사용량, 손실률, 반올림 규칙을 BOM 항목에 저장한다.
- [ ] 주문 실제 수량으로 자재별 필요량을 계산한다.
- [ ] CMT/FP와 무관하게 BOM 항목별 고객 지급·회사 구매를 저장한다.
- [ ] 자재나 계산 규칙이 누락되면 임의 값으로 대체하지 않고 미설정 상태를 표시한다.

완료 기준:

- 주문수량을 넣으면 박스, 폴리백, 길이 자재, cone 자재가 각각 올바른 규칙으로 계산된다.
- 주문 확정 후 과거 BOM 필요량이 바뀌지 않는다.

### 3-3. 입출고 원장과 현재고

- [ ] 구매, 고객 지급 입고, 위치 이동, 납품, 생산 투입, 생산 반환, 고객 반환, 폐기·손실, 실사 조정을 원장 유형으로 설계하고 유형별 출발·도착 창고 필수 조건을 강제한다.
- [ ] 현재고 숫자를 직접 덮어쓰지 않고 모든 확정 입출고 합계로 계산한다.
- [ ] 위치·소유자·품목·lot별 현재고를 조회한다.
- [ ] 음수 재고는 허용하되 관리자 경고와 원인·해결 상태를 영속적으로 추적한다.
- [ ] 취소와 정정은 원본 삭제가 아니라 반대 원장 또는 명시적 정정 이력으로 남긴다.
- [ ] 실사 차이 조정은 관리자 승인 후 원장에 반영한다.
- [ ] BOM 예상 출고량, 실제 출고량과 반환량을 기록해 예상 사용량과 실제 순사용량을 비교한다.

완료 기준:

- 원장 합계와 위치·소유자·lot별 현재고가 일치한다.
- 모든 수량 변화의 원인과 사용자를 추적할 수 있다.

### 3-4. 구매발주·입고·lot 원가

- [ ] 공급처, 자재, 발주수량, 예상단가, 통화, 예정일과 발주 상태를 설계한다. 발주는 기본적으로 공용 재고이며 특정 주문 연결을 선택 기능으로 둘지는 보류한다.
- [ ] 실제 입고수량, 실제 매입단가, 직원 입력 환율, 직접 운송비, 관세, 기타 취득비와 부가세 처리 근거를 lot에 연결한다.
- [ ] 부분 입고와 발주 잔량을 지원한다.
- [ ] 고객 지급 입고와 회사 매입 입고를 분리한다.
- [ ] lot별 실제 취득단가와 보유 회사 재고의 가중평균 참고단가를 계산한다.
- [ ] 공동 비용은 명시된 배분 기준이 있을 때만 나누고, 없으면 미배부로 남긴다.

완료 기준:

- 발주 예상원가와 실제 입고원가가 구분된다.
- 고객 지급 재고가 회사 평균단가에 섞이지 않는다.
- 실제 투입 lot의 원가를 주문에 연결할 수 있다.

### 3-5. 예약·가용재고·부족량

- [ ] 주문별 자재 예약을 실제 입출고와 별도 관계로 설계한다.
- [ ] `가용재고 = 현재고 - 활성 예약량`으로 계산한다.
- [ ] 현재 부족량과 확정 미입고 발주를 포함한 예정 부족량을 함께 표시한다.
- [ ] 주문별 회사 구매 필요량과 고객 지급 요청량을 구분한다.
- [ ] 예약 해제, 주문 변경, 주문 취소가 원장 수량을 바꾸지 않도록 한다.

완료 기준:

- 필요량, 현재고, 예약량, 가용재고, 확정 입고예정, 최종 부족량이 한 화면에서 구분된다.
- 어떤 주문 때문에 부족한지 추적할 수 있다.

---

## 4. 기존 기능 후속 개발

재고의 직접 선행조건은 아니지만 데이터 무결성과 운영 편의를 위해 별도로 처리한다.

### 데이터 무결성

- [ ] WorkLog 저장 후 진행률 snapshot 동기화 실패를 영속 기록하고 재시도하는 구조를 추가한다.
- [ ] `AssignmentPlan.cardId` 사용처를 UI 외부 식별자와 관계 매칭 용도로 분류하고 관계 용도만 실제 FK로 전환한다.
- [ ] WorkLog의 line 메타데이터 정규화 범위를 설계한다.
- [ ] `WorkRecord.lineId -> Line.id`에 제조사 `orgId` 복합 FK가 필요한지와 `onDelete: SetNull` 정책을 확정한다.
- [ ] `PUT /assignment-board-state`에 version 또는 `updatedAt` 기반 optimistic locking을 추가한다.

### 운영 화면과 이력

- [ ] 버킷 변경 고객, 영향 스타일·공정, 복사 원본, 신규 ST, 변경자·변경시각을 FK 기반 이력으로 저장한다.
- [ ] 대시보드에 버킷 변경 영향과 단가/ST 검토 필요 알림 및 검토 완료 기록을 추가한다.
- [ ] 출퇴근 대체 비율과 AT 학습 제외 사유를 운영자가 이해할 수 있는 화면으로 제공한다.
- [ ] 작업기록 중복 입력 오류와 주문 수량 부족·초과 경고를 서로 다른 상태로 표시한다.
- [ ] 스타일과 수량을 선택해 공정별 ST표를 인쇄하는 생산관리 화면을 추가한다.
- [ ] 완료 확정 카드에 연결된 모든 작업월의 급여가 잠기면 운영 배정 보드에서 제외한다.

### AT 증분 갱신

- [ ] 과거 WorkLog 또는 출퇴근 수정 시 전체 초기화 없이 영향 월부터 AT bucket을 다시 만드는 증분 갱신을 설계한다.
- [ ] 같은 작업자의 같은 근무일이 여러 WorkLog에 겹친 경우 fail-closed 진단과 운영 수정 절차를 제공한다.

---

## 5. 재고 이후 수익성과 출고

### 주문별 예상 수익성

- [ ] 청구 가격 스냅샷이 있으면 가장 최근 적용 가능한 청구 라인 단가를 사용하고, 없으면 현재 활성 판매단가와 주문수량으로 예상 매출을 계산한다.
- [ ] 청구 스냅샷과 현재 활성 단가가 모두 없으면 예상 수익률을 0원으로 보완하지 않고 미계산으로 표시한다.
- [ ] 예상 자재원가는 보유 lot, 확정 발주, 견적 등 원가 출처별로 구분한다.
- [ ] 예상 노동원가는 주문에 확정된 CT 스냅샷과 생산수당 초당 단가를 사용하며, 기본급·고정수당은 별도 고정 노동원가 정책으로 계산하고 ST나 AT로 대체하지 않는다.
- [ ] 예상 물류비의 미입력과 실제 0원을 구분한다.
- [ ] 계산 근거가 하나라도 없으면 0으로 대체하지 않고 일부 미확정 또는 계산 불가로 표시한다.

### 월간 생산 가능량과 예상 이익

- [ ] 라인별 가용시간, 배정 카드의 남은 ST, 진행률, 휴일과 기존 일정을 이용해 월간 생산 가능량을 계산한다.
- [ ] AT는 참고 보정값으로만 사용하고 근거 없이 ST를 덮어쓰지 않는다.
- [ ] 생산완료 예상과 출고 기준 매출을 구분한다.
- [ ] 월간 고정비까지 포함할지, 우선 주문별 직접이익만 볼지 정책을 정한다.

### 출고와 실제 이익

- [ ] 출고·반품·취소·정정 이력을 원장으로 만들고 순출고량을 계산한다.
- [ ] 부분 출고의 매출 기준은 청구 라인 스냅샷을 우선하고, 청구 전에는 현재 활성 판매단가를 사용한다.
- [ ] 실제 투입 lot 원가, 실제 노동원가, 실제 물류비를 출고분과 연결한다.
- [ ] 연결 근거가 없는 비용은 임의 배분하지 않고 미배부로 남긴다.
- [ ] 출고 기준 운영매출과 회계상 인보이스 매출은 별도 단계로 구분한다.
